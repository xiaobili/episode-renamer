# SDD ledger — plan: docs/superpowers/plans/2026-09-25-frontend-1-foundation.md

系列计划（本 ledger 统一覆盖，按序执行，同一分支 feat/frontend-refactor）：
1. `docs/superpowers/plans/2026-09-25-frontend-1-foundation.md` — 7 任务（地基）
2. `docs/superpowers/plans/2026-09-25-frontend-2-shell.md` — 7 任务（App Shell）
3. `docs/superpowers/plans/2026-09-25-frontend-3-features.md` — 8 任务（业务组件 + 设置）
4. `docs/superpowers/plans/2026-09-25-frontend-4-polish.md` — 7 任务（收尾 + 验证）

Spec: `docs/superpowers/specs/2026-09-25-frontend-refactor-design.md`（已读，为绑定权威）

分支: `feat/frontend-refactor`（非 main，spec 自身指定的分支 → 隔离工作区成立，不另建 worktree）
MERGE_BASE: d72f4c98
HEAD at start: 81da867

Ruling: 四份分期文档是同一分支上的同一次连续改造，共用本工作区与这一份 ledger — 分四个 ledger 会在第 2 期开始时丢失第 1 期的进度映射，而 compaction 恢复正是 ledger 的唯一用途 — 代价：若某期被单独中止，ledger 里多出与该期无关的行，可读性略降。

Ruling: 计划中大量验证步骤需要浏览器 / DevTools（`npm run dev` + 点击走查）。子代理无法执行这些，全部停在浏览器前会让整个计划无法连续执行。决定：子代理必须执行**机械等价**验证（构建产物 grep、`curl`、量化断言、node 脚本），浏览器步骤改为「报告为 deferred-to-human + 给出精确清单」；所有 deferred 项在最终交付时汇总给用户一次性走查 — 代价：视觉类缺陷（布局错位、颜色偏差、动画观感）在最终人工走查前不会被发现，只能靠产物级断言兜住能兜的部分。

---

## Pre-flight 冲突扫描

### A. 共享文件 / 跨任务接口（逐对）

| 任务对 | 共享物 | 一方产出 vs 另一方消费 | 结论 |
|---|---|---|---|
| F1-T1 ↔ F1-T2 | `style.css` | T1 写 `--font-sans`/`--font-mono`；T2 整文件重写 | 干净：T2 的完整快照里含字体栈两行 |
| F1-T2 ↔ F4-T4 / F4-T6 | `style.css` | T2 加别名块；T4 追加 reduced-motion；T6 删别名块 | 干净：三段互不重叠，T6 删的是 T2 加的那一段 |
| F1-T1 ↔ F3-T2 | `main.js` | T1 加字体 import；T3-T2 加 `useSettingsStore().load()` | 干净 |
| F1-T3 ↔ F2-T2 | `App.vue` | T3 在旧侧栏换 BrandMark；T2-T2 整文件重写为 shell | 干净（覆盖是有意的，F1-T3 正文已声明「第二期会整体重写」） |
| F1-T6 ↔ F2-T3 | `ui/AppSelect.vue` | T6 建 `{modelValue,label,disabled,id}`；T2-T3 Step 2 加 `ariaLabel` | 干净：F1-T6 Interfaces 显式声明 ariaLabel 延后到 F2-T3 |
| F1-T6 ↔ F3-T5 | `ui/AppInput.vue` | T6 建；T3-T5 加 `size` + `ariaLabel` + 内建 `tabular-nums` | 干净：F3-T5 Interfaces 声明两个新 prop |
| F1-T7 ↔ F3-T5 | `ui/AppCheckbox.vue` | T7 建；T3-T5 加 `ariaLabel` | 干净 |
| F1-T5 ↔ F3-T6 | `ui/AppModal.vue` | T5 定义 `title`/`zIndex`；T3-T6 传 `z-index="50"`/`"55"`（字符串） | 干净：`zIndex` 类型为 `[Number, String]` |
| F1-T4/T7 ↔ F2/F3 | `AppButton`/`AppBadge` | 变体名 `primary/secondary/danger/ghost`、tone `neutral/accent/warn/danger` | 干净：全部调用点取值都在 validator 集合内 |
| F2-T1 ↔ F2-T2/T3/T4/T5 | `HomeView.vue` + `stores/workspace.js` | T1 迁状态；T2–T5 逐个改布局 | **缺陷（见裁定 1、2）** |
| F2-T1 ↔ F3-T4 / F3-T8 | `stores/workspace.js` | T4 改 `initialize()`；T8 改两处请求体 | 干净：区域不相交，且都消费 F3-T2 的 settings store |
| F2-T2 ↔ F2-T7 / F4-T5 | `layout/AppTopBar.vue` | T7 窄屏修；F4-T5 aria 审计 | 干净 |
| F2-T3 ↔ F2-T5 | `HomeView.vue` 中段 | T3 建 2 行 grid；T5 同段替换为 2 列 | 干净：顺序替换，T5 明确「替换为」 |
| F2-T4 ↔ F2-T5 | `SourceConfigPanel` 入左栏 | T4 建组件（props 含 `olStore`/`olForm`）；T5 放进 `#source` 插槽 | 干净 |
| F2-T6 ↔ F3-T5 ↔ F4-T2/T3/T5 | `FileTable.vue` | T6 容器与滚动；T3-T5 控件原语化；F4-T2 加 `scanning` prop + 骨架；F4-T3 空态；F4-T5 aria | 干净：`scanning` prop 与 `emptyState` 都在 T6 之后才引入；T3-T5 的 props 快照不含 `scanning`（当时确实还不存在） |
| F2-T2 ↔ F3-T3 | `SettingsView.vue` | T2-T2 加滚动外壳；T3-T3 整文件重写 | 干净（覆盖是有意的） |
| F3-T1 ↔ F3-T2 | `settingsSchema.js` 导出 | T1 导出 5 个符号；T2 import 全部 5 个 | 干净：名称逐字一致 |
| F3-T2 ↔ F3-T3/T4/T8 | settings store 契约 | `toObject/apply/save/storageAvailable/load` + 各字段 | 干净：三处调用点名称与 T2 Interfaces 一致 |
| F4-T2 ↔ `stores/files.js` | 删除 `scanning` | 全项目唯一引用是 FileTable 的蒙层，同任务内被替换 | 干净：T2 Step 5 用 grep 归零 |
| F4-T4 ↔ F4-T1/F4-T2/F1-T4 | reduced-motion 通配符覆盖 | `animate-spin`（AppButton/ExecutingOverlay）、`animate-pulse`（F4-T2 骨架）、组件 `<style>` 过渡 | 干净：通配符 `*,*::before,*::after` + `!important` 一次覆盖三类载体 |
| F1-T2 ↔ F4-T6 | 别名块 vs 禁用正则 | T2 的 17 个别名名 vs T6 的 kill-list 17 名 | 干净：已逐名比对，且 T6 的正则不误伤新名 `border-border-control` |

### B. 各任务自身文本一致（测试/验证 vs 代码/文件）

| 任务 | 自洽性检查 | 结论 |
|---|---|---|
| F1-T1 | 验证 grep `Geist Variable` vs 字体栈字面值 | 干净 |
| F1-T2 | 15 个新 token 名 vs 验证 grep 列表 | 干净 |
| F1-T3 | favicon 内联色 `#0F766E` vs `--color-accent` | 干净 |
| F1-T4 | 验证 grep 的 11 个类名 vs VARIANT/SIZE 字面量 | 干净 |
| F1-T5 | 三个对话框调用方 vs 删除的 `backdrop`/`backdropBlur`/`close` | 干净：Step 2 有对应调用方 grep 校验 |
| F1-T6 | 验证 grep 10 类 vs 模板字面量 | 干净 |
| F1-T7 | AppPanel 只剩 `title` vs F3-T3 只用 `title` | 干净 |
| F2-T1 | 替换对照表覆盖模板全部绑定 vs 新 script 的标识符集合 | **缺陷（见裁定 1、2）** |
| F2-T2 | 删除 SourceTabs 后 `sources` 数组的归属 | **缺陷（见裁定 2）** |
| F2-T3 | `<Toast v-if>`（F3-T7 才移除）不影响本任务 | 干净 |
| F2-T4 | 4 处手写样式在本任务被清 vs 全期 18 处账 | 干净：本期清 8（4+3+1），第三期清 10 |
| F2-T5 | `insertVariable` 经 `$el.querySelector('input')` 取原生 input vs AppInput 根为 div | 干净 |
| F2-T6 | md 列宽和 904≈900 vs 窄屏 552；验证行给量化判据 | 干净 |
| F2-T7 | 三条窄屏修法都给了代码而非「视情况」 | 干净 |
| F3-T1 | 断言脚本 34 条断言 vs 实现覆盖 | 干净 |
| F3-T2 | store 初值用默认 + `load()` 才读盘 vs main.js 调用 | 干净：Step 3/4 各有验证 |
| F3-T3 | `padRangeError` vs `AppInput.error` 契约 | 干净 |
| F3-T4 | 预设回落链 vs 空 `defaultTemplateId` | 干净：Step 2 第 5/6 项覆盖 |
| F3-T5 | `tabular-nums` 写在 AppInput 内部而非调用点 class | 干净：已在正文说明根 div 透传无效 |
| F3-T6 | `<td colspan>`/结果表 vs AppBadge | 干净 |
| F3-T7 | `<Transition>` + 父组件 `v-if` 冲突 | 干净：T7 Step 1 末条说明须去掉 HomeView 的 `v-if` |
| F3-T8 | 后端未合入时须跳过 vs 前置声明 | 干净 |
| F4-T1 | `@keydown.tab.prevent` + `tabindex="-1"` 自洽 | 干净 |
| F4-T2 | `h-[58px]` 标注为待校准 + 校验步骤 | 干净 |
| F4-T3 | 空态移出 `<td>` 的理由 + 走查项 | 干净 |
| F4-T4 | 兜底 `duration .01ms` 的理由（Transition 依赖 transitionend） | 干净 |
| F4-T5 | 审计正则覆盖面 + 已知盲区（textarea / div@click）补齐命令 | 干净 |
| F4-T6 | 禁用正则 vs 产物 CSS 校验 | 干净 |
| F4-T7 | §15 九项逐项落位 | 干净 |

### C. 计划文本明确要求、但评审规则会判为缺陷的东西

无。本系列不含测试断言真空、也不要求逐字复制逻辑块（六处原语各自独立，非复制粘贴）。

---

## Ruling 记录（preflight 阶段）

Ruling 1（计划缺陷，已改计划文本）：F2-T1 的 `HomeView.vue` 新 `<script setup>` 只定义了 `ws`，但模板仍引用 `filesStore`（FileTable 的 `:files-store`）、`tplStore`（TemplateConfig 的 `:tpl-store` 及两个 `@update:` 处理器）、`olStore`（ScanPanel 的 `:ol-store`）、`sources`（SourceTabs 的 `:sources`）——四个标识符全部会变成 `undefined`，且 Vue 只在 dev 下告警、构建不报错，F2-T1 Step 4 的走查会大面积失败。修正：这三个 store 实例与 `sources` 数组在 F2-T1 一律保留，并补进替换对照表标注「不变」；`sources` 由 F2-T2 Step 3 删除（其唯一消费者 SourceTabs 在那一步消失）。— 代价：若我判错，HomeView 多带三个 store 实例与一个数组到后续任务，属无害冗余。

Ruling 2（计划缺陷，已改计划文本）：F2-T1 的替换对照表把 SourceTabs 写成 `v-model="ws.activeSource"`。`v-model` 直接赋值会绕过 `switchSource()`，而「存旧源 files/scanResult、载新源」正是 `switchSource()` 的职责 —— 结果是切换数据源后 `activeSource` 与 `path`/`previewRows` 都变了、`filesStore.files` 却停在旧源，表格显示错的文件。修正：改为 `:model-value="ws.activeSource"` + `@update:model-value="ws.switchSource($event)"`，并在正文写明理由（F2-T2 搬进顶栏后同样如此）。— 代价：若我判错，切源时多调用一次 `switchSource`（幂等，无副作用）。

Ruling 3（工作区归属）：四份分期文档共用本工作区与 ledger（见上方 Ruling）。— 代价：见上。

---

## 进度

| 任务 | 状态 |
|---|---|
| F1-T1 Geist 字体自托管接入 | 待执行 |
| F1-T2 设计 token 重写 + 过渡别名层 | 待执行 |
| F1-T3 品牌标记 SVG + favicon | 待执行 |
| F1-T4 AppButton | 待执行 |
| F1-T5 AppModal 重写 | 待执行 |
| F1-T6 AppInput + AppSelect | 待执行 |
| F1-T7 AppCheckbox + AppBadge + AppPanel | 待执行 |
| F2-T1 workspace store | 待执行 |
| F2-T2 AppTopBar + App Shell | 待执行 |
| F2-T3 AppBottomBar + 两行骨架 | 待执行 |
| F2-T4 SourceConfigPanel | 待执行 |
| F2-T5 AppLeftRail + 三栏成型 | 待执行 |
| F2-T6 FileTable 撑满 + 内部滚动 | 待执行 |
| F2-T7 响应式三档收口 | 待执行 |
| F3-T1 settingsSchema + node 断言 | 待执行 |
| F3-T2 settings store + main.js | 待执行 |
| F3-T3 SettingsView 重写 | 待执行 |
| F3-T4 工作区接入设置 | 待执行 |
| F3-T5 FileTable 原语化 | 待执行 |
| F3-T6 三个对话框适配 | 待执行 |
| F3-T7 Toast 适配 | 待执行 |
| F3-T8 补零位数接线 | 待执行（依赖后端计划合入，未确认） |
| F4-T1 ExecutingOverlay 精简 | 待执行 |
| F4-T2 表格骨架屏 | 待执行 |
| F4-T3 空态重做 | 待执行 |
| F4-T4 reduced-motion 兜底 | 待执行 |
| F4-T5 aria / focus-visible 收口 | 待执行 |
| F4-T6 删除过渡别名层 | 待执行 |
| F4-T7 §15 全量验证 + 截图 | 待执行 |

deferred-to-human（浏览器走查，最终一次性交付）：
（暂无）

---

## 执行日志

- BASE for F1-T1: `8bce8c53`（计划修正提交后）。F1-T1 已派发（implementer, haiku）。
- F1-T1 报告 DONE_WITH_CONCERNS（commit dbc01cb）。**裁定 4（dist 跟踪策略）**：核实后确认 `backend/app/main.py:60-73` 挂载 `frontend/dist` 作 `/assets` + SPA fallback，即 `python run.py` 的单端口部署依赖它（Dockerfile 不用它，是 node 阶段现构建）。因此 **dist 必须继续跟踪**，不能 gitignore。但改为**按阶段**提交 dist 而非按任务：29 个任务几乎每个都重建 dist（约 400 KB/次，含字体二进制），逐任务提交会给历史增加约 12 MB 且不改写历史无法回收；阶段边界已是「可构建可运行可部署」的完整状态。已把「阶段收尾」步骤写入四份文档（commit 7808376）。任务内 `git add` 列表不含 dist，构建后它呈现为未暂存修改，不影响 BASE..HEAD 的评审区间 — 代价：若我判错，某个中间提交的 dist 与源码不同步，从该检出直接起后端会 serve 过期前端；回退方式是在任意提交上重跑 `npm run build` 并提交。
- F1-T1 关注点 3（npm 报 2 个漏洞、esbuild/vue-demi 安装脚本告警）：**与本次改动无关的既有问题**，implementer 正确地没有跑 `npm audit fix`。记为 deferred minor，交最终评审 triage。
- **裁定 5（F3-T8 前置不满足）**：实测 `backend/app/models/api.py` 的两个 Request 模型均无 `episode_pad_digits` / `season_pad_digits`，`backend/tests/` 与 `backend/requirements-dev.txt` 都不存在 —— 后端计划 `2026-09-25-backend-pad-digits.md` **未实施**。该任务自己的文本规定此情形下必须跳过。裁定：跳过 F3-T8，不代为执行后端计划（它是独立计划、有自己的工作区与 ledger，本轮授权范围是前端四期）。后果：spec §3 第 11 条「补零位数是假设置」未闭环、§15.3 的闭环验证不适用 —— 交付时明确列给用户，并说明可另起一轮 SDD 执行后端计划。— 代价：前端设置页会把补零位数发出去，但后端静默丢弃该字段（Pydantic 默认忽略未知字段），表现为「设置改了没用」且零报错；F3-T8 的 Step 4 排查路径已写明如何区分这两种原因。
- **F1-T1 完成**：评审 Spec ✅ / Approved，无 Critical 与 Important。构建闸门由我独立复跑确认（`rm -rf dist && npm run build` → 无警告；两个 woff2 产出；`Geist Variable` / `Geist Mono Variable` 均在产物；`font-family:` 全量枚举无裸 `Geist`；产物 CSS 哈希与实现者报告一致 → 构建确定性）。
- F1-T1 minor（deferred）：①`--font-mono` 里 `ui-monospace` 排在 `"SFMono-Regular", Menlo` 之前，使后者不可达 —— 无实际影响，且逐字来自计划；②实现者报告里把 style.css 的改动写成 8 插入/2 删除，实为 6/2；③注释里「回退到中文字体」措辞略粗。
- F1-T1 deferred-to-human：`npm run dev` 后 `getComputedStyle(document.body).fontFamily` 应以 `"Geist Variable"` 开头；Network 只应请求 latin / latin-ext 子集。
- **裁定 6（计划缺陷，已改计划文本）**：F1-T2 Step 2 原本要求「新 token 的工具类都生成 ≥1」。实测（Task 1 后构建产物）证明这是错的：Tailwind v4 的 `@theme` 无条件把主题变量写进 `:root`，但**只为源码里出现过的候选类名生成工具类**。Task 2 只改 `style.css`，此时没有任何组件用新 token，所以这些工具类**必然全为 0**。原闸门会把实现者引去「修」一个不存在的问题，甚至去改 `@theme` 的名字，反而破坏后续 27 个任务。已改为：Step 2 查 **17 条主题变量 + 3 条阴影/字体变量**是否落到 `:root`（这才是 `@theme` 写对没写对的判据），并显式写明工具类为 0 是正常的、不要去修；Step 3 改查**别名**的工具类必须 ≥1（由 11 个旧组件使用）+ 17 条别名变量。— 代价：若我判错，Task 2 的验证会漏掉某个新 token 名字写错；由 Task 3 起的组件改造会立刻暴露（组件用了不存在的类名时样式块为 0）。
- F1-T2 报告 DONE_WITH_CONCERNS（commit 7fc936b）。两个 concern 都经我独立实测确认，且第二个是真的构建缺陷：
- **裁定 7（确认实现者的发现 + 定修法）**：①实现者发现 Tailwind 4.3.3 **默认会 tree-shake 未被引用的 `@theme` 变量** —— 实测 `--color-danger-hover` 与 `--shadow-overlay` 缺席 `:root`（其余变量因别名块的 `var()` 引用而幸存），我的 Step 2 闸门期望 17+3 因此仍不成立。②实现者发现 Tailwind **会自动扫描已跟踪的 `dist/`**（它不在 .gitignore 里），把上一版构建的类名当候选，产出 `.hidden/.static/.table/.filter` 幽灵工具类，且产物大小随 dist 是否存在而变 —— **构建不确定**，会让后续所有任务的 grep 计数闸门漂移。我在 /tmp 探针上实测了两个修法（Tailwind 4.3.3）：`@theme static` 让未引用变量也发射且**工具类仍按需生成**（无膨胀），代价实测 **2 个变量 / 72 字节**（先前看到的 +3.9KB 是 `du` 报磁盘块、不是字节数，属误读）；`@source not "../dist"` 干净消除幽灵工具类。决定：token 块用 `@theme static`（设计 token 是系统公开接口，不该取决于谁碰巧用了它；组件 `<style>` 块里的 `var(--shadow-overlay)` 遇上 tree-shaking 就是静默失效）、**别名块不加 static**（它是第四期要删的脚手架，按需发射正好）、并在快照里加 `@source not "../dist"`。已改计划正文（含两处机制说明），重生成 brief，resume 实现者应用。— 代价：若我判错，代价是 :root 里多 72 字节，或 Tailwind 的自动源码探测漏掉某个应当扫描的目录（已确认 src/ 与 index.html 仍在范围内）。
- F1-T2 修复完成（commit 38f2b19）。**事件：实现者 `git commit --amend` 打到了 HEAD（我的 docs 提交 23bee91）而非它自己的 7fc936b**，短暂吞掉我的提交，随后用 `git commit -C 23bee91` 还原为 b03bb76。我独立核实：`git diff 23bee91 b03bb76` 输出 0 行、提交信息/作者/日期逐项一致、历史线性（7fc936b → b03bb76 → 38f2b19）、三个提交均未触碰 frontend/dist。**结论：内容逐字节等价，无需回滚**；仅 SHA 变化，旧 SHA 仍在 reflog。根因是我在修复指令里说了「amend 7fc936b」却又说「amend 或追加提交均可」——`--amend` 只作用于 HEAD，措辞有歧义。**后续所有修复指令一律只说「追加一个修复提交」，禁止 amend。**
- F1-T2 修复后闸门达标：Step 2 命中 **17 条 color 变量 + 3 条 shadow/font 变量**（含 `--color-danger-hover:#8e1d17`、`--shadow-overlay:…`）；四个幽灵工具类（`.hidden/.static/.table/.filter`）归零而 src/ 与 index.html 扫描仍正常；构建确定性已验证（dist 存在与否给出相同的 `index-CrkyUZl2.css` / 27.35 kB）。`static` 确实不强制生成工具类（`bg-danger-hover` / `shadow-overlay` 仍为 0）。
- F1-T2 披露：字节代价在本仓库实测为 +98 而非探针里的 +72（压缩后的 hex/分号记账不同），量级一致、结论不变。
- **F1-T2 完成**：评审 Spec ✅ / Approved，无 Critical 与 Important。评审在 `dist` **存在**的前提下实测幽灵工具类为 0（比实现者的 `rm -rf dist` 变体更强），并独立确认 17 条别名变量 + 14 个别名目标都在产物中、11 个旧组件依赖的 14 个旧工具类全部生成、`text-error-light`=0 是正确的（`src/` 里确实没人用）。评审还主动查了一个具名风险：是否有组件在 `<style>` 块里用 `var(--color-旧别名)` —— 全 `src/` 只有 `style.css` 命中，风险不成立。
- F1-T2 minor（deferred）：①`style.css:15` 注释写的字节数 72 是本仓库未验证的（实测 +98）—— 我已把**计划文本**改成「百字节级（本仓库 +98 字节）」，但**代码注释仍写 72**，需最终整支评审的 triage 扫掉；②`7fc936b` 与 `38f2b19` 提交标题完全相同，`git log` 读不出哪个是修复轮（改写已评审的 SHA 不可取，故不修）；③前瞻提示：别名块刻意非 static，若有组件在 `<style>` 块里 `var(--color-text-faint)` 会被 tree-shake —— 已据此给第二、三期加了全局约束「组件 `<style>` 块不得引用旧别名 var()」。
- F1-T2 deferred-to-human：视觉回归对照 `screenshot/home.png` / `screenshot/settings.png` 的逐项判据（布局需逐像素一致、只有配色变化）。
- BASE for F1-T3: 423f218a3d456d422212277b5c16c59a395f45f1 (423f218)
- **F1-T3 完成**：评审 Spec ✅ / Approved，无 Critical 与 Important。评审逐元素核对了 favicon 与组件的几何一致性（rect 属性、两条 path 的 `d` 字符串、stroke-width、linecap/linejoin、viewBox 全部一致，差异只有两处刻意的颜色替换），确认 `index.html` 未混入预加载、`aria-hidden` 在 svg 根上、`size` 同时绑到宽高、`App.vue` 用的是新 token `text-accent`、四文件范围严格。构建闸门由我独立复跑确认（无告警、无错误、`dist/favicon.svg` 就位、App.vue emoji 计数 0）。
- F1-T3 minor（deferred）：①箭头白来自 `text-white`，即 Tailwind 默认调色板的 `--color-white`，本项目未声明白 token —— 若将来做 `--color-*: initial` 调色板收窄，箭头会静默变成青绿叠青绿且构建不报错。**但这是全站性约定，不是 BrandMark 的问题**：spec §8.2 的 AppButton 契约本身就写 `bg-accent text-white`，`text-white` 贯穿按钮与复选框。正确的修法位置是「将来收窄调色板时补一个 `--color-white` token」，而不是单独改 BrandMark。②`shrink-0` 把 flex 布局决策写进了展示型组件（源自被替换掉的那个 div 的类）。
- F1-T3 deferred-to-human：侧栏左上角应为深青绿圆角方块 + 白色向右箭头；标签页图标与之一致（需强制刷新绕开 favicon 缓存）；箭头不应有填充溢出；右侧「Episode Renamer」文字位置未位移。
- **裁定 8（计划缺陷，已改计划文本）**：F1-T4 的 Step 2 闸门脚本本身是坏的，实现者正确识别并拒绝照抄。三处叠加的缺陷：①`grep -o "\.$c{"` 要求类名后紧跟 `{`，但 Tailwind 产出的变体选择器是 `.hover\:bg-accent-hover:hover{`、`.disabled\:opacity-45:disabled{`，匹配不到；②类名列表里做了双重转义（`"hover\\\\:bg-accent-hover"`），bash 解析后得到 `hover\\:bg-accent-hover`，而 CSS 里只有单反斜杠；③任意值类（`rounded-[8px]`、`scale-[.98]`）的 `[`/`]`/`.` 同样被转义。三者叠加使 11 项里 6 项给出**假的 0**。修正：改用 `sed 's/\\//g' dist/assets/*.css | grep -oF -- "$c"`（剥掉 CSS 里的反斜杠转义后做固定串匹配），类名列表一律写未转义的原样形式，并加 `--` 分隔符（否则 `-translate-y-1/2` 会被 grep 当成选项报错）。四处闸门（Task 2/4/6/7）整块重写。实测：Task 4 的 11 项全部转为 ≥1 且与设计一致；搜不存在的类名为 0；Task 6/7 此时大多为 0 是正确的（对应原语尚未创建）。— 代价：若我判错，闸门从「精确匹配选择器」放宽为「类名字符串出现在产物中」，理论上一个拼写错误若恰好是某个合法类名的子串会漏报（例如 `bg-accent` 之于 `bg-accent-soft`）—— 可接受，因为拼错的类名通常不会是任何合法类名的子串。
- **事件（我方失误，已修复）**：我在修闸门时用「扫到下一个 `done` 为止」的整块替换脚本改计划文档，但 Task 2 Step 3 的别名循环终止行是 `done && \` 而非独立的 `done`，扫描没能停下，**一路吞掉了 Task 3 与 Task 4 整节**（1392 → 1188 行、7 个任务变 5 个）。`assert g is not None` 没能拦住，因为被吞掉的区间里恰好也含有一个合法的 Task 4 闸门标识。已从 `423f218` 恢复，改用**精确字符串替换**重做：替换前先断言「`### Task` 标题集合前后完全一致」，替换后校验净变化只有 14 行删除（全是旧 grep 行与被转义的类名列表）。现文档 1405 行 / 7 个任务，五条闸门逐条实跑通过。教训：**不要用「扫到某个终止符」的方式结构化改写长文档**，改为按精确文本替换 + 标题集合断言。
- **裁定 8（计划缺陷，已改计划文本）**：F1-T4 的 Step 2 闸门脚本本身是坏的，实现者正确识别并拒绝照抄。三处叠加：①`grep -o "\.$c{"` 要求类名后紧跟 `{`，但变体选择器是 `.hover\:bg-accent-hover:hover{`，匹配不到；②类名列表被双重转义（`"hover\\\\:bg-accent-hover"`），bash 解析后多一层反斜杠；③任意值类的 `[`/`]`/`.` 同样被转义。三者叠加使 11 项里 6 项给出**假的 0**。修正为 `sed 's/\\//g' dist/assets/*.css | grep -oF -- "$c"`（剥掉 CSS 的反斜杠转义后做固定串匹配），类名一律写未转义形式，并加 `--` 分隔符（否则 `-translate-y-1/2` 被 grep 当选项报错）。共改 5 处闸门。实测：Task 4 的 11 项全部 ≥1，Task 2 Step 3 的 6 个别名类全部 ≥1 且 17 条别名变量齐全，搜不存在的类名为 0；Task 6/7 此时大多为 0 是正确的（对应原语尚未创建）。— 代价：闸门从「精确匹配选择器」放宽为「类名字符串出现在产物中」，理论上一个拼写错误若恰是某个合法类名的子串会漏报（如 `bg-accent` 之于 `bg-accent-soft`）—— 可接受，拼错的类名通常不会是任何合法类名的子串。
- **F1-T4 完成**：实现者复跑修正后的闸门，11 项全部 ≥1；未新建提交（修复只涉及验证闸门，不涉及代码），确认 `eea5fea` 完好、`git diff eea5fea -- AppButton.vue` 为空、不是 amend 产物、不含 dist。
- F1-T4 残留观察（实现者提出，我采纳并据此又改了一次计划）：`grep -oF` 是**无锚点子串匹配**，所以 ①`bg-sunken` 计到 1 其实是命中 `.hover:bg-sunken:hover` 的子串 —— 也就是说我那句「既不漏也不误报」是错的，我等于把「列表写成了从不单独出现的裸形式」这个问题**掩盖**了而不是修掉；②截断型写法（`rounded-[8p`）会假通过。**裁定 9**：不改成带锚点的正则（任意值类的 `[` `]` `.` 在 ERE 里需要再转义一层，比它要检查的东西更易错），改为**把三处列表项改成组件实际使用的完整形式**（`bg-sunken`→`hover:bg-sunken`、`ring-offset-canvas`→`focus-visible:ring-offset-canvas`、`ring-accent/35`→`focus-visible:ring-accent/35`），并把闸门的「能证明什么 / 不能证明什么」逐条写进计划正文（含为何不用锚点的理由）。— 代价：闸门仍抓不到截断型拼写，已在计划里明确声明，不再宣称它能。
- F1-T4 的闸门列表修正后复核：Task 4 的 11 项**零命中**，Task 6/7 此时全零属正确（原语未创建）。
- **裁定 10（F1-T4 评审的 Important 发现，经我独立核实成立）**：`AppButton` 基类串没有 `cursor-pointer`，而 **Tailwind v4 的 preflight 删掉了 v3 的 `button { cursor: pointer }`**（实测 4.3.3 的 `preflight.css` 全文无任何 button 光标声明，只在注释里提到 Safari 的数字输入微调按钮）。后果面比评审指出的更广：改造前应用有 **9 处手写 `cursor-pointer`**（App.vue / ScanPanel / TemplateConfig / BrowseDialog），而我的第二、三期引入了 **15 个裸 `<button>`（8 + 7）与零个 `cursor-pointer`** —— 第四期把 9 处手写替换成原语后，全站按钮的指针光标会静默消失。裁定：**不加在组件上，而是在 `style.css` 基线里恢复一次** `button:not(:disabled) { cursor: pointer }`。理由：①一处胜过 15+ 处且新写的按钮不会漏；②`:not(:disabled)` 让 `disabled:cursor-not-allowed`（特异性 0,2,0 > 0,1,1）继续生效；③改造前那 9 处手写其实是 v3 时代的冗余，真正表达意图的是「按钮可点」这一条基线。已把该规则与理由写进 Task 4 的新 Step 2、并给第二/三/四期各加一条全局约束（裸 `<button>` 无需再写 cursor 类，也不得写冲突的）。— 代价：基线规则作用于全部 `<button>`，若将来出现「看起来不可点但没设 disabled」的按钮，它仍会显示指针光标；这在当前应用里没有这种按钮。
- **F1-T4 完成**：再评审裁定「全部发现已解决，无新增 Critical/Important」。基线规则落在 `style.css` 的层外、选择器精确为 `button:not(:disabled)`、`AppButton` 未混入 `cursor-pointer`、产物中锚点断言为 1。修复轮：commit `f82a63b`。
- F1-T4 minor（deferred）：①`style.css:120-121` 的注释**理由写错了**（我写的），且 `AppButton.vue` 无任何注释 —— 四个不显然的不变量（`undefined` 而非 `false`、`disabled || loading`、`transition-colors`、`opacity-45`）只存在于计划文档里；②variant/size 映射无兜底（Vue 生产构建会剥离 prop validator，未知 variant 会得到无配色的按钮）；③`type` 缺 validator（与 `variant`/`size` 不一致）；④两张映射表写在 `<script setup>` 里，每个实例都会重新分配（该原语将来会在列表里按行实例化）；⑤`text-white` 非设计 token，与 BrandMark 的 minor 同源，属全站约定；⑥`loading` 同时置 `disabled`，于是加载中的按钮也会 45% 透明 + `not-allowed` 光标 —— 是两条要求叠加的必然结果，值得交第四期判断是否要区分「工作中」与「不可用」。
- **裁定 11（我方注释的技术性错误，已核实并改计划文本）**：我在 `style.css` 的注释里写「`:not(:disabled)` 让 `disabled:cursor-not-allowed` 生效，因为该工具类特异性 (0,2,0) 高于本规则 (0,1,1)」。**理由是错的**。实测（按花括号配对判断层归属）：`.disabled\:cursor-not-allowed` 在 `@layer utilities` 内，而基线规则在**层外**；按 CSS 级联层规则，无层样式优先于任何层内样式，**与特异性无关** —— 若写成裸 `button { cursor: pointer }`，哪怕工具类特异性更高，禁用态也会被覆盖成指针光标。真正起作用的是 `:not(:disabled)` 让两者不在同一元素上竞争。已把计划文本（Task 4 Step 2 的规则注释 + 第二/三/四期的全局约束）改为正确理由。**代码注释仍是错的**（Minor，交最终整支评审 triage）。— 代价：若我判错，未来的维护者可能按错误理由「优化」选择器；但改后的说明与实测一致。
BASE for F1-T5: 8d20d55c1a3f2442b6d84be890a6f27679cc267b
- F1-T5 实现完成（commit 6a739eb，+147/−22，单文件；Step 2 的消费者 grep `backdrop|backdropBlur|@close` 无输出，证实被删的 props/emit 确无调用方）。实现者报告 DONE，列出四条关注点：①`aria-labelledby` 目前**是惰性的** —— 三个对话框都还没传 `title`（第三期 Task 6 才传），所以这一期的无障碍改进要到第三期才真正生效；②`watch(..., { immediate: true })` 在 setup 期执行，若消费者挂载时 `modelValue` 已为 true，关闭时会把焦点归位到 `<body>`（无害空操作，当前不可达，按 brief 保留）；③浏览器走查未做（无浏览器），已在报告里逐字复现 8 行表格与两段 Console 构造供人工执行；④dist 因构建变脏且刻意未暂存。
- F1-T5 评审：Spec ✅ / Approved。1 条 Important + 6 条 Minor。评审按我要求的「生命周期状态机」逐条追踪并确认：监听器在四条关闭路径（Esc / 点遮罩 / 父组件改 modelValue / 打开态卸载）上增删**完全对称**；滚动锁先存后写、恢复幂等；焦点归位对已卸载触发元素确有 `document.contains` 防护；零可聚焦与焦点在面板外两种边界都不会漏到背后。它还独立 grep 确认 `AppModal.vue` 是 `src/` 里**唯一**写 `document.body.style.overflow/paddingRight` 的地方，以及三个对话框都是急切挂载（无 `v-if`），因此挂载期先跑关闭分支无害 —— 结论有据而非假设。
- **裁定 12（Important，计划自身缺陷，已改计划文本并派发修复）**：模特的**离场**过渡是 150ms/130ms，而进场是 200ms —— 与我自己写在该块上方的注释「模态进出 200ms」以及 spec §11（第 432 行：`| 模态进出 | 状态转换：层级来源 | 200ms |`）**直接矛盾**。成因是我从旧文件照抄了 leave 的两行而只改了 enter。已改为进出都 200ms（同时消除 leave 内部 opacity 150 vs transform 130 的不一致），并在注释里写明「不要因为『收起可以更快』改回去」。— 代价：离场淡出比原来慢 50ms，纯观感。
- **裁定 13（把一条 Minor 并入本轮修复，并说明这次为何破例）**：评审把「`package.json` 声明 `vue: ^3.4.21`，而 `AppModal` 用的 `useId()` 需要 ≥3.5」标为 Minor，理由是它超出本任务的单文件范围。我把它提上来处理：①超出范围这件事是**我**定的范围，我作为计划作者可以改；②它不是措辞或观感问题，而是**清单与代码实际要求不符**（一旦有人把 vue 固定到 3.4.x，三个对话框都会在 setup 抛 `useId is not a function`）；③我本来就要为本轮 Important 派发一次修复，它的边际成本接近零。实际风险不高（caret 允许 3.5.x，npm 会解析到 3.5.43），所以这是「清单诚实」修正，不是应急。**明确记录这条破例，以免与我对 BrandMark 的 `text-white`、字节数笔误那两条「Minor 只记账不修」的处理显得不一致：那两条是注释与约定层面、零行为影响；这一条是清单正确性。**— 代价：`package.json` 与 lockfile 多一处改动。
- F1-T5 修复轮 1 完成（commit `4e53bcd`，3 文件 +8/−5）。我核实：`package.json:20` 与锁文件根范围均为 `^3.5.0`，`node_modules/vue` 与锁内 resolved 都仍是 3.5.43（未降级/升级）；离场过渡两侧均 0.2s；`6a739eb` 未被 amend。**裁定 14**：保留 `^3.5.0` 而非实现者建议的备选 `^3.5.43` —— `useId` 自 3.5.0 起就有，任意 3.5.x 都满足代码要求，收紧到 `^3.5.43` 会无理由地把 3.5.0–3.5.42 排除在外。— 代价：若我判错，底线略宽松，但不会造成功能问题。
- 实现者指出一处我预测错的地方：我在指令里写「预期 `package.json` 变为 `^3.5.0`」，但 `npm i vue@^3.5.0` 实际写入的是**解析到的版本** `^3.5.43`（npm 11.19.0 的 save-prefix 行为），它改为手写范围 + `npm install` 同步锁文件才落到我要求的字面状态。**这是我在本轮里第三次凭记忆断言工具行为而未先实测**（前两次是 Tailwind 的 `@theme` 发射语义与类名转义）。已把这条模式写进 ledger：凡在指令里写「预期输出」的，要么先实测，要么明确标注为未验证的猜测。
- **F1-T5 完成**（commits `6a739eb` + 修复轮 `4e53bcd`；再评审：「全部发现已解决，无新增 Critical/Important 破坏」）。再评审逐项确认离场两侧同为 0.2s、`0.15s`/`0.13s` 已全无残留、进场与 `-from`/`-to` 状态块都是未改动的上下文行（只改了时长与一段注释）、锁文件 stat 为 `2 +-` 且两行都在根范围行上（无其他依赖被动到）。评审给出的 6 条 Minor 全部记账不修（其中 2 条已在轮内处理，见裁定 12/13）。
- F1-T5 minor（deferred）：①`lockScroll()` 不具重入性、且 `immediate: true` 让挂载期先跑关闭分支 —— 今天无害（已核实 `AppModal.vue` 是 `src/` 唯一写 body 内联样式处，且三个对话框都急切挂载），但若将来有消费者用 `v-if` 惰性挂载对话框且此时已有另一个打开，新实例的挂载期关闭会把打开者的滚动锁抹掉；修法是加一个 `let locked` 标志；②焦点陷阱的可见性过滤漏掉 `visibility:hidden` / `inert` / `aria-hidden` 的可聚焦元素（浏览器会跳过它们，可能导致一次 Tab 漏到背后，是否可达取决于第三期对话框的内容）；③离场过渡的 ~150ms 窗口内 Tab 不再被陷阱拦截（监听器在 `modelValue` 翻转时就移除，而 `v-if` 元素要等过渡结束才卸载）；④`immediate: true` 这个不显然的构造没有注释；⑤面板是 `outline-none` + `tabindex="-1"`，零可聚焦回退时焦点被「隐形停靠」，与 `getClientRects` 过滤想避免的「Tab 像卡死」症状同源。
BASE for F1-T6: 4e53bcd878ccc629a8fa3f1a5f84becfa1f6aa5e
- F1-T6 实现完成（commit `c4eab55`，2 文件 +98）。实现者报告 DONE，并给出三条 concern：①**构建从不编译这两个文件** —— 没有 importer 就不在 Vite 的模块图里，所以「build 通过」不证明它们能编译；它自行用 `vue/compiler-sfc` 编译 + SSR 渲染输出 + 自定义渲染器 emit 校验补上了这个缺口，临时装置已删、未加依赖；②空 `placeholder`/`value` 会渲染成裸属性（按 brief 逐字实现，无浏览器/辅助技术影响）；③语义提示：用户**手输 `0`** 会 `emit(0)`，只有**清空**才 `emit(null)` —— 未设置与零的区分是保留的，第三期的调用方不要假设 `0` 不可达。
- **裁定 15（计划缺陷，已核实并固化进计划）**：实现者的第①条 concern 指出了一个**影响全部原语任务的验证盲区** —— Vite 只打包从入口可达的模块，本项目一期刻意「先建六个原语、暂不接线」，因此 `npm run build` 通过**不证明**这些文件可编译：语法错误、模板里的非法指令、`<script setup>` 里调用未定义函数，构建都不报错，要到第二/三期第一次 import 时才炸。**Task 4 的 `AppButton` 同样从未被编译过。** 我先用 `vue/compiler-sfc` 对全部 18 个 `.vue` 跑了一遍：全部可编译（含三个未导入的原语），缺口对当前状态已闭；再把校验固化为 `frontend/scripts/check-sfc-compile.mjs`（Task 7 的新 Step 6 创建并提交，各期「阶段收尾」各加一条执行项），因为第二、三期还会继续新建「先建后接线」的组件。已从计划里抽出脚本实跑验证可用（18 个文件全 OK、退出码 0）。— 代价：仓库多一个约 1.7KB 的开发脚本；它不在 spec 的组件清单里，属我为堵验证盲区而加的**验证工具**，不是功能代码。
- F1-T6 评审：Spec ✅ / Approved，无 Critical 无 Important，7 条 Minor。评审做了几处有价值的独立验证：`lucide-vue-next` 确实导出 `ChevronDown`（**这很关键** —— 因为构建从不编译 `AppSelect.vue`，一个坏 import 要到第三期才暴露）；`useId` 的 per-instance 计数器保证 `input-v-N`/`select-v-N` 不会撞；产物里 `focus-visible:ring-accent/35` 确实解析成 `color-mix(... 35%, transparent)` 加 `#0f766e59` 回退（**35% 是真的**，而「只查类名存在」的子串闸门抓不到这一点）；`null` 契约穿过 Vue 的 prop 层仍然成立（`value == null && !required` 时 validateProp 提前返回，`default: ''` 只对 `undefined` 生效）。
- **裁定 16（我的约束与自己的任务书互相矛盾，已改计划文本）**：`AppButton` 的 `secondary` 变体用 `border-line-strong`（1.52:1），而我在 Global Constraints 里写的是「`line`/`line-strong` **禁止**用于交互控件」。核实后确认矛盾源自 spec 自身：§5.1 第 172 行那句措辞过宽，但同一节第 151 行给出的**理由**明确只针对 input（「`#CDD2D7` 对白底仅 1.52:1，**用作 input 边框**是不合格的」），而 §8.2 第 346 行的具体类表给 secondary 按钮定的就是 `border-line-strong`。裁定：按理由而非按过宽的措辞执行 —— **表单控件**（input/select/checkbox/textarea）的边框是它唯一的识别线索，必须 3:1，用 `border-border-control`；**按钮**靠文字标签识别（`ink-2` 对白底实测 7.94:1），边框属装饰，保留 `border-line-strong`。已把两期文档的约束改成这个具体二分，并写明「不要去『修』按钮的边框」。这是具体规则压过一般规则，也与主流设计系统一致（Primer / Linear / Vercel 的次级按钮边框都不达 3:1）。**spec 第 172 行的措辞本身仍过宽** —— 那是用户的文档，我不擅自改，会在最终交付里点出来。— 代价：若我判错，全站次级按钮边框的对比度是 1.52:1；但按钮的可识别性由 7.94:1 的标签承担，1.4.11 不因此不达标。
- F1-T6 minor（deferred）：①`AppSelect` 的 `:value="modelValue"` 缺 `?? ''`（今天不可达，类型只声明 String/Number）；②`AppInput` 的 `modelValue` 类型声明漏了 `null`，而 `null` 恰是这个 prop 的核心用途（无运行时影响，但声明是第三期读者会信任的文档）；③`AppSelect` 的 emit 方向恒为字符串，prop 类型里的 Number 半边不可达；④空 `placeholder`/`value` 会渲染成裸属性（无影响）；⑤半截非法输入（`-`、`1e`）会被浏览器规范化成 `''` 从而 emit `null` —— 已把这条实际后果写进第三期 Review Focus 第 4 条；⑥自绘的 chevron 缺注释说明为何需要 `pointer-events-none`。
BASE for F1-T7: 3f60ca51ad55f33ce84e95d56dd60bb92a9195c7
- F1-T7 实现完成（commit `4a8dfb2`，4 文件 +150）。Step 4 全部类名 ≥1、build 无告警；Step 5 确认六个原语**零调用方**；Step 6 编译校验 21 个 `.vue` 全 OK、退出码 0。实现者还主动做了**负向验证** —— 往临时目录放一个语法错误的 SFC，脚本打印 `FAIL` 且退出码 1，证明它不是空跑（这条超出了我的要求，我已把「必须做负向验证」补进计划 Step 6）。它同时指出我 brief 里两处计数过时：说 18 个 `.vue`（实际 21，我写的是 Task 7 之前的数），Step 5 说「只有一行」而实际两行（BrandMark 的 import 与模板使用各一行）。两处均已修正；我写「一行」时的原文措辞也不够精确。
- **F1-T7 完成**（commit `4a8dfb2`）。评审 Spec ✅ / Approved，无 Critical 无 Important，6 条 Minor。评审**读了脚本控制流**确认失败路径结构可靠（`problems` 由 `parse` 的错误数组播种、`compileScript` 与 `compileTemplate` 的抛错与返回错误都进同一个计数器、`failed` 与打印 `FAIL` 在同一分支、无提前 return、`process.exitCode` 由 `failed` 驱动），因此它确认那个「负向验证」不是声称。它还独立补齐了 Step 4 类名清单漏掉的 token（`--color-sunken`/`ink-2`/`border-control`/`surface`/`canvas`/`line`/三个 soft 色全在，`.text-white` 也确实发射），并核对 21 个 `.vue` 与脚本的遍历范围一致。
- **裁定 17（我的文本过度声称，已改计划）**：`check-sfc-compile.mjs` 的 docstring 声称能抓「`<script setup>` 里调用了未定义的函数」——**编译器根本检测不到**：模板里未解析的绑定会编译成 `_ctx.x`，setup 里调用未定义函数是运行时 ReferenceError。这段话是我写的（brief 逐字来自计划）。已把计划里的 docstring 改为准确的能力边界（能抓 SFC 语法/模板编译/script setup 语法错误；**抓不到**运行时未定义函数、模板里不存在的变量、类型错误、`<style>` 块内容），并加了一句「它证明的是可编译，不是正确」。**提交的代码里那段 docstring 仍是旧的**（Minor，交最终整支评审 triage；我按既定纪律不自行改代码）。
- F1-T7 minor（deferred）：①脚本 docstring 过度声称（见裁定 17，已改计划未改代码）；②脚本无零文件守卫（walk 返回空时会打印「全部可编译」并返回 0；评审已核实当前仓库根无 `src/`，cwd 错会 ENOENT 非零退出，故爆炸半径小）；③若 `compileScript` 抛错则 `compileTemplate` 不执行，一次只报一个文件的首个错误（仍计 FAIL，闸门正确）；④脚本不做 `compileStyle`，`<style>` 块未被编译（今天无影响：有 `<style>` 的 6 个组件全是可达的，构建会处理）；⑤**`AppCheckbox` 的属性透传**：根是 `<label>`，所以调用方传的 `aria-label`/`id` 会落在 label 而非 input 上，而 `v-if="label || $slots.default"` 又会丢掉文字 span —— 图标型复选框会没有可访问名。**这条我已在计划里规避**：第三期 Task 5 给 `AppCheckbox` 加 `ariaLabel` prop 并绑到 input 上，而 `FileTable` 的「全选」正是用 `aria-label`（会映射到该 prop）；但调用方传 `id` 的场景仍不成立，第三期不要给 `AppCheckbox` 传 `id`；⑥Step 4 的类名清单漏了中性 tone 与 surface 相关的一批类（验证广度缺口，评审已手工补齐且未发现缺陷）。

---

## 第一期收官

7 个任务全部完成（含 2 轮修复）。第一期从 `d72f4c9` 到 `3d96634`，共 32 个提交（含计划文档修正与一次 dist 收尾提交）。

**工件位置说明**：本 ledger 一直放在 `.superpowers/sdd/2026-09-25-frontend-1-foundation/`（由第一份计划解析出的工作区）。第二、三、四期的 **brief 与 report 会落在各自计划名解析出的目录**（如 `.superpowers/sdd/2026-09-25-frontend-2-shell/`），但**ledger 只有这一份**，继续在这里追加。compaction 恢复时以此为准。

第二期 BASE for Task 1: `3d96634`（第一期 dist 收尾提交）。
- **F2-T1 完成**（commit `5c84219`）。评审 Spec ✅ / Approved，无 Critical 无 Important —— 逐语句比对了每个搬迁的函数（请求载荷、`filesStore` 变更、toast 文案、控制流），确认 `buildPreview` 与 `executeAction`（这两个的请求体决定生成的文件名）**逐字节一致**。我独立核实了两处覆盖性修正（三个 store 实例 + `sources` 保留、`SourceTabs` 走 `switchSource`、模板变更 watcher 在 store 里、无残留旧标识符）。评审还查了几个具名风险：`ScanPanel` 的 `v-if="source === 'local'"` 与 `FileTable` 的 `activeSource === 'local'` 使「读到错误源的路径」在今天不可达；`stores/files.js` 的 `clear()` 只做两件事而 `switchSource` 已显式写入，故去掉它无损失。
- **澄清（不必行动）**：评审说评审包「丢了 HomeView 最后 8 行」。核实后**不是截断** —— `git diff -U10` 对 HomeView 的 hunk 是 `-1,514 +1,140`，而文件 148 行；末尾 8 行与旧版完全相同且超出 10 行上下文窗口，按定义不入 hunk。这是标准 diff 语义。评审的直接结论（style 块未变）它读文件核实过，是对的。
- **裁定 18（评审核实的用户可见潜伏 bug，已写进第三期计划）**：`ConfirmDialog.vue:2` 用 `@update:model-value="$emit('update:modelValue')"` 转发，**丢掉了 `$event`**；而 `AppModal.vue:77` 在 Esc / 点遮罩时 emit 的是 `false`。于是 `v-model="ws.confirm.open"` 只把标志位置为假，**`resolveConfirm` 永不调用**，`askConfirm()` 的 promise 永远悬空 —— `clearAll()` 的 `.then()` 再也不会执行：**用户点「清空」后在确认框上按 Esc，清空静默不生效**。今天不可达（因为 `clearAll` 是唯一消费者），但它既存于改造前，且现在把活的 resolver 留在 app 级状态里（下一次 `askConfirm` 会静默抛弃它）。已作为**必修项**写进 F3-T6（`ResultDialog`/`BrowseDialog` 无待 resolve 的 promise，不需要）。— 代价：F3-T6 的 diff 会比原计划多几行。
- **裁定 19（把 F2-T1 的一条 Minor 带进 F2-T2 的派发）**：`browseConfirm()` 写到 `path.value`（当前源）而对话框来源记在 `browse.source`，不变式是隐式的。今天不可达，但**F2-T2 把源切换搬进顶栏后那个窗口就可达**（180ms `out-in` 离场期内旧 ScanPanel 仍可点、而 `activeSource` 已翻转 → 浏览结果被写进另一个源的路径）。已把这条指针写进 F2-T2 的「需携带的前序评审发现」，并给出收紧方案（写 `sourceStates[browse.source].path`，或来源不一致时 return）。不列为验收项。— 代价：若窗口实际不可达，这段是白读的。
- F2-T1 minor（deferred）：①`browseConfirm` 的隐式不变式（见裁定 19）；②confirm 的悬空 resolver（见裁定 18）；③`path` computed 缺一行注释，而模板仍把它喂给名为 `:local-path`/`:openlist-browse-path` 的 prop，未来读者容易误以为 `ws.path` 就是本地路径；④模板变更 watcher 现在是**应用级生命周期**（Pinia store 不随 HomeView 卸载），今天无写入方在 HomeView 之外，但第三期 SettingsView 有能力编辑模板时要复查；⑤模态标志位也会跨路由存活（打开确认框 → 浏览器后退 → 前进，确认框会重新出现且 OK 仍能 resolve 那个尚未 resolve 的 promise）—— 今天被模态阻断页面的特性挡住，但 F2-T2 把导航控件搬到了模态之外，届时值得考虑在挂载时重置 `confirm.open`/`browse.open`；⑥既有的「扫描中途切源会让在途扫描写进新源缓存」竞态，与改造前表达式完全相同，非本次漂移。
- **F2-T2 报告 DONE_WITH_CONCERNS**（commits `8327031` + `7031` 之外的 `3ec473a`）。我先核实了它的三条 concern：
- **裁定 20（中间态破坏，确认成立，派发修复）**：出口包装层是 `min-h-0 overflow-hidden`，而 `HomeView` 根是 `flex flex-col gap-4` 且**全文没有任何滚动容器**（grep 计数 0）。旧卡片栈的自然高度在常见笔记本视口下超过「视口 − 56px」，于是底部的 `FileTable` 与 `ActionBar`（含「试运行 / 执行重命名」）被**裁掉且不可达** —— 这不是「不好看」，是主操作点不到，属于中间态功能破坏。裁定：本轮修复给 `HomeView` 根加 `h-full` + `overflow-y-auto`（**该行是一次性的**，Task 3 会把根换成 `grid grid-rows-[1fr_56px] overflow-hidden`）。— 代价：多一次修复轮；若我判错（比如某些视口实际不裁剪），代价是一行很快被替换的 CSS。
- **裁定 21（我的派发错误，已纠正）**：F2-T1 的派发里我说 `sources` 数组「到 Task 3 才删」，因此 F2-T2 要保留它。**这是我看错了计划** —— 计划 Task 2 的 Step 3 第 3 项明确要求在**本任务**删除它（其唯一消费者 SourceTabs 正是在本任务删掉的）。实现者按我的错误指令保留了它并**主动指出了这处冲突**，判断正确。核实：`sources` 仅剩定义（HomeView.vue:118）、无任何消费者；Task 3 未重复要求删除。裁定：本轮一并删除它及其上方那条现已误导的注释。— 代价：若我判错，`sources` 被提前删掉而某处仍需它 —— 已用全局 grep 排除。
- **裁定 22（计划里我自己的注释破坏自身两道检查，已改计划）**：doc2 顶栏代码块的注释原文写了 `旧 SourceTabs 的 📁☁️ 一并清掉` —— 这让 Task 2 Step 5 的 `grep -rn "SourceTabs" src/` **不可能无输出**，也让第三期那道 `grep -rn "🎬\|📁\|☁️\|⚠️\|✅" src/ | wc -l` **不可能为 0**。两道检查查的是「代码里还有没有引用」，注释里的字面量会让它们假失败。实现者正确识别并改写了注释（保留信息、去掉字面量）。已把计划里的注释改成不含标识符与 emoji 字面的措辞，并在 Step 5 旁加了一段说明：**要提就描述它，不要写字面量**。— 代价：无（纯文档修正）。
- **F2-T2 修复轮 1 完成**（commit `d5be45c`）：`HomeView` 根改为 `flex h-full flex-col gap-4 overflow-y-auto`（注释写明该容器是一次性的），删除 `sources` 死数组，顶栏注释去掉字面 emoji。验证：build 0 warnings、编译校验 21/21、`grep SourceTabs` 无输出、`grep sources` 无输出。
- **裁定 23（实现者发现的新机制，我已实测厘清并写进四期约束）**：我要求它在注释里写出 Task 3 的替换类名；它照做后发现 **Tailwind v4 把源码注释里的字面类名当成真实候选编进了 CSS**（产物 30.24→30.30 kB，多出无人使用的 `grid-rows-[1fr_56px]` 规则），于是改成描述性措辞。我用干净对照实验厘清了边界：**CSS 入口文件（`style.css`）的注释不被采集**（注释里的 `border-line-strong` 生成 0 条，与 F2-T1 评审关于 `.hidden`/`.static` 的观察一致），而 **`.vue` 注释里的可解析类名确实生成真实规则**（`rounded-[12px]`、`grid-rows-[56px_1fr]` 各 1 条）；不可解析的名字即便被采集也不出规则。**这比「多点死 CSS」更重要**：我的类名闸门是「产物里查得到这个字符串」，于是**注释提过 = 闸门通过**，一个没有任何元素使用的类名照样绿灯。已给四期文档加一条全局约束：源码注释里不要写字面类名，要提就描述它。— 代价：若我判错，实现者少了一条方便的表达方式；但实测依据明确，且 `style.css` 的注释不受影响。
- **顺带核实（不影响交付）**：第三期那道 emoji 清零检查当前计数为 **1**，唯一命中在 `src/components/FileTable.vue:104`（`'⚠️ 待确认' / '✅ 已解析'`），而该文件正是第三期 Task 5 要整体重写的；实现者改过的文件贡献 0。
- **F2-T2 完成**（commits `8327031` + `3ec473a` + `d5be45c`）。评审 Spec ✅ / Approved，无 Critical 无 Important。它逐项核实了 `h-dvh`（且 `grep h-screen` 无输出）、出口 `min-h-0`、源切换走 `@update:source` 而非 v-model、两个路由各自可滚、齿轮/返回箭头与 `route.name` 条件、segmented 的 `accent-soft`/`accent` 与 `aria-pressed`、`SourceTabs.vue` 无残留引用、`sources` 已删、顶栏单行约束、无越界圆角/阴影、字号在阶梯内、路由未动、无 dist 入提交；并核实 `browseConfirm` 的修复在「两源一致」时与原 setter **逐字等价**（原 setter 就是 `sourceStates[activeSource.value].path = v`），且在 `browse.source` 只有 `'openlist'`/`'local'` 两个取值、`sourceStates` 两键都声明的前提下不会写未定义属性。它还确认 Tailwind 那条机制的效果已消除（`56px_1fr` 的假规则已消失，只剩真实使用的 `1fr_56px`）。
- **裁定 24（计划文本错，已改；代码注释仍错，记账）**：`App.vue` 里解释 `min-h-0` 的注释写「少了它……**整页开始滚动**，底栏被推出视口」。**症状描述是错的** —— 外壳是 `h-dvh` + `overflow-hidden`，根本没有页面滚动条；真实症状是路由内容把第二行（1fr）撑破、其底部被裁掉且无从滚到，等于**静默丢失内容**（比能滚更糟）。这段文字是我写的（brief 逐字），实现者照抄。已改计划文本；**交付的代码注释仍是被照抄的旧措辞**（Minor，交最终整支评审 triage）。— 代价：读者对这条机制的直觉会略偏；但机制子句（「路由内容会把 grid 行撑破」）本身是对的。
- **裁定 25（Task 3 的指令与实际文件已脱节，已改计划）**：Task 3 Step 3 第 1 项写「根 `flex flex-col gap-4` → grid」，但 Task 2 的修复轮已把根改成 `flex h-full flex-col gap-4 overflow-y-auto` —— 机械替换会**不匹配**。已改为「替换前先读一遍当前文件的根元素」，并写明 `h-full`/`overflow-y-auto` 与那段临时注释随本次替换一并删除、新根必须带 `min-h-0`。— 代价：无（纯文档修正）。
- F2-T2 minor（deferred）：①`SettingsView` 仍带 `rounded-xl`/`shadow-sm`（计划如此，第三期转原语），记此以免阶段末的视觉闸门意外；②**中间态 HomeView 完全失去了页面内边距** —— 被删掉的 `<main class="p-8 max-w-[1400px] mx-auto">` 是唯一的内边距来源，于是卡片栈与滚动条贴住窗口边缘、与顶栏的 `px-4` 差 16px；Task 3 会补回来（内层容器是 `overflow-y-auto p-4`），我判断不值得为纯观感花一轮修复；③`App.vue` 注释的症状描述（见裁定 24）；④Task 3 的指令脱节（见裁定 25）；⑤顶栏单行约束缺一行注释说明，将来容易被「清理」掉；⑥实现者的报告正文前几节描述的是修复前状态（`sources` 保留、HomeView 未滚），只有追加节是最新的 —— 报告卫生问题，Task 3 读的是 brief 而非报告，风险低。
- **F2-T3 报告 DONE_WITH_CONCERNS**（commit `82595ed`）。实现者做了远超要求的验证：用一次性 /tmp 脚本走项目自身的 Vite SSR + `@vue/server-renderer` **实测了 DOM 事实** —— `aria-label="冲突策略"` 落在 `<select>` 而非 AppSelect 的根 div、HomeView 的 grid 恰有两个元素子节点（中段 + `h-14` footer）、对话框 teleport 到 body、计数渲染为「已选 3 / 128」且 `tabular-nums`、两个按钮正确解禁、`executing=true` 时只有「执行重命名」进 loading 而「试运行」保持可点。它明确标注了哪些行是**实测**、哪些只是**推演**（第 2、4 行——底栏固定与内部滚动——它明确说没测）。
- **裁定 26（本任务引入的回归，派发修复）**：`HomeView.vue:85` 的 `<Toast v-if="ws.toast.show">` 会在内层 `<Transition name="toast">` 观察到变化之前把整个组件卸载 → spec §11 的「Toast 进出 200ms」离场动画失效。**改造前 Toast 没有外层 `v-if`**（旧 HomeView 是 `<Toast :show=... />`），这个 `v-if` 是我在计划里「为稳妥起见」加的，属**我造成的回归**。核实：Toast 是 `fixed` 元素、且已被移到 grid 根之外，所以该 `v-if` 对两行 grid 毫无必要。裁定：现在就删掉（一行），而不是留到 F3-T7 —— 引入一个已知回归、8 个任务后再修，不是可接受的中间态。— 代价：多一轮修复；若我判错（比如卸载时机实际不影响动画），代价是一行可回退。
- **裁定 27（陈旧注释，一并修）**：`stores/workspace.js:226` 的注释以「source-fade 的 180ms 离场过渡里旧的 ScanPanel 仍可点」来解释 `browseConfirm` 的写回目标 —— 而 `source-fade` **正是在本任务被删除的**，所以该前提已不成立（无过渡则 `activeSource` 翻转后组件立即卸载，窗口大概不再可达）。裁定：把注释改为「这是一条**防御性**守卫」并说明窗口赖以存在的过渡已被移除、保留只因便宜且能防将来重新引入过渡。同一轮顺带修 `App.vue` 的注释（症状描述是错的 + 含类名字面量）。— 代价：无。
- **裁定 28（细化我刚加的约束，避免过度约束）**：我给四期加的「源码注释不要写字面类名」过于绝对 —— 实际受害的是**只存在于注释里**的类名（它们产出死 CSS，且让闸门为无人使用的类名开绿灯）；一个确在真实标记里用过的类名，注释里再提一次无害（`App.vue` 注释里的 `min-h-0`/`overflow-hidden` 就属此类）。已把约束改为说明这个判断标准。— 代价：若我判错，实现者多一处需要判断的地方；但原措辞会让「解释某个类在做什么」这类正当注释变成违规。
- **F2-T3 修复轮 1 完成**（commit `2006807`）：删掉 Toast 的外层 `v-if`（并用 SSR 重新实测：`show=true` 时 grid 仍恰有两个元素子节点、`show=false` 时组件保持挂载而内层 Transition 恢复观察翻转）；`workspace.js` 的注释改为说明守卫是**防御性**的、并**描述**那个已删除的过渡而不写它的字面名（实现者主动问要不要用字面名 —— 描述更好，因为将来可能有针对该已删标识符的 grep 闸门）；`App.vue` 注释改为陈述真实症状且不写 Tailwind 类名。
- 待办观察（非本任务）：`ExecutingOverlay` 上的 `v-if="!ws.executeDryRun"` 是改造前就有的冗余（`executeAction(true)` 本就把 `executing` 置为 false，所以干跑时遮罩不会显示），无害，记此以免后续误判为新引入。
- **F2-T3 完成**（commits `82595ed` + `2006807`）。评审 Spec ✅ / Approved，无 Critical 无 Important。它逐项核实并确认本期两个核心机制**结构性成立**：根与中段的 `min-h-0` 都在（`HomeView.vue:5,6`），grid 恰有两个元素子节点（五个浮层/对话框都在 grid 之外且 `fixed`/`teleport`，不会产生隐式行）；`source-fade`、`ActionBar`、`<style scoped>` 全部消失且无悬挂引用（两次 grep 均无输出）；`ariaLabel` 确实绑到 `<select>` 且缺省时用 `undefined`（避免空 `aria-label` 覆盖其它调用点的真实 `<label>`）；`AppButton` 未声明 emits 所以 `@click` 原生透传、按钮是活的；`totalCount` 确实存在。它还查了一个具名风险：删掉 `:key="ws.activeSource"` 后三个卡片不再随源切换重挂载 —— grep 确认它们都没有「由 prop 播种的组件内状态」，故无泄漏。
- **裁定 29（我的推断错误，记账不修代码）**：我在 F2-T3 的修复指令里让它把 `workspace.js` 的保留理由写成「对话框常驻期间源仍可切换」。**这是错的**：弹窗打开时 `AppModal` teleport 一个 `fixed inset-0` 的 z-50/55 遮罩、`AppTopBar` 无 `z-index`/`sticky`、模态还有焦点陷阱，所以 `activeSource` 不可能在对话框期间翻转。评审指出这条**反而更强地**支持保留守卫（它是不可达的保险）。该措辞只存在于代码注释里（我的派发消息里，计划正文没有这段），故无从「改计划」；按既定纪律记为 deferred minor 交最终整支评审 triage。**这是本轮第四次我凭推断断言机制而未先验证**（前三次：Tailwind `@theme` 发射语义、Tailwind 类名转义、npm save-prefix 行为）。纪律应是：凡要写进代码注释或计划的「因为我们知道 X」式论断，先实测或先标为推测。— 代价：注释里一句不成立的理由；读者若照它推理会误判模态的阻断性。
- F2-T3 minor（deferred）：①上述注释里不成立的保留理由；②`AppBottomBar.vue:3` 的 `shrink-0` 在 grid 子项上是**空操作**（grid 项不受 `flex-shrink` 影响，真正钉住底栏的是 `56px` 轨道），计划如此、无害，但读者可能误以为它是「底栏压不扁」的原因；③`HomeView.vue:71` 的 `ExecutingOverlay v-if="!ws.executeDryRun"` 冗余（组件内部已有 `v-if="modelValue"`），系 Task 1 原样搬来的，非本任务引入。
- **F2-T4 完成**（commit `3b98ec1`）。**这是本轮到目前最强的一次验证**：实现者起了一个后端（:8000）+ 一次性 dev server（:5199，已杀），用 **Playwright 驱动系统 Chromium 对着真实应用**跑 —— 7 行走查里 6 行**实测**（第 5 行的未连接分支用独立挂载真实 SFC 实测，因为活应用停在已连接分支），9 个 emit 全部实测触发，`items-end` 用像素核实（浏览按钮与输入框底边同为 136.8），焦点环实测为真实 `box-shadow`，OpenList 写回链路用源码往返核实，且确认守卫**没有**被合并（`path == selectedMount` 时按钮保持 enabled）。Step 4 计数实测 **13**，分布正是 SettingsView 7 / TemplateConfig 3 / FileTable 3。
- **裁定 30（实测量出的布局缺陷，已落到负责它的任务）**：400px 视口下面板列被排成 **423.67px** 并右溢、`浏览` 按钮丢 7.7px；隐藏底栏后轨道回到正好 400px → 驱动源是 `AppBottomBar` 内容的 max-content。根因：`HomeView` 根只有 `grid-rows-[1fr_56px]`、**没有声明列**，唯一的隐式列按 `auto` 取 max-content，于是内容比容器宽时轨道被撑宽，再被根的 `overflow-hidden` 裁掉（我核实了根确无 `grid-cols`）。修法两半，缺一不可：给根补 `grid-cols-1`（`repeat(1, minmax(0,1fr))`，宽屏行为不变、窄屏允许轨道收缩），同时按 F2-T7 已有的 (a)(b) 两项处理底栏内容 —— 只补前半会把「整条轨道右溢」换成「底栏内容左溢出界」。已作为修法 (d) 写进 F2-T7 的 Step 2。**归属 T7 而非现在**：驱动源是底栏的 max-content，而 T5/T6 改的是中段，不会动它；T7 本就是响应式收口任务，且 (a)(b) 与它必须同轮生效。— 代价：这个窄屏缺陷会存在到 T7；影响仅限 <430px 视口、约 8px 的右侧裁切。
- F2-T4 minor／观察（deferred）：①`grep -rn "ScanPanel" src/` 仍有 **1** 处命中，在 `stores/workspace.js:227` —— 那是 Task 3 的修复轮刻意改成的**过去式历史陈述**（「当时旧的 ScanPanel 在过渡期间仍可点」），改掉它反而会篡改事实；实现者判断正确，且该文件不在本任务范围内；②计划里我把控件高度估成 58px，实测 **64.8px**（label 20.8 + gap 8 + 输入框 36），结论不受影响；③计划 Step 2 让我替换 `</ScanPanel>`，但该元素是**自闭合**的、根本没有这一行 —— 又一处我写得不够精确的指令。
- **F2-T4 评审：Spec ✅ / Approved，1 条 Important（我的 brief 强制）+ 4 条 Minor。** 评审逐分支核对了与旧 `ScanPanel` 的等价性：九处 emit 与 `mount-change` 的载荷形状精确保留；三处与扫描相关的守卫经查证**仍是三处**（本地禁用、OpenList 禁用、以及留在 store 里的 `path === selectedMount` 分支，后者因为新代码只按「空」禁用而仍可达）；`update:localPath` 的写回是对的而非 bug（`ws.path` 是按源可写的 computed）；新文件里**没有任何手写的控件样式**（无裸 `input/select/button`、无 `focus:border-primary…`）；`font-mono` 已从路径上去掉（这正是旧面板的 §6.2 缺陷）；`show-password` 未复现；无卡片外壳；字号/圆角/阴影预算合规。
- **裁定 31（Important，计划自身缺陷，已改计划并派发修复）**：`SourceConfigPanel.vue:68` 手写了一个 `AppSelect` **本来就提供**的 label —— 它逐字复制了原语自己的 label 标记，还抄错了间距（`gap-1.5` vs 原语的 `gap-2`），于是「挂载点」距它的下拉 6px，而所有 `AppInput` 的 label 距输入框 8px。功能无影响（`AppSelect` 会把 `props.id` 转发给内层 `<select>`，`for`/`id` 关联成立），但它把本任务要消灭的「重复控件样式」请了回来，且会随原语演进而漂移。修法：`<AppSelect label="挂载点" …>` 并删掉包裹层与手写 label。已在计划里改好，并加了一条通则：**任何原语已提供的结构（label / hint / error / chevron / loading spinner）都不要再手写一份**。— 代价：多一轮修复；若我判错，代价是一行可回退。
- F2-T4 minor（deferred，含一条留给用户裁决）：①**OpenList 的扫描按钮从「会指路」变成「纯禁用」** —— 旧面板该按钮只按 `scanning` 禁用，所以路径为空时点它会走到 store 的守卫、弹出「请浏览选择具体的子目录后再扫描」并自动打开浏览对话框；新代码按 `!openlistBrowsePath` 禁用，于是那条指引从 UI 不可达（store 里的守卫仍在，只是前端够不到）。这是**可发现性的收窄，不是能力丢失**，且与本地分支的既有行为一致（本地一直是按空禁用）。我判断这需要用户裁决而非我改：两个分支的「禁用 vs 指路」取舍是产品判断，spec 没写。②`SourceConfigPanel` 的 prop 默认值与旧组件不同（`recursive`/`includeSubs` 由隐式 `undefined` 变显式 `true`，`path`/`scanning` 由 `undefined` 变 `''`/`false`）—— 今天无影响（HomeView 显式传全部八个），但计划里「契约完全一致」的说法对默认值不成立。③400px 的轨道被撑宽（见裁定 30）；评审另指出**面板根没有 `w-full min-w-0`**，若 Task 5 把它放进自动尺寸的 grid/flex 轨道，同样的 max-content 撑宽会以 280px 的形式复现 —— 已作为指针带进 F2-T5 的派发。④若干刻意的文案/图标变化（「扫描中...」→「扫描中…」、「断开」→「断开连接」、去掉三个图标与绿色徽章）列出以便人类看出是选择而非遗漏。⑤`grep "ScanPanel"` 仍有 1 处历史陈述命中（见裁定 30 的说明）。
- **F2-T4 修复轮 1 完成**（commit `1931c10`）。实现者用同一套隔离挂载 harness **实测了前后差异**：行间距 computed `row-gap` 6px → 8px（终于与所有 `AppInput` 的 label 一致）、手写 label 包装 1 → 0、`label[for]` ↔ `select[id]` 关联仍成立、`mount-change` 仍发出 `'/ALI'`（证明删掉的硬编码 `id` 不承重）、`logout`/`browse`/`update:localPath` 仍触发、无 console 报错。它还排查了文件里其他「手写原语已提供的结构」：label（grep 已无 `<label>`）、chevron（只有 AppSelect 自带）、spinner（只有 AppButton 的 loading）、hint/error（未用）、checkbox（只传 label）；并**有据地否掉**了一个候选 —— 用 `AppBadge` 做状态条会把它正在删除的绿色胶囊重新引入（`AppBadge` 是四种固定 tone 的胶囊，而 spec §5.2 要求的是朴素的内联 `text-ink-2` 文本）。
- **裁定 32（我的计划数字偏小，已改）**：计划里写「`AppInput` 的 label 让控件高度变成 58px」，实测 **64.8px**（label 20.8 + gap 8 + 输入框 36）。`items-end` 的结论不受影响（它是相对比较），但这个数是我纸上算的。已改为实测值并注明初稿偏小 —— 与本轮其它几处一样，凡我给的具体数值都应视为「待实测校准」而非事实。
- **F2-T4 完成**（`3b98ec1` + 修复轮 `1931c10`）。再评审：「全部发现已解决，无新增 Critical/Important」。它另用 grep 直接回答了实现者报告无法从代码判定的一点（是否有陈旧 `id` 残留）：全仓（含 dist）搜 `ol-mount` 无命中，故生成的 `select-v-N` 确实生效。
- 待办：`.superpowers/.../task-4-brief.md` 仍是旧的 58px（我改的是计划正文 3d32dcd，没重生成 brief）—— scratch 工件，已重生成对齐。
- **F2-T5 报告 DONE_WITH_CONCERNS**（`c32307e` + 修复轮 `02b0024`）。核心成果：三栏成型；`min-h-0` 链逐环点名且完好；280px 左栏在 1440/1280/1024 三档实测零溢出（`clientWidth == scrollWidth == 247`）；`insertVariable` 静态+动态双确认（光标 6 → `selStart 14`、输入框重聚焦）。
- **裁定 33（实测出的全站缺陷，已修 + 写进四期约束）**：`font-mono` 在所有 `input`/`select`/`textarea`/`button` 上一直失效。我按花括号配对独立核实：`.font-mono` 在 `@layer utilities`，而 `button { font-family: inherit }` 与 `input, select, textarea { font-family: inherit }` 都在**层外**；层外样式优先于任何层内样式、**与特异性无关**，故静默压掉工具类。影响：`AppInput` 的 `mono` prop 是**本期第一期新引入的 API**，从诞生起就没生效；模板输入框与变量芯片都算不出等宽栈，而这正是 spec §6.2 等宽纪律依赖的。修法：两条规则移入 `@layer base`（base 早于 utilities），光标规则保留层外不动（其行为依赖 `:not(:disabled)` 而非层归属）。修后实测：芯片与两个 mono 输入框均为等宽栈、「目录路径」与扫描按钮仍为 sans（无溢出）、禁用态与启用态光标未破。已把该机制作为约束写进四期。**这是本轮第三次 CSS 层叠层教训**（前两次：按钮光标、`disabled:cursor-not-allowed`）。
- F2-T5 minor（deferred）：①`VARIABLE_DEFS[].example` 已成死数据（brief 要求保留表结构）—— 可删、或把它并进 chip 的 `title` 使其复活；②`<main>` 暂未加 `bg-surface`（Task 6 会同移除 FileTable 卡片壳一起做，实现者的判断正确）；③Row 1「工作区为白底」因此只是部分成立。
- **F2-T5 评审：Spec ✅ / Approved，1 条 Important（我的 brief 强制）+ 8 条 Minor。** 评审逐项核实：左栏零圆角零阴影、两级折叠都在且窄屏默认收起（纯 CSS，无 JS）、`insertVariable` 的 ref 路径对着真实 `AppInput`（单一根 `<div>`）成立且只改了取用方式、props/emits 与三个原语的**载荷级**语义都保留（AppSelect 值为字符串、AppInput 在 `@input` 上触发故逐键刷新存活、AppCheckbox 布尔）、openlist 的两处禁用路径都在、`VARIABLE_DEFS` 未扩张、`@layer base` 的顺序成立（`@import` 在最前，故 base 早于 utilities）、旧的层外重复规则是**删除**而非留下、光标规则确实仍在层外且有注释说明。它还查了一遍 style.css 剩余的层外规则（`html,body,#app`、`body`、`*`、滚动条伪元素），确认没有与任何实际使用的工具类冲突 —— 故本次修复对活的情况是完整的。
- **裁定 34（Important，先测后修，已派发）**：短视口（<约 660px 高）下工作区**裁掉内容而非滚动**。机制（评审推演，非实测）：旧中段是 `overflow-y-auto`，而 `HomeView.vue:9`/`:45` 都是 `overflow-hidden` 且无祖先补滚动；`FileTable` 卡片无 `flex-1`（被 `flex-shrink` 压缩）而其内部滚动盒被硬编码 `max-height: 480px` 卡住无法随之收缩，于是卡高约 547px 在 660px 视口下超出、底部无滚动路径 —— 与 F2-T2 那处同类（内容静默不可达），且 1366×768 笔记本减去浏览器 chrome 正落在这一区间，**真实可达**。裁定：不直接改，让实现者**在 1024×620 实测**该预测；若成立则用一处临时类让工作区可滚（Task 6 会用真正的 `flex-1` + 去掉 480px 上限闭合它），若不成立则带回测量结论。理由与 F2-T2 一致：我此前已裁定「被裁且不可达」不是可接受的中间态。同时给 F2-T7 的计划加了**高度轴**实测档（1024×620），并在计划里写明这条是推演、必须留实测记录。
- F2-T5 minor（deferred）：①工作区失去了 `p-4`/`gap-4` 内边距，旧 FileTable 卡片贴住右下边缘、圆角与阴影被窗口切掉（Task 6 移除卡片后自解）；②§5.4 的明度层级尚未可见（`HomeView` 的 `:9`/`:45` 都没设背景，白色与阴影仍来自 FileTable 的旧卡片），记此以免被误判为 Task 5 的缺陷；③「六环链」的第 6 环（FileTable 根）**名不副实** —— 它没有 `min-h-0`/`flex-1`，靠卡片自身的 `overflow-hidden` + 硬编码 480px 上限在撑，Task 6 才真正成立；**我的约束措辞应据此软化**；④`{season_padded}`/`{episode_padded}` 的 desc 丢了「默认2位」，而 `desc` 现在只进 `title`，该信息不再可见（brief 代码块如此，但 brief 正文又说表「原样保留」，属我自相矛盾）；⑤`html, body, #app { height: 100% }` 与 `body {…}` 仍是层外 —— **与 font-mono 同类的潜伏 bug**，将来若有作用于 body/#app 的高度或排版工具类会被静默压掉（今天无活用途），值得在第四期删别名层前留意；⑥分组按钮有 `aria-expanded` 但面板无 id、按钮无 `aria-controls`（brief 如此，纯无障碍打磨）；⑦dist 相对 src 暂时陈旧（按裁定属预期）。
- **F2-T5 修复轮 2 完成**（commit `b36df64`）。**我给的修法不完整，实现者测出来并补全了。** 它先在活 DOM 里试了四种状态：单把 `<main>` 的 `overflow-hidden` 改成 `overflow-y-auto` **无效**（卡片作为列 flex 子项被 `flex-shrink` 压到 508px，`<main>` 的 `scrollHeight === clientHeight`、永不溢出，于是没东西可滚；真正的裁剪者是卡片自身的 `overflow-hidden`）；补上 `shrink-0` 后才 `cardClippedPx = 0`、末行可达。最终修法 = `overflow-y-auto` **加** 经 class 透传的 `shrink-0`（未碰 `FileTable.vue`）。实测阈值 **659px**（卡高恒 545 + 114），与评审推演的「约 660」吻合；620px 下裁 39px、600px 下 59px；末行 94.9px 高（「新文件名」列换行）。修后：任意高度可达、`<main>` 在 ≥660px 完全无滚动条（1440×900 / 1024×700/660/659 实测 `mainScroll = 0`，兜底惰性）、底栏仍 56px 钉住、`docOverflowX/Y = 0,0`、左栏仍自滚且不牵动 `<main>`。已把「本任务收掉这两处一次性兜底并复测 1024×600 / 900×620」写进 F2-T6 的计划。
- **F2-T5 完成**（`c32307e` + `02b0024` + `b36df64`）。再评审：「全部发现已解决，无新增 Critical/Important」。它做了一件对的事：`shrink-0` 经 class 透传到 `FileTable` 根这一点**无法从 diff 判定**（该文件不在本次 diff 内），于是它去读文件确认单一根元素、无 `inheritAttrs`、`defineProps` 未声明 `class`，而不是把声称当事实。它也核了兜底的惰性（阈值 659 的算术与实测的 0/0/0/0/9/39/85/59 逐行对上）、确认 diff 只碰了一个文件、确认注释里点到的类名都在同文件真实使用（无「只存在于注释」的类名）。
- F2-T5 minor（承前 + 新增）：①`HomeView.vue:4` 的文件头注释「中段自己不再滚动：左栏滚自己，工作区里的表滚自己」现已**不完整** —— 矮视口兜底下 `<main>` 自身就是滚动容器（新加的注释在 45-53 行已说明正确行为，属文档漂移，无功能影响）；②`<main>` 尚无 `bg-surface`（Task 6 与移除卡片壳配对做）；③`FileTable.vue:39` 的 `max-height: 480px` 与卡片的 `rounded-xl shadow-sm` 仍在（Task 6 范围）；④前序各条见上。
- **F2-T6 完成**（commit `f751a67`）。评审 Spec ✅ / Approved，无 Critical 无 Important。核心判据实测达标：整页每档都不滚动（含 375×812）、六环链**逐环**受约束（3 环在 diff 内、3 环按具名风险在 diff 外核实）、产品卡片的边框/圆角/阴影与 `max-height: 480px` 全去且无回加、Task 5 的两处一次性兜底连同注释一并删除、行内控件**一个都没转**（正确）、空态仍在表内、`props/emits` 未动。它同时确认了窄屏断点的**头/身配对**没有错配（`#` 在 `sm`、`解析剧名` 在 `md`，表头与表体一致）。
- 我修正的第三处自算错值：md 档表宽应为 **≈952px**（48+48+220+160+80+80+96+220），初稿写 ≈900 漏算了复选框列 —— 评审实测为 ≈952。已改计划（含 9d 的判据）。
- F2-T6 minor（deferred）：①`filesStore.scanning` 已被孤立（移除扫描蒙层后无消费者；活的是 workspace store 里那个同名状态）—— 第四期 Task 2 已计划删除该字段作为死代码，故此处算是按计划推进的一步；②`hover:bg-sunken/60` 缺内联理由说明（理由只在 brief 里），将来「简化」成 `bg-sunken` 会与 sunken 的表头撞色；③`<main>` 与 FileTable 根都设了 `bg-surface`（有意冗余，FileTable 保持自洽）；④`check-sfc-compile` 与 Playwright 的实测结论无法从 diff 复核（依报告）。
- **F2-T7 报告 DONE_WITH_CONCERNS**（commit `ec708fb`）。四档全过（真实 Chromium 8 视口扫描）：375×812 底栏 375/56、内容右边界 359 ≤ 375、计数隐藏、主按钮文案缩短、网格轨道 375（修前 439.266）；1024×620 主滚动条 0、底栏 56、末行可达（余 0.38px，Task 6 的修复在最后一次结构改动后仍成立）；≥1024 三栏 + 表格内部滚动 + 整页不滚；768 左栏折叠条默认收起、展开恰为 45dvh。640/768/1024/1280/1440 几何与基线逐项一致，无回归。
- **裁定 35（采纳实现者的判断：拒绝执行前提不成立的指令）**：我的 `(c)` 写成条件式「若仍溢出，把品牌名在极窄屏隐藏」。它实测 375 下顶栏溢出为 **0**、品牌名宽 88.47px（被省略号截断而非零宽），**前提不成立**，故未应用 —— 应用反而会在每台手机上藏掉应用名。我裁定采纳。**这是本轮第一次由实现者用测量推翻我的指令**，也是我一直要求「先测后改」的直接回报。— 代价：无。
- **裁定 36（`(d)` 的同一缺陷在上一层，授权补齐）**：320×640（在验收档之外）仍溢出：App 轨道 367.4px 而视口 320；隐藏顶栏后轨道回到 320、隐藏底栏不变 → **驱动源是顶栏，即 App.vue 的根 grid，与 `(d)` 同一缺陷类、上一层**。且 375 只余 7.56px，而 **360px 是最常见的安卓宽度之一**，很可能就在边界外。裁定：把**同一个修法**（根加 `grid-cols-1`）应用到 App.vue 的根 —— 这不是我禁止的「发明第五个修法」，而是同一修法在剩余落点上的补齐。已写进计划。— 代价：多一轮；若我判错，代价是一个类。
- **裁定 37（我的横滑距离算错，已改计划）**：窄屏表宽实测 min-content **783.88px**（原文件名列因两处 `max-w-[320px] truncate` 占 351.83、新文件名列因 `truncate` 无 `max-w` 占 192.03），我按 `th` 的 `min-w` 算出的 552px 是错的。连带：`min-w-[560px]` 在 `sm` 以下**是惰性的**（内容 min-content 已超过它），读到「新文件名」需约 **409px** 横滑而非 180px。要压到 552 必须改行标记 → 属第三期 Task 5，已记入。**这是我第四次把布局尺寸算错**：布局尺寸一律按实测处理，不再写「算出来的」判据。
- **F2-T7 修复轮 1 完成**（`ec708fb` + `fca9bb5`）。10 视口实测：**360 有真实余量**（App 轨道 = 视口 360、底栏宽 360、内容右边界 344 → 16px 余量、底栏高 56、顶栏零溢出、品牌名 73.47px 仍显示、**0 个元素越界**）；375 与第一轮逐数一致（无回归）；**280×640 也零越界**（App 轨道 min-content 从 367.44px 降到 ≤280px，收回 ≥87.4px）；低于约 360 时唯一退化是品牌名被截到 0 宽（既有 `truncate` 的优雅降级，无裁剪、`pageScrollX = 0`）。1024×620 仍 0 主滚动条、末行可达（余 0.19px）。640/768/1280/1440 无回归。
- 环境观察（交用户）：5173 上有一个**不在本轮任何实现者名下**的 vite 进程（pid 1094348）在跑 —— 实现者只杀过自己起的 :5199，未动它。我未去动，可能是用户自己的 dev server。
- **F2-T7 完成**（`ec708fb` + `fca9bb5`）。评审 Spec ✅ / Approved，无 Critical 无 Important。它用**内部一致性**反向佐证测量真实性（品牌名宽度序列 300→13.47/320→33.47/360→73.47/375→88.47 恰随视口线性、饱和 116.91，反推固定消耗 ~286.5px；375 底栏几何从按钮宽度算到亚像素吻合）——这是不重跑而验证测量可信的方法。它也确认 (d) 的两半**各自独立承重**（修前底栏被撑到 439.27、主按钮右边界 407.27，故只加 `grid-cols-1` 会把溢出转移到内容上，正是约束警告的失效模式）、`App.vue` 只插入了 `grid-cols-1` 而外壳标识类逐字未动、`(c)` 正确缺席、无任何一揽子隐藏、干跑入口完好、注释里点到的类名都在真实标记中。
- F2-T7 minor（deferred）：①`App.vue:6` 的注释把两个分属不同元素的类（`min-w-0` 在包装层、`truncate` 在 span）并作一句说，归属略松，实质与结论都对；②「360 有 16px 余量」低估 —— 那 16px 是右内边距，真实几何余量在 360 下约 **47px**、320 下约 7px（我更正记录）；③测量均为**空闲态**：`执行` → `执行中…` 会增宽约 26px，正是收紧后宽度的最坏情况（评审推算 360 下仍在档内、320 下由可收缩的下拉吸收），记此以免记录被读作已覆盖 executing 态。

---

## 第二期收官

7 个任务全部完成（含 4 轮修复）。第二期从 `3d96634` 到 `fca9bb5`。

**产出**：`stores/workspace.js`（工作区状态与流程）、`layout/AppTopBar.vue` / `AppLeftRail.vue` / `AppBottomBar.vue` 三段式外壳、`SourceConfigPanel.vue`（垂直化 + 原语）、`TemplateConfig.vue`（去卡片壳 + 垂直化 + 变量 chips）、`FileTable.vue`（撑满 + 内部滚动 + sticky 表头）。删除 `SourceTabs.vue` / `ActionBar.vue` / `ScanPanel.vue`。

**实测确认的核心主张**：整页永不滚动（`scrollHeight − innerHeight = 0`，含 280/360/375 档）；六环 `min-h-0` 链逐环受约束；表格撑满并内部滚动；底栏 56px 常驻且四档不换行不溢出；280×640 起零元素越界。

**本轮由实测（而非推演）发现的缺陷**：`font-mono` 被层外基础样式静默压掉（全站表单控件与按钮）；矮视口裁掉表格末行且不可达；网格轨道被底栏 max-content 撑宽（两处：HomeView 与 App.vue 的根）；`buildPreview()` 漏发 `overrides` 导致预览与执行不一致（已移交第三期）。

第三期 BASE for Task 1: `fca9bb5`。
- **F3-T1 实现完成**（commit `49adacd`，2 文件 +192）。fail-first 确认真会失败（`SyntaxError` + exit 1，脚本非空跑）；30 条断言全通过、退出码 0，非零退出路径在脚本第 101 行；从 `/tmp` 导入确认模块零依赖（只导出 5 个符号）；build 0 告警、SFC 22/22。实现者自查：模块无任何 import/require；`null`(→2) 与 `0`(→1) 未混淆；`PAD_MIN/MAX = 1/6`；旧字段 `defaultTemplate` 回退存在且新字段优先；抛错路径都捕获（getItem/JSON.parse 与 setItem）。
- **更正**：我在计划里写「34 条 node 断言」，实际是 **30 条**（又一处我数错的计数，不影响结论）。
- F3-T1 minor（deferred）：`clampNumber` 用 `Number(value)`，故 `' '`（纯空白字符串）与 `false` 都会先被强转成 `0`、再被钳到 `1`，而不是回落默认值 2。无 UI 路径会产生这两种输入（`AppInput` 清空发的是 `null`），只在手工改过 localStorage 时可达，故不修、记账。
- **F3-T1 完成**（commit `49adacd`）。评审 Spec ✅ / Approved，无 Critical 无 Important。它逐条核实到「使约束成立的那一行」：unset 判断**在强转之前**（这是唯一正确的顺序，也是 `''` 不被卷进钳制的原因）、四个失败分支都在且被断言覆盖、`[1,6]` 两端两字段、合并不可能产出 `undefined`、非零退出确实可达。它还确认 `Number.isFinite` 顺带挡掉了 `'abc'` 的 NaN 与超大值的 Infinity。
- **裁定 38（评审查证的我的事实错误，已改计划）**：我在 `settingsSchema.js` 的注释里写「与后端 `app/core/template.py` 的 `PAD_MIN`/`PAD_MAX` 一致」—— **这些符号在整个仓库里不存在**，路径也不对（应为 `backend/app/core/template.py`，而那里也没有）。评审按具名风险去核，实测 `backend/app/core/utils.py:20` 就是裸的 `str(num).zfill(digits)`、`config.py` 只设默认 2、全 `backend/` 无任何补零钳制。根因是**后端补零计划从未实施**（这正是我早先裁定跳过 F3-T8 的理由）。**正面含义**：既然后端不钳制，前端的 `[1,6]` 就是当前**唯一的防线** —— 放行 99 会让后端生成 99 位补零的文件名。已把注释改为「区间来自 spec §13.2/§17、后端目前无此区间、本钳制是唯一防线、待后端计划实施后两侧才真正一致」，并把另一处「后端会钳到 1」改为陈述 `null`/`0` 的语义差别（不依赖后端行为）。
- **裁定 39（评审提出、我判定非缺陷）**：`normalizeSettings({ defaultTemplateId: '' })` 返回 `''`，评审认为「而数值字段里 `''` 表示未设置」，值得刻意决定。裁定：`''` 是**刻意的哨兵值** —— 第三期设置页的预设下拉有 `<option value="">跟随系统</option>`，所以 `''` 是合法输入、必须原样透传，不能按数值字段的规则回落默认。不修。
- F3-T1 minor（deferred）：①注释里的后端引用（见裁定 38，已改计划、**代码注释仍是旧的**）；②`' '` / `false` / 数组会被强转成 0 再钳到 1（`Number([3])` → 3），属「模块保证」的缺口而非现网 bug（`AppInput` 清空发 `null`），已由实现者主动披露；③「写盘抛异常被吞掉」那条是**无条件打印 PASS**、不经过 `assert`（故报告里的「30 条」实为 29 条真断言 + 1 条），不空但自身不验证任何东西；④断言脚本用 `JSON.stringify` 作等值判据 —— 对键序敏感、且分不清「缺键」与「显式 undefined」，此处不咬（整对象比较都用 `defaultSettings()`，缺字段会改变字符串）。
- **F3-T2 实现完成**（commit `b21486a`，2 文件：新 `settings.js` + `main.js` +6）。验证质量高：**故意把 `main.js` 里的 `load()` 删掉跑一遍**（再恢复并 md5 校验），确认设置确实静默回到默认值 —— 把「漏调用不报错」这个失效模式变成可观测证据，6/6 通过；真实应用重载持久化 6/6（从已挂载应用的 pinia 里读，而非从模块直接读）；`toObject()` 浅拷贝 1/1；存储不可用 6/6（`addInitScript` 让 localStorage 抛异常：应用正常渲染、无未捕获错误、回默认、`storageAvailable` 为 false、保存与 apply 都不抛错）。
- **裁定 40（实现者提出的语义锐边，记文档不修代码）**：`apply(data)` 是「**用这份数据替换全部设置**」—— 它委派给 `normalizeSettings`，而后者对着**默认值**逐字段合并，所以传残缺对象会**把其余字段抹回默认**，而非保持不变。本任务与 Task 3 都传完整对象（`load()` 传 `readSettings()`、设置页传 `toObject()`），故无影响。裁定：保留该语义（`load()` 正需要「替换」而非「补丁」），但把这一点写进 Task 3 的 brief 以免将来有人误用。— 代价：若我判错，将来「只改一个字段」的调用会静默重置其余字段；已在文档里明确警告。
- **F3-T2 完成**（commit `b21486a`）。评审 Spec ✅ / Approved，无 Critical 无 Important。**我特意要求追的「字段一一对应」回来了干净结果**：评审把四个侧面（`toObject()` 读、`apply()` 写、state 初始化、`normalizeSettings` 返回）的**七个叶子字段逐个列出对照**，完全一致、无孤儿字段。它另确认：探测连「getter 抛异常」（隐私模式的真实形态）也兜住了，而不只是方法抛异常；且**这条修复在真实既有数据上端到端成立** —— `SettingsView.vue:167` 仍以旧字段名写同一个键，Task 1 的迁移分支会映射它，故 `load()` 读回用户已保存的值而非重置它们。
- F3-T2 minor（deferred，两条已落到计划）：①**`openlist` 子键的三处同步要求** —— `toObject()` 展开、会自动带上新子键，而 `apply()` 显式枚举、永远写不进新子键，只加 schema 的症状正是「设置改了不生效」（本期要修的那类缺陷本身）；已在计划里写明加子键须同时改三处；②**`storageAvailable` 只表示「启动探测成功」**，不等于「写入一定持久化」（配额满/只读存储能通过探测，而 `writeSettings` 静默吞掉失败）—— 已写进 Task 3 的 brief，要求降级提示不要过度声称；③`load()` 会归一化两次（`readSettings` 已归一化、`apply` 又归一化一次），幂等、无害、计划如此；④`SettingsView.vue:135,151,167` 仍以**裸的、无探测的** `localStorage` 读写同一个键（Task 3 owns 该文件），其写入形状完整且与迁移兼容，故不会污染 `load()` 读到的内容。
- **F3-T3 实现完成**（commit `658b9e1`，单文件 +132/−166）。无头走查 **28/28 通过**（Playwright + 系统 Chromium 对 scratch vite :5199，已杀、临时文件已删），含「保存跨重载存活、重置丢弃改动、补零越界提示出现与消失、未保存改动不出现在 store 与 localStorage」四条关键行为。build 0 告警、两个校验脚本绿。
- **裁定 41（我的计数判据从根上算错，已按实测更正）**：计划说手写输入框样式「17 → 10，剩 TemplateConfig 3 + FileTable 3」，**实测是 7 → 0**。实现者按我给的防护（「实测与 brief 不符就停下报告、不要调整判据」）如实上报。我核实后确认：全项目那 18 处手写样式**随时间用了两套写法** —— 第二期重写 `FileTable` 行内控件与 `TemplateConfig` 时直接按当时已有的新 token 写成了**新模式**（`focus-visible:border-accent … ring-accent/35`），而判据的 grep 只匹配**旧模式**（`focus:border-primary …`）。所以旧模式只剩设置页那 7 处，本任务清零后即为 **0**。**结论更正**：这笔债务在设置页重写后就**已完全清偿**，不留到第四期（我原先的三期分摊表是错的）。已改计划，并顺带更正 F3-T5 的同类判据（它不必再为一个已归零的计数负责，该证明的是「行内控件改用原语」本身）。`TemplateConfig` 里那个 `<input` 经查是**注释文本**、非真实控件。
- 环境观察：`5173` 上仍有一个不在本轮实现者名下的 vite 进程在跑（实现者只杀自己起的 :5199）。
- F3-T3 concern（deferred）：①上述计数判据（已更正）；②我的 brief 里降级提示的**字面文案**（「当前浏览器禁用了站点数据存储」）与我自己的正文/全局约束（「不要过度声称」）**自相矛盾**，实现者按正文优先、改用「当前浏览器可能无法保存设置，改动可能只在本次会话内有效」，并实测该横幅只在探测失败时出现、用 warn 色、不含「禁用/不会被保存/已保存」等措辞；③走查第 12 行的「边框变红」**仅在未聚焦时成立** —— 聚焦时 `AppInput` 自身的 `focus-visible:border-accent` 会盖过追加的 `border-danger`（红色错误文字始终显示）；属原语内部行为，非本任务引入，记此供第四期考虑；④第 11/14 行当前**不具区分力**（HomeView 尚未消费 settings store、本页也不向后端发设置），它们要等 F3-T4 与 F3-T8 才有意义。
- **F3-T3 完成**（commit `658b9e1`）。评审 Spec ✅ / Approved，无 Critical 无 Important。它把风险点追溯到**机制而非巧合**：draft 边界之所以成立，是因为 `toObject()` 深拷贝了 `openlist`（否则 `reactive(toObject())` 会写入穿透）；`dirty` 的比较是**稳定**而非碰巧可用（`toObject()` 固定键序 + 闭合键集，否则会让 `dirty` 永久为真、表现为保存按钮一直可点）；`save()` 传的是完整 draft；`:max="6"` 真正绑到 `<input>` 上而非装饰；旧的 `SETTINGS_KEY`/`loadSettings`/`saveSettings`/`original` 与 `CheckCircle2` 全部清除、无孤儿 import；209 → 175 行。
- F3-T3 minor（deferred）：①**`save()` 的 loading 态永远观察不到** —— 它是 `async` 但内部无 `await`，`saving = true … false` 在一个同步任务内完成，Vue 不会在中间 flush 渲染，故 `:loading="saving"` 与「保存中…」文案不可观测（计划如此、无回归，`:disabled="!dirty"` 的语义仍正确）；②**小数被静默截断且无提示** —— `padRangeError(2.5)` 返回空，而 `normalizeSettings` 会 `Math.trunc`，于是输入 2.5 保存成 2 且无解释，**正是这个错误提示特性存在要消除的那类意外**（超出 brief 的打磨，加一条 `!Number.isInteger(n)` 即可闭合）；③JS 注释里漏进了 markdown 粗体（`:137` 的 `**钳制是静默的**` 会显示成字面星号）—— 与我在计划里已修过的「模板里的 markdown」同类；④`savedFlash` 的 `setTimeout` 未在卸载时清理（继承自旧文件，Vue 3 下无害）。
- **F3-T4 完成**（commit `f3e2aa6`，单文件 +16/−4）。六行**全部实测**（无 Playwright，改用 Node 内置 WebSocket 走 CDP 驱动系统 Chromium，对真实 Vite + FastAPI）：保存的 `with_title` 到达工作区预设下拉与模板输入框；保存的 `overwrite` 到达底栏；底栏改 `abort` 后 localStorage 仍是 `overwrite`、刷新即回退（**会话内有效语义得证**）；空串设置与不存在的预设 id 都回退到可用预设且模板非空、零页面异常。build 0 告警、两个校验脚本绿。
- F3-T4 观察（deferred）：回退链在**连续会话**里落到 `with_title`（模板 store 携带的 `currentPresetId`，链条第二环），**全新加载**时落到 `emby_standard` —— 两种都满足「绝不空模板」，实现者如实标记差异而未擅自「修复」（改就偏离 brief 指定的链条）。
- 环境：实现者启动并杀掉了 Vite dev server（brief 要求清理），而**用户自己的桌面 Chrome 里也开着同一 dev server 的标签页**，该标签可能已断。
- **F3-T4 完成**（commit `f3e2aa6`）。评审 Spec ✅ / Approved，无 Critical 无 Important、无可归档 minor。五项具名检查全过：无回写设置的通路（`settingsStore` 仅两处引用、都是读）、回退链是 brief 的三环原样、**任何落空路径都终止在非空模板上**（评审去读 `template.js` 核实：`currentTemplate` 初值非空、`setPreset` 同时写 id 与 template、空预设列表有 `if (presets.length)` 守卫）、未提前发送补零字段、watcher 未被触碰、仅一个文件改动。评审另确认实现者披露的差异是真实的链条第二环行为（`setPreset` 会改 `currentPresetId`），「修」它反而偏离链条 —— 判断正确。
- F3-T4 越界观察（非本任务）：`settingsSchema.js:52-54` 把 `''` 当作合法的 `defaultTemplateId` 原样接受，而未归一化到 `'emby_standard'`；此处无害（链条第二环吸收它），但与 `conflictStrategy` 走白名单校验的做法**不一致**。记此供 schema 的归属者（第四期）决定。
- **F3-T5 实现完成**（commit `3463c5f`，3 文件）。九行**全部实测**：编辑「集」→99 后，真实 `POST /rename/dry-run` 请求体里出现 `"episode":99`、对话框显示 `S01E99.mkv`（**事件改接端到端成立**）；17 个行内 `<input>` 全部有 aria-label、0 个无标签；`font-variant-numeric: tabular-nums` 实测落在数字 `<input>` 上、而 `AppInput` 根 `<div>` 上是 `normal` —— **反证了「必须写在原语内部」这一步是必要的**；emoji 在源码（含 index.html）与表格 textContent 中均为 0。
- **裁定 42（C2 揭示的既存缺陷新维度，已落到 F3-T8 计划）**：F3-T5 的实现者实测发现，除「`buildPreview()` 不发 `overrides`」之外还有第二半 —— **`buildPreview()` 重建 `previewRows` 时把每行写成 `override: {}`，所以用户手动点一次「预览」就会抹掉全部已编辑值**（实测：编辑后点预览会重新取数并丢弃改动）。两半缺一，症状都是「编辑白做」。已把「重建行时必须按 id/original_path 保留旧行的 override」写进 F3-T8 的计划。它也证明该缺陷**先于本任务存在**（本任务只是改了触发它的事件名，严格更灵敏）。
- F3-T5 minor（deferred）：①我的走查第 1 行把「清空」按钮描述成「红色描边」，而 spec §8.2 与 brief 自己的代码把 `danger` 定义为**实心红**（实测 `#B3261E` 填充 + 白字、无边框）—— 实现者按代码与 spec 执行，是我的措辞错；②我的 brief 里「`md` 的 `h-9` 会让行高从 33 涨到 37px」**未复现**：实测行高 59.39px，由「原文件名」两行单元格决定，输入框 32→36px 对行高**零影响**；实现者按规格实现了 `size="sm"`，但**没有**把这个未验证的数字写进源码注释（纪律正确）；③`shadow-sm` 唯一残留是 `AppPanel.vue:13` 注释里的文字，无标记使用它；④`:8000` 后端与 `:5173` vite 是前序任务遗留仍在运行，实现者驱动后**未杀**（故用户标签页应无恙）。
- **F3-T5 完成**（commit `3463c5f`）。评审 Spec ✅ / Approved，无 Critical 无 Important。它**编译验证了**最险那处：`v-model.number + @update:model-value` 会被 Vue 合并成 `[先赋值, 后 emit]` 的数组，故 `update-row` 读到的是新值（这是对「漏改事件名会静默丢 override」这一失效模式的决定性反证）。它也**没有采信**「C2 是既存缺陷」的说法，而是核了 `update-row` 契约与 base 一致、本 diff 只把触发时机从失焦改成逐键（严格更灵敏）。`size`/`ariaLabel` 经 SSR 实测落在**内层控件**上、缺省时属性整体省略（不产出空 `aria-label`）。
- **裁定 43（我自己的注释第三次绊到我，已改计划）**：`AppPanel.vue:13` 的注释里写着 `shadow-sm` —— 那是**我第一期写的**，而按我自己定的规则，注释里提一个**只存在于注释里**的类名会让 Tailwind 采集它并生成一条**无人使用的 `.shadow-sm` 规则**（后果正是我文档里预言的那个）。已把计划里那句改为「一层浅阴影」（描述而非字面量），并把 doc3 的 `shadow-sm` 判据改成排除注释行的精确形式。**代码注释仍是旧的**（Minor，交最终整支评审 triage）。— 代价：无。
- F3-T5 minor（deferred）：①**本 diff 其实没有移除任何 emoji** —— base 的 `f3e2aa6` 已 grep 干净，末个 emoji 是**第二期 F2-T6 重写 FileTable 时**随行标记一起清掉的（我当时的模板就直接写了无 emoji 的 `待确认`/`已解析`）；终态正确（0），但提交信息把功劳记给了本任务；②`v-model.number` 用在本原语上是**惰性的** —— 编译器产出 `modelModifiers:{number:true}` 而赋的是 `$event` 原值，数字强转只对原生元素有效；此处无害（`AppInput` 本就对 number 类型 emit `Number(raw)`/`null`），属死意图，可去掉或在原语里处理 `modelModifiers`；③上述 `shadow-sm` 注释；④C1（走查说「清空」是描边、实为实心红）与 C3（行高数字未复现）均属我写错、实现者按代码执行，已记。
- **裁定 44（实现者发现的计划内自相矛盾，已改计划）**：我在第三期计划的对话框代码块里沿用了旧文件的 `text-[15px]`（两个可见 h2），而我的全局约束与 spec §6.3 的字号阶梯**都没有 15px**（最近的是 14px 与 20px）；已重构的 `AppPanel` 的 h2 用的正是 14px。裁定：**阶梯赢** —— 这类残留正是那条约束存在的目的。已把四期文档里所有 `text-[15px]` 改为 `text-[14px]`。实现者自行推断出正确结论并**先问后做**，判断与纪律都对。
- **F3-T6 实现完成**（commit `17cdd2d`，三文件）。11 行走查里 **10 行实测**（CDP 真实鼠标/键盘事件）：用 store spy 捕获到 Esc→`resolveConfirm(false)`、点遮罩→`(false)`、确定→`(true)` 三条路径；方向键与 Home/End 的焦点移动；45 行结果下 `min-h-0 flex-1 overflow-y-auto` 滚动且页眉页脚钉住；Esc 后焦点归位到「清空」/「浏览」。build 0 告警、旧 token grep 0、两个校验脚本绿。
- **裁定 45（实现者纠正我的实质性错误，已改计划）**：我在派发里把第 3 行验收写成「清空 → Esc → 文件真的被清掉」——**不可能且方向相反**。按指定修法 Esc 让 promise 以 `false` settle，`clearAll()` 在 `!ok` 时 return，所以 Esc = 关闭且不清空。**该缺陷的真实危害不是「清空没生效」（那与取消的可见结果恰好相同），而是 promise 永不 settle**：`confirmResolver` 一直挂着、`askConfirm()` 的调用方永远悬停（`doExecute()` 同理永远走不到执行）、且下一次 `askConfirm` 会静默抛弃它。我把症状写成了后果。已把计划里那段改为上述准确表述，并明确「**不要**把 Esc 改成确认」。同时修掉 Step 1 代码块里仍是旧的「丢 payload 转发」写法（brief 正文要求 `onModalUpdate`，代码块没跟上 —— 与 `text-[15px]` 同一类陈旧）。
- F3-T6 minor（deferred）：①面包屑的累加器对绝对路径会产出 `//tmp/...`（后端会归一化故不可见，属既有）；②`ExecutingOverlay.vue` 仍有 `text-[15px]`（不在这三文件范围内；第四期 Task 1 重写它，计划给的代码已是 14px）；③焦点归位只在「表格非空、触发元素仍在 DOM」时实测过，「触发元素被 v-if 移除」那条分支是推演 —— 而第一期 Task 5 已为它写了专门的 Console 构造供人工验证。
- **F3-T6 完成**（commit `17cdd2d`）。评审 Spec ✅ / Approved，无 Critical 无 Important。它确认缺陷修成了**恰好要求的形状**（转发值 + 仅假值发 `cancel`，`ok` 绝不来自 Esc），另两个对话框的 emits 未变，目录项与面包屑都是真 `<button>` 且键盘处理器挂在它们**真正的祖先**上（避开了「委托给非祖先」这个经典陷阱），路径的 `font-mono` 已去且三个文件里全无残留，明度/圆角/阴影/旧 token 全扫干净，未动第四个文件。
- **裁定 46（评审判为 minor，我给归属而非花一轮）**：`BrowseDialog` 的 `@dblclick="navigate(...)"` 是**纯鼠标**下钻 —— Enter/Space 只触发 `click`，故键盘用户能选中目录但**无法进入子目录**，面包屑又只能往上。这是 brief 级别的缺口（我给的代码如此），而本任务的目的恰是「补键盘导航」。裁定：不为此单独开一轮（主用途「选中目录」可用，下钻是次要路径），但**指定给第四期 Task 5**（它的 a11y 收口任务），已在计划里写明修法与「不要动已实测通过的焦点循环」。
- F3-T6 minor（deferred）：①上述键盘下钻缺口（已指定归属）；②面包屑按钮上的 `rounded-[4px]` 看似越出「8/12px」阶梯，但同一圆角已在已评审代码里使用（`AppCheckbox`、`SettingsView`）且 brief 强制，故阶梯应理解为作用于卡片/容器而非小控件；③关闭按钮与目录项用了完全相同的 hover/focus recipe，无视觉区分（观感）。
- **F3-T7 实现完成**（commit `abcd14f`，单文件 +36/−23）。五行**全部实测**（CDP 对运行中的 :5173）：成功=白底 `rgb(255,255,255)`/`#4B5257` + 对勾 + `role="status"`；警告=`#FDF6E3`/`#8A5F00`；错误=`#FDECEA`/`#B3261E` + `role="alert"`/`assertive`；**离场动画实测**（元素仍带 `.toast-leave-active`、600ms 后消失）——证明那个 `v-if` 陷阱未复发；进场 0.2s opacity+transform。无绿/`rounded-lg`/`shadow-lg` 残留，`HomeView` 上无 `v-if`。
- **裁定 47（第三次「我的代码块是陈旧方」，已记规则）**：brief 的 Step 1 代码块**漏了 `<Transition>` 包裹层**，而同一段正文与 style 块都要求它 —— 照代码块字面实现会让 `.toast-enter-*`/`.toast-leave-*` 全部惰性、走查第 4 行静默失败。实现者按正文保留 `<Transition>`，判断正确。**三次同类的累计证据**（`text-[15px]` 字号、`ConfirmDialog` 的丢 payload 转发、本次漏 `<Transition>`）指向一个稳定规律：**我在计划阶段「逐字保留旧文件」的代码块，是这类矛盾的来源**；而约束与 spec 才是权威。已把这条规则口头告知各实现者，**第四期的执行者同样适用**：凡代码块与正文/全局约束冲突，以正文与约束为准，并先问或在报告里显著标出。
- F3-T7 观察（deferred，与 F2-T4 minor 1 同源，合并记此供用户裁决）：走查第 2 行在 UI 上**不可达** —— `SourceConfigPanel` 的扫描按钮 `:disabled="!localPath"`，所以路径为空时点不到它，`doScan()` 里那句「请输入目录路径」的警告分支是**死代码**。本地分支的这个性质**改造前就有**（旧 `ScanPanel` 也是 `:disabled="scanning || !localPath"`），云盘分支则是第二期我改出来的。两个分支同样都是「禁用而不指路」：可发现性收窄、非能力丢失。**这是产品判断（禁用 vs 指路），spec 未写，我一并留给用户**。实现者改为直接调 store 的 `showToast(msg,'warning')` 验证色调与 role，诚实标注「色调/role 已测、点击驱动的那条路径未测」。
- **F3-T7 完成**（commit `abcd14f`）。评审 Spec ✅ / Approved，无 Critical 无 Important。它确认中心缺陷类**结构上不可能**：`role` 与 `aria-live` 都派生自同一个 `isAlert` computed，无法互相漂移；no-guard 的两处也逐一核过（`<Transition>` 在且包着内层 `v-if="show"`；`HomeView.vue:103` 上无外层 `v-if`，离场动画活着）；所有 token 都已定义故类名不是静默空操作。
- **裁定 48（第四次「代码块是陈旧方」，已改计划）**：Toast 的离场时长，brief 的**代码块**写 `0.18s`、而同一份 brief 的注释与正文都写 200ms、走查行又写 ~180ms —— 实现者照代码块逐字实现，评审标为 minor（20ms、不可见）。这与 `text-[15px]`、`ConfirmDialog` 的丢 payload 转发、漏 `<Transition>` 是同一模式的第四次发作。已把计划里 Toast 的离场改为 `0.2s` 统一。**规律已足够稳固，值得写进交接**：我在计划阶段「逐字保留旧文件」的代码块是矛盾来源，正文/约束/spec 才是权威。
- F3-T7 minor（deferred）：①离场 0.18s（见裁定 48，已改计划、代码未改）；②`success` 与 `info` 的 tone 字符串**逐字相同**（符合 spec，但将来若只改一边会静默漏掉另一边，可提取共享常量）；③`HomeView.vue:103` **没有注释说明「`<Toast>` 上不得加 `v-if`」** —— 而这正是我在第二期亲手引入过一次的回归，目前无守卫；属第四期范围（其 Task 2 会改 HomeView 的 FileTable 一行），已在 ledger 记录。

---

## 第三期收官

**实现 7 个任务（F3-T8 按裁定 5 跳过）。** 三期从 `7d0434f` 到 `abcd14f`。

**产出**：`settingsSchema.js`（零依赖纯函数 + 30 条 node 断言）、`settings.js`（Pinia，`load()` 在 mount 前）、`SettingsView.vue`（重写 + 原语 + 补零越界内联提示）、`workspace.js`（接入默认模板与冲突策略）、`FileTable.vue`（行内控件原语化 + AppBadge + emoji 清零）、三个对话框（token/原语适配 + 键盘导航 + **confirm 悬空 promise 修复**）、`Toast.vue`（`role`/`aria-live` + 无绿）。

**实测确认的核心主张**：设置改一项保存后**在工作区立即生效**（默认模板到达预设下拉、冲突策略到达底栏）；保存跨重载存活、未保存改动不影响工作区；编辑某行的「集」真的进了 `POST /rename/dry-run` 的请求体；Esc 让 confirm 的 promise 以 `false` settle（不再悬空）；Toast 的四种 tone 与 role 全部实测。

**未完成（明确移交）**：F3-T8（补零位数请求带参）—— 依赖未实施的后端计划；且其两项修复（`buildPreview` 补发 `overrides`、重建行时保留 `override`）已写进它的计划，待后端就绪后一并做。

---

## 第四期（收尾 + 全量验证）

第四期 BASE for Task 1: **`21eb1b8`**（`abcd14f` 之后的收尾提交：`a60f12e` docs + `21eb1b8` build）。MERGE_BASE 仍为 `d72f4c98`。
工作区归属（沿用 Ruling 3）：brief/report 落 `.superpowers/sdd/2026-09-25-frontend-4-polish/`，**ledger 仍是本文件**。

### Pre-flight 冲突扫描（第四期）

#### A. 共享文件 / 跨任务接口

| 任务对 | 共享物 | 一方产出 vs 另一方消费 | 结论 |
|---|---|---|---|
| F4-T1 ↔ F4-T4 | `ExecutingOverlay` 的 `<style>` 过渡与 `animate-spin` | T1 产出 `.overlay-enter-active` / `.overlay-leave-active` + `animate-spin`；T4 的通配兜底覆盖它，Step 2 的 grep 消费 `.overlay-enter-active` | 干净（T1 先执行，T4 时该类已存在）。**注意 T1 原把类名从 `modal-*` 改成 `overlay-*`**，正是为了让 T4 的 grep 与 AppModal 的 `.modal-*` 可区分 |
| F4-T1 ↔ F4-T5 | 遮罩的 `aria-busy` vs 卡片的 `role="status"` | T1 的代码把 `aria-busy="true"` 挂在 `role="status"` 卡片的**祖先**上 | **冲突（见裁定 51）** |
| F4-T1 ↔ F4-T7 | 执行遮罩的走查 | T7 Step 2 第 1 条走查它 | 干净 |
| F4-T2 ↔ F4-T3 | `FileTable.vue` 的 tbody 三支 + `defineProps` 形态 | T2 加 `scanning` prop + 骨架支（保留空态 `<tr v-else>`）；T3 把 `defineProps` 改为 `const props =` 并**删掉**空态支 | 干净：顺序替换，T3 的 props 快照已含 `scanning`，且 T3 正文已声明「须先读当前文件」 |
| F4-T2 ↔ F4-T4 | 骨架的动效 | T2 正文要求 `animate-pulse`；T4 的兜底会压制它 | **冲突（见裁定 50）** |
| F4-T2 ↔ F4-T5 | `FileTable` 的 `scanning` prop；滚动容器 | T5 Step 3 给滚动容器加 `:aria-busy="scanning ? …"`，并给 `<table>` 加 aria-label | **冲突（见裁定 55）**；`scanning` prop 由 T2 引入，T5 消费 |
| F4-T2 ↔ F4-T7 | 长表格专项 | T7 Step 8 验证 T2/T3 改动后的表格 | 干净（T7 只验证不改） |
| F4-T3 ↔ F4-T5 | `FileTable` 的滚动容器与空态结构 | T3 把滚动容器改成空态/表格二选一（滚动容器带 `v-else`）；T5 在该 div 上加属性 | **冲突（见裁定 55）** |
| F4-T4 ↔ F4-T6 | `style.css` | T4 末尾追加 reduced-motion 块；T6 删除中间的别名 `@theme` 块 | 干净：三段（`@import` / 新 token `@theme` / 基线 / 兜底）互不重叠；T6 的 kill-list 正则已实测不误伤新名 `border-border-control` |
| F4-T5 ↔ F4-T7 | 键盘走查表 | T7 Step 4 复用 T5 Step 4 的 10 项表 | 干净 |
| F4-T6 ↔ F4-T7 | `style.css` | T7 的 9 项验证跑在 T6 删除别名层后的最终 `style.css` 上 | 干净：计划 Architecture 已明定「删别名层在全量验证之前」 |
| F4-T2 ↔ `stores/files.js` | 删除死 `scanning` | 全项目**零引用**（见裁定 54） | 干净：T2 Step 5 用 grep 归零 |

#### B. 各任务自身文本一致（验证 vs 代码 / 文件）

| 任务 | 自洽性检查 | 结论 |
|---|---|---|
| F4-T1 | `tabindex="-1"` + `@keydown.tab.prevent` + 聚焦时机 | **缺陷（见裁定 52、53）** |
| F4-T2 | ①「删除 `v-if="filesStore.scanning"` 蒙层」vs 当前文件；②`animate-pulse` vs Global Constraints | **缺陷（见裁定 50、54）** |
| F4-T3 | import 块（只列 lucide 三图标）vs 模板用的 `AppButton` | 干净：`AppButton` 已在该文件 import（第 144 行），lucide 三图标也已 import（第 141 行），`computed` 为新增 |
| F4-T4 | 兜底通配符 vs 三类动效载体 + `.01ms` 的理由（Transition 依赖 transitionend） | 干净 |
| F4-T5 | 审计正则覆盖面 + 已声明盲区（`<textarea>` / `<div @click>`）+ BrowseDialog 修法二选一 | **缺陷（见裁定 56）** |
| F4-T6 | kill-list 正则 17 名 vs 第一期别名块 17 名；产物 CSS 校验 | 干净 |
| F4-T7 | §15 九项逐项落位；Step 3 依赖未实施的后端 | **缺陷（见裁定 57）** |

#### C. 计划明确要求、但评审规则会判为缺陷的东西

无。本期不含测试断言真空，也不要求逐字复制逻辑块。

---

### Ruling 记录（第四期 pre-flight）

Ruling 50（计划内部矛盾，已裁定）：**骨架行不加 `animate-pulse`**。计划三处对一处：Global Constraints「只保留 spec §11 动效表里的 5 类动效」、spec §11 的 5 行动效表（穷举）、以及计划自己的 Self-Review 表（「Task 2 Step 2 的骨架『**不加** `animate-pulse`』」）都持否定；只有 T2 Step 2 的正文与代码块持肯定，而它的论据是把 §11 的「状态转换」扩张解释到无限循环动画上 —— 而 spec §11 删除 `animate-bounce` 的理由恰恰是「**它是无限循环**」。骨架屏的「正在加载」语义由 8 行对齐列宽的灰块形状 + T5 的 `aria-busy` 承担，静态灰块不会被误读成「页面坏了」。连带：T2 Step 3 走查第 7 项、T4 Step 2 走查第 7 项改为「骨架是静态灰块（设计如此）」。— 代价：若判错，骨架少一点「活着」的观感；回退成本 = 两处加类，且 T4 的兜底无需改动。

Ruling 51（计划内部矛盾，已裁定）：**`ExecutingOverlay` 遮罩外层不挂 `aria-busy`**。T1 的代码把 `aria-busy="true"` 挂在遮罩 div 上，而 `role="status"` 的卡片是它的**子元素** —— Review Focus 第 5 条要的正是「两者不嵌套在同一个可聚焦子树里」，因为祖先的 `aria-busy=true` 会让 AT 暂缓播报**后代** live region 的更新。T1 的注释引用了 Review Focus 5 却只按「不同元素」理解，未考虑父子关系。裁定：遮罩的阻断语义由 `fixed inset-0` + `tabindex="-1"` + `@keydown.tab.prevent` 承担，「正在忙」由卡片的 `role="status"` + `aria-live="polite"` 承担；`aria-busy` 的唯一归属是 T5 给表格容器加的那处。— 代价：若判错，遮罩少一个 AT 提示；但遮罩出现时焦点已被收进来、背后表格不可达，信息无损失。

Ruling 52（T1 的实现缺陷，已裁定）：**聚焦改用 `watch(..., { immediate: true, flush: 'post' })`**。brief 的 `onMounted(() => overlayRef.value?.focus())` 在本项目的实际用法下**永不执行**：`HomeView.vue` 用一个非惰性的 `v-if="!ws.executeDryRun"` 挂载该组件，挂载时 `modelValue` 为 `false` → 内层 `v-if` 不渲染 → `overlayRef` 为 `null`，可选链静默跳过；此后遮罩由 `false→true` 时也不会再有 `onMounted`。后果不止「焦点没收进来」：`@keydown.tab.prevent` 挂在遮罩上，焦点若仍在背后的表格输入框里，**该 keydown 根本不会传到遮罩**，走查第 3 项（执行中按 Tab 焦点不动）必然失败。修法：`watch(() => props.modelValue, v => { if (v) nextTick(...) })` 不够 —— 还需覆盖「挂载时 `modelValue` 已为 true」的情形，故用 `{ immediate: true, flush: 'post' }`（`flush: 'post'` 让 immediate 那次回调也等到首次渲染之后运行）。需相应把 `defineProps` 改为 `const props =`。— 代价：若判错（例如将来改为惰性挂载），该写法仍覆盖两种情形，无副作用。

> ⚠️ **Ruling 52 的理由部分已作废，目标保留**（见下方执行日志的更正条）。目标（遮罩打开时真能聚焦，让 `@keydown.tab.prevent` 生效）已达成且可达路径工作正常；作废的是「`flush: 'post'` 让 immediate 那次也延后」这一条 —— 实测该论断为假。

Ruling 53（与 spec §11 及裁定 12 冲突，已裁定）：**T1 的离场改为 `0.2s`**。brief 代码写 `.overlay-leave-active { transition: opacity 0.15s ease-in }`，而 spec §11 动效表第 3 行是「模态进出 200ms」，Global Constraints 亦同；裁定 12 已就 AppModal 定过同一条原则（「不要因为『收起可以更快』改回去」）。ExecutingOverlay 是模态式阻断遮罩，适用同一行。走查第 6 项的「约 150ms」改为「约 200ms」。— 代价：离场慢 50ms，纯观感。

Ruling 54（计划基于过时事实，已裁定）：**T2 的「删除蒙层」步骤跳过，Step 1 的 grep 预期改为 0 行**。计划「前期实测结论」与 T2 Step 2 都要求删除 `FileTable.vue` 里 `v-if="filesStore.scanning"` 的蒙层及其 `<div class="relative">` 外壳 —— **这两段在第二期 F2-T6 重写 `FileTable` 时已被删除**（实测：`grep -rn "filesStore.scanning" src/` 零命中，`frontend/src/stores/files.js:7,29` 是唯二出现处）。原结论「该蒙层从未渲染过」仍成立，只是它现在也不存在了。T2 只需做：加 `scanning` prop、加骨架分支、`HomeView` 传 prop、删除 `stores/files.js` 的死 `scanning`。Step 1 的验证 grep 预期输出**为空**（原写「只输出 FileTable 里那一行」）。— 代价：若判错（蒙层其实在别处），T2 Step 5 的 `filesStore.scanning` 归零闸门会失败并暴露。

Ruling 55（「代码块是陈旧方」的第五次，已裁定）：**T5 Step 3 的滚动容器必须保留 T3 引入的 `v-else`**。T5 的代码块写 `<div class="min-h-0 flex-1 overflow-auto" :aria-busy="…">`，而 T3 已把该 div 改成 `<div v-else class="min-h-0 flex-1 overflow-auto">`（空态/表格二选一结构）。机械替换会让 `v-else` 丢失 → 空态与表格同时渲染。实现者须先读当前文件，只追加 `:aria-busy`，其余属性/指令逐字保留。这与 ledger 已记的规律同源（`text-[15px]`、`ConfirmDialog` 丢 payload、Toast 漏 `<Transition>`、Toast 离场 0.18s）。— 代价：若判错，多一次很便宜的复核。

Ruling 56（T5 内嵌的设计二选一，已裁定）：**BrowseDialog 的下钻接 `ArrowRight`，`Enter`/`Space` 语义不变（仍为选中）**。plan 给了「ArrowRight 下钻」与「Enter 改下钻 + 选中改用 Space」两个选项。选前者：`onListKeydown` 已有一张 `KEYS` 白名单与索引移位逻辑，加 `ArrowRight` 是同一形状的扩展；而改 `Enter` 会推翻第三期实测通过的「Enter 选中」行为，且需要改写第 153 行的既有注释与 `aria-pressed` 的心智模型。裁定 46 已认定「选中」是主用途、下钻是次要路径，故不动主路径。实现要点：`items` 与 `dirs` 同序，`navigate(dirs[idx].path)`。— 代价：若判错，键位映射不是用户预期的那个，改一行即可换。

Ruling 57（T7 的执行方式 + 适用范围，已裁定）：①T7 **不是**一个可由子代理完整执行的任务 —— 其 11 个 Step 里绝大多数需要浏览器 / DevTools / 真实后端 + OpenList 服务。按第一期已有的 Ruling（「浏览器步骤 → 机械等价验证 + deferred-to-human 清单」）执行：子代理只做可机械判定的部分（Step 1 的构建无警告、Step 2 的产物级断点、Step 5 的对比度取色公式计算、Step 8 的 `scrollHeight` 判据说明），其余逐条列入 deferred-to-human 清单交用户一次性走查。②**T7 Step 3（补零位数闭环）标「不适用」** —— 后端补零计划未实施（裁定 5），`episode_pad_digits` 无处可传。计划 §前置 已声明此情形，照办。③T7 Step 9 的 6 张截图全部 deferred-to-human。— 代价：视觉类缺陷（截图、观感、真实对比度）在交付前不会被发现；这是第一期 Ruling 已接受的同一笔代价。

Ruling 58（跨期遗留的归属决定）：**`settingsSchema.js` 的 `defaultTemplateId: ''` 不修**（F3-T4 越界观察提出「与 `conflictStrategy` 的白名单校验不一致」）。`''` 是刻意的哨兵值（裁定 39），且两者本属不同的约束类型：`conflictStrategy` 的取值是**闭合集**（schema 有权复制并校验），`defaultTemplateId` 是**开放引用**（指向模板 store 的预设 id，其集合由 `template.js` 拥有，交由 schema 复制只会造成第二份真相）。分层上「schema 原样透传 + 消费端回退链」（F3-T4 已实测「任何落空路径都终止在非空模板上」）是正确的。— 代价：若判错，将来 `''` 与白名单的风格差异会被读者当成遗漏再提一次。

**带入本期各任务派发的遗留项**（前序期 ledger 已记账，属本期任务触碰的区域）：
- F3-T7 minor ③ → **F4-T2**：`HomeView.vue` 的 `<Toast>` 上**不得**加外层 `v-if`（第二期裁定 26 引入过该回归）。T2 会改 `HomeView` 的 `<FileTable>` 绑定，顺带为 `<Toast>` 补一行注释说明这条守卫，并在提交前 grep 确认无外层 `v-if`。

**交最终整支评审 triage 的 deferred minor（代码注释类，本期任务范围外，不自行改）**：`AppPanel.vue:13` 注释里的 `shadow-sm` 字面量（会产出死规则）、`Toast.vue` 离场 `0.18s`（计划已改 0.2s、代码未改）、`style.css:15` 的字节数 72（本仓库实测 +98）、`workspace.js:226` 的守卫理由措辞、`check-sfc-compile.mjs` docstring 的过度声称、`settingsSchema.js` 注释里的后端引用（裁定 38）。

### 执行日志（第四期）

- BASE for F4-T1: `21eb1b8`。F4-T1 已派发（implementer, sonnet；携带裁定 51/52/53 —— 删遮罩外层 `aria-busy`、`watch(..., {immediate:true, flush:'post'})` 聚焦、离场 0.2s）。已告知：浏览器走查列 deferred-to-human；`check-sfc-compile.mjs` 与构建产物 grep 为机械闸门；不得 `git add dist`、不得 amend、5173 上的 vite 不属于本轮。
- **F4-T1 实现完成**（commit `173eda4`，单文件 +47/−31）。报告 DONE_WITH_CONCERNS。实现者自验：`npm run build` 无警告、`check-sfc-compile.mjs` 退出 0；产物 CSS 新增 `.overlay-{enter,leave}-{active,from,to}[data-v-394baa3b]`、旧 scoped 哈希 `data-v-ca61c276` 归零（残留的 `.modal-*` 全是 AppModal 的无 scoped 规则，未被触碰）；三条裁定全部落实（`aria-busy` 已删、`watch` 无 `onMounted`/`nextTick`、离场 0.2s）；`backdrop-blur` 已去，`bg-ink/50`→`#15181b80`、`shadow-overlay`、`rounded-[12px]`、`w-[320px]` 均生成。dist 保持未暂存、未还原、无 amend。
- **更正 Ruling 52（实现者纠正我的机制论断，我亲手核实后确认实现者正确）**：`immediate: true` **会同步直调 job、完全绕过 scheduler**，因此 `flush: 'post'` 对 immediate 那一次**不生效**。核实位置：`node_modules/@vue/reactivity/dist/reactivity.cjs.js:1962-1968` —— `if (cb) { if (immediate) { job(true) } else { oldValue = effect.run() } } else if (scheduler) { scheduler(job.bind(null, true), true) }`（Vue 3.5.43；注意 `scheduler` 只在**无 cb** 的路径上被调）。runtime-core 侧 `flush: 'post'` 的 scheduler 是 `(job) => queuePostRenderEffect(job, …)`，只对**变化触发**的那次生效。**后果分层**：①可达路径（`modelValue` 由 false 翻 true）经 scheduler 排进 post 队列 → DOM 已更新 → 聚焦**正常工作**，Ruling 52 的目标已达成，无活 bug；②「挂载时 `modelValue` 已为 true」仍**不被覆盖**（当前不可达，因为 `HomeView` 用非惰性 `v-if="!ws.executeDryRun"` 挂载）；③代码注释 `ExecutingOverlay.vue:49-50` 那句「因此即便某天组件以 modelValue 已为 true 的状态挂载，immediate 的那次也会拿到元素」是**我的错误措辞被照抄**，属事实错误但零行为影响。裁定：**Minor，不进修复轮**，与 ledger 已记的同类注释缺陷（裁定 11 / 24 / 29 / 43）一并交最终整支评审 triage；修法是把该从句改为陈述可达路径，或直接删掉它 + 把 `immediate: true` 也删掉（可达路径不需要它）。— 代价：若判错，注释里留一句不成立的从句。
- F4-T1 minor（deferred）：①上述注释从句 + `immediate: true` 的 dead intent；②brief Step 2 的 7 项浏览器走查全部 deferred-to-human（第 6 项时长按 ~200ms 而非 ~150ms）；③卡片上 `role="status"` 自带的显式 `aria-live="polite"` 是冗余的（brief 指定，无害）。**另记**：这是本轮第一次由实现者用源码位置推翻我的机制论断 —— 与裁定 35（实现者用测量推翻我的指令）同类，纪律价值同上：**凡我在指令里写「因为 X 所以 Y」式论断，要么先实测，要么标注为未验证的猜测**（这条纪律在裁定 29 已立，本次再次被验证有效）。
- **暂停点（用户要求：子任务完成后暂停）**：T1 的**任务评审尚未派发**。已就位的工件：brief `.superpowers/sdd/2026-09-25-frontend-4-polish/task-1-brief.md`、report 同目录 `task-1-report.md`、评审包 `review-21eb1b8..173eda4.diff`（1 commit，4557 B）。**恢复后的第一个动作** = 派 T1 任务评审（reviewer, sonnet；三份工件路径已列，global constraints 照抄本期约束与裁定 50–58，**不 pre-judge** concern 1，让评审独立判其严重度）。**第二个动作** = 派 T2（brief 已生成 `task-2-brief.md`；派发须携带裁定 50「不加 `animate-pulse`」、裁定 54「蒙层删除步骤已由 F2-T6 完成、Step 1 的 grep 预期为 0 行」，并携带 F3-T7 minor ③「`HomeView` 的 `<Toast>` 上不得加外层 `v-if`」）。
- **F4-T1 任务评审：Spec ✅（三条 binding 裁定逐条在 diff 内核实）/ Task quality: Needs fixes。2 条 Important（同源）+ 3 条 Minor。** 评审做的关键工作是把我的「不可达」判断**推翻**了：`HomeView.vue:89` 的 `v-if="!ws.executeDryRun"` 在试运行期间会**卸载**该组件（试运行把 `executeDryRun` 置 true），而执行时 `executing=true` 与 `executeDryRun=false` 在同一 tick 写入（`workspace.js:352-353` 是 `executing.value = !dryRun` + `executeDryRun.value = dryRun`）→ 下一 flush **重新挂载**且 `modelValue` 已是 `true` → `immediate` 的 job 在 `setup()` 内同步跑、ref 为 null → 聚焦 no-op → `@keydown.tab.prevent` 收不到 keydown → Tab 逃逸。**这条路径就是 brief 走查第 3 项，且在普通的「试运行→执行」流程上可达。**
- **更正我对 F4-T1 concern 1 的裁定（我判「不可达」，错）**：我独立核实了 `workspace.js:345-356` 与 `HomeView.vue:89`，**评审正确**。因此 concern 1 不是 Minor 而是 **Important**，T1 进修复轮。同时评审确认：可达的「已挂载 → 翻 true」路径**正确**（refs 在 patch 末尾同步绑定、早于 post flush），且 `immediate: true` 除每次挂载贡献一次 guaranteed no-op 外无作用。**这是本轮第二次由子代理用证据推翻我的判断（前一次是实现者用源码位置纠正 Ruling 52 的理由）—— 两次都指向同一个根因：我在没有实测的情况下断言可达性/机制。**
- F4-T1 评审的 Minor（**已裁定不进修复轮，记账**）：①遮罩关闭后不归还焦点（错误路径上焦点掉到 body；brief 未要求，spec §10 未要求）；②`role="status"` 上显式 `aria-live="polite"` 冗余（brief 指定，保留）；③`dryRun` prop 恒为 false、干跑根本不显示遮罩（既有行为，非本 diff 引入，且它是 Important #1 得以成立的使能条件）。评审的 ⚠️ 项（构建/SFC/产物 grep 仅为声称、diff 无 dist 变化）**已由我独立复跑闸门消解**：build 通过无警告、`.overlay-enter-active`/`.overlay-leave-active` 各 1、`shadow-overlay` 2、`bg-ink/50` 2、`#15181b80` 1、`rounded-[12px]` 1、`text-[14px]` 1、`backdrop-blur` 0、旧 scoped 哈希 `data-v-ca61c276` 0。
- 我的失误记录（第二次踩同一个坑）：独立复跑闸门时把类名模式写成带反斜杠转义的 `bg-ink\/50` / `rounded-\[12px\]`，得到两个**假的 0**，一度误判产物缺规则。ledger 裁定 9 已明确「类名一律写未转义的原样形式」—— 再记一次，避免第三次。
- **F4-T1 修复轮 1 已派发**（resume 原实现者）。findings 逐字给出：Important #1 + #2；修法指定为 **去掉 `immediate: true`、改用 `watch(..., { flush: 'post' })` + `onMounted(() => { if (props.modelValue) overlayRef.value?.focus() })`**（两条路径各自落在「元素确定已在 DOM 里」的时刻，不需要读者推理调度时序），并更正 `:46`/`:50` 两处不成立注释。明确划出三条 Minor 不在本轮范围。要求覆盖验证：build + SFC 编译（必须），CDP 驱动系统 Chromium 跑「试运行→执行」断言 `document.activeElement` 落在遮罩上（应当），失败时退回隔离挂载并说明卡点。
- **F4-T1 修复轮 1 完成**（commit `694bad2`，单文件；无 amend、`dist` 未暂存、分支未变）。改动：去掉 `immediate: true`（watch 现为 `{ flush: 'post' }`，只负责"已挂载 → 翻 true"）、新增带守卫的 `onMounted(() => { if (props.modelValue) overlayRef.value?.focus() })` 与 `onMounted` 的 vue import、重写 `:46`/`:50` 两处不成立的注释为「两条路径」的准确描述（并写明 `immediate` **不能**覆盖第一条）。**验证质量本轮最高**：用 CDP 驱动系统 Chromium 跑真实「试运行 → 执行重命名」流程，断言 `document.activeElement === document.querySelector('div[tabindex="-1"].fixed.inset-0')` —— 修前 `activeIsOverlay=false`（`activeTag=BUTTON`，即那个被困在遮罩后的「确定」按钮，正是评审预测的失效模式），修后 `=true`；且它做了**同 driver 的 before/after 对照**（临时把已提交的 pre-fix 组件写回工作区、跑同一驱动、再恢复并用 `diff` 确认逐字节一致），排除了"两次跑的环境不同"这一解释。它还在编码前独立复核了可达性（`workspace.js:352-353` 同 tick 双写 → `HomeView:89` 的 `v-if` 翻向 → 带 `modelValue=true` 重挂载），并再次在源码层确认机制（`reactivity.cjs.js:1964-1965` 的 `job(true)` 绕过 `effect.scheduler`）。清理：:5199 dev / :8000 后端 / :9222 Chromium 全部停止并确认，:5173 那个不属于本轮的 vite 未被触碰（仍返回 200）。
- **F4-T1 scoped re-review 已派发**（reviewer, sonnet；fix diff `review-173eda4..694bad2.diff`，1 commit / 3213 B；findings 只有两条 Important，#1 与 #2 逐字给出；明确告知三条 Minor 不在范围）。待其 verdict；若「All findings addressed, no new Critical/Important breakage」即写 T1 完成行并进 T2。
- **F4-T1 scoped re-review：All findings addressed, no new Critical/Important breakage。** 两条 Important 均判 **ADDRESSED**，且是**源码级**佐证而非采信报告：`onMounted` 在该路径上**确实**能看到已绑定的 `overlayRef` —— `setRef` 把模板 ref 作为 post-render job 入队并令 `job.id = -1`（`runtime-core.cjs.js:1871,1873`），而组件自身的 mounted hook 在**子树 patch 之后**才入队（`:6224-6225`），post 队列按 `getId` 排序（`:362-366`），`getId` 对 ref job 返回 `job.id`（-1）、对无 id 的 mounted wrapper 返回 `Infinity` → **同一次 flush 内 ref 先赋值、onMounted 后跑**。新破坏：**无** —— 并论证了两条路径**互斥**（watch 不再 immediate，故 mount-with-true 时只有 `onMounted` 触发、后续翻转时只有 watch 触发，不存在双重聚焦），以及遮罩在 post flush 抽干前卸载时 ref job 排在前、`overlayRef` 被置空而 `?.` 吸收。fix diff 只碰 `ExecutingOverlay.vue`（+12/−7），`Transition` / `@keydown.tab.prevent` / `tabindex="-1"` / 两条 0.2s 规则均未被本轮触碰。Out-of-scope 三条（`dryRun` 死文案、焦点不归还、`aria-live` 冗余）与我先前的裁定一致，均记账不进 loop。
- **F4-T1 完成**：`Task 1: complete (commits 21eb1b8..694bad2, review clean)` —— 一轮修复（4 条 finding：2 Important 已修 + 3 Minor 记账；含我一次错误裁定的更正）。
- BASE for F4-T2: `694bad2`。
- **F4-T2 已派发**（implementer, sonnet；携带裁定 50「不加 `animate-pulse`，连带 Step 3 第 7 项改写为「静态灰块（设计如此）」」与裁定 54「蒙层删除步骤已由 F2-T6 完成、Step 1 的 grep 预期为空」；并携带 F3-T7 minor ③「给 `HomeView` 的 `<Toast>` 补一行『不得加外层 v-if』的注释 + 提交前 grep 确认」）。要求 Step 3 的行高校准**必须实测**（首选隔离挂载量真实 `<tr>` 高度，次选真实应用 + CDP），不接受把 `h-[58px]` 的猜测留在原地。
- 本地核实（为 T4/T6 预备，`style.css` 156 行的结构）：`@import` 在 :1；新 token 的 `@theme static` 在 :16–57；**别名块在 :59–91**（注释 :59–72 + `@theme` :73–91，正是 T6 要删的范围，与 brief 的起始行文本吻合）；`html,body,#app` :93–97、`body` :99–106、`*` :108–110、`@layer base` :116–124、`button:not(:disabled)` :136–138、滚动条与其后 :143–156（T4 的兜底块追加在文件末）。**Ruling 59（F2-T5 minor ⑤ 的归属）：`html,body,#app{height:100%}` 与 `body{…}` 仍在层外，本期**不修**。** 理由：①无活的需求（今天没有作用于 body/#app 的工具类与之竞争）；②把它们移入 `@layer base` 会改变优先级（base 早于 utilities → 工具类将能覆盖基线），这是"更正确"的分层但会削弱「body 字体是基线」这一意图，属设计变更而非缺陷修复；③真正被识别出的缺陷（`input/select/textarea/button` 的 font-family 压掉 `font-mono`）已在 F2-T5 修复轮修过。留作「将来若有人给 body/#app 加工具类会踩的坑」。— 代价：若判错，将来的一个工具类会被静默压掉，且症状（样式不生效、构建不报错）与 ledger 已记的三次层叠层教训同形。
- **F4-T2 实现完成**（commit `c07345d`，3 文件 +52/−3）。报告 DONE_WITH_CONCERNS。自验：build 无 warning、SFC 编译 exit 0、Step 5 两个死代码计数均 0、源码与产物 CSS 中 `animate-pulse` **均 0 处**。**行高实测达标**：真实数据行 **59.39px**（40 行合成数据、1440/1024/800/700/375 五种宽度一致；唯一 58.89 是末行落在分数像素上的底边残差）→ `h-[58px]` 改为 **`h-[59.4px]`**，复测骨架行 59.39px、逐行差 <0.01px。**额外闸门（本轮最强）**：与 HEAD 的 worktree 构建逐条 diff 产物 CSS —— 本次只新增 6 条规则（`.h-[59.4px]`/`.w-16`/`.w-[120px]`/`.w-[180px]`/`.w-[200px]`/`.w-[240px]`），全部被新 markup 真实使用、**0 条死规则**（正是裁定 23 那类陷阱的直接防线）。两条裁定落实：不加 `animate-pulse`（DOM 探针实测骨架行 `animation-name: none`），走查第 7 项记为「不适用」；蒙层那步确认无事可做（改动前即 0 命中，DOM 里 0 个 `inset-0` 绝对定位元素 / 0 个 `bg-white/60` / 0 个 `div.relative`）。`HomeView` 的 `<Toast>` 上方已加「不得加外层 v-if」注释并 grep 确认。环境已清干净，:5173 未触碰。
- **F4-T2 的 concern 1（待评审）**：brief 走查第 3 项断言「各列天然对齐（列宽由 `<th>` 决定…天然对齐）」**实测不成立** —— `table-layout: auto` + 单元格 min-content 会顶开固定宽度：1440 下 `#` 列 48 vs 56、`状态` 102.02 vs 96；1000 宽下数据态 thead 66.09 vs 骨架 47.3（`解析剧名` 表头换行）。实现者**未擅自改 markup**（brief 权威、spec §10 明文只要求行高），并论证**非骨架引入的回归**：空态基线 thead 46.8 且列宽与骨架几乎一致，是真实数据把列挤变形，没骨架屏照样跳。**我的初步判断（留待评审独立判后再定）**：spec §10 的硬判据是「行高与真实行一致」，已达标；列宽跳动在骨架出现之前就存在（任何「无表格→有表格」的转换都经历它）；修它要动列宽机制，会牵动 F2-T6 的 md 档 952px 与 F2-T7 的窄屏 min-content 783.88px 等已实测结论，风险与收益不成比例 → 倾向**不修、记 deferred**。但按纪律**不 pre-judge**，让评审先判。
- **F4-T2 任务评审：Spec ✅ / Task quality: Approved，无 Critical 无 Important，5 条 Minor。** 评审逐项核实了 brief 的每个细节与两条裁定，并做了几处有价值的独立判断：①确认**列几何按构造同步而非碰巧** —— 骨架 `<td>` 1 用 `sm:table-cell`、`<td>` 4 用 `md:table-cell`，与真实行（`:102`/`:114`）及 `<th>`（`:46`）逐一对上，断点处不会显/隐错列；②确认 `h-[59.4px]` 是**实质收益**（照 brief 的未校准 58px 会留下 spec §10 明文禁止的 1.4px/行跳动）；③确认新注释含**零个**类名样 token（v4 注释采集闸门未被触发），且实现者是用**产物 CSS 规则级 diff** 证明的而非声称；④确认删除死 store 字段安全（`stores/files.js` 唯一消费者 `workspace.js:19` 从不碰 `scanning`）。它另核实了新 prop 绑到的是**真正活的状态**（`workspace.js:33`，在 `:137/155/188/206` 翻转、`:393` 导出），正对「从未被赋值」这个根因。
- **Ruling 60（评审判 Minor、我裁定为「约束措辞过窄」）：`rounded-[4px]` 保留不动。** 全局约束写「只允许 `rounded-[8px]`/`rounded-[12px]`」，而 `rounded-[4px]` 既不在显式禁用清单（`rounded-full`/`xl`/`2xl`/`md`/`lg`）里、又是**本项目已确立的第四档小元素圆角**（`AppCheckbox.vue:12`、`SettingsView.vue:86`、`BrowseDialog.vue:35` 都在用，且骨架块正是复刻复选框的尺寸）。这与裁定 16 同构（具体实践压过过宽的一般措辞）。裁定：**不改 markup**，约束应理解为「容器 12px / 控件 8px / 既有小元素 4px」三档；修正约束文本属于 spec 的归属，不擅自改用户的文档，**在最终交付里向用户点出**。— 代价：若判错，全站小元素多一档圆角；但四处在用且视觉一致。
- **Ruling 61（澄清，评审的 Minor 2 不成立）**：`HomeView.vue:104-105` 那两行 `<Toast>` 注释**是我在派发里明确要求带的**（F3-T7 minor ③ 的遗留项：第二期曾把外层 `v-if` 引入 Toast 造成离场动画失效的回归，而该处无守卫），不是实现者越界扩范围。评审不知此背景，其「属任务范围外」的判断在信息不全时是合理的。不修。
- F4-T2 minor（deferred，记账）：①`rounded-[4px]`（见 Ruling 60）；②`HomeView` 的 Toast 注释（见 Ruling 61）；③`h-[59.4px]` 是**硬编码魔数**，与字体度量耦合且无守卫 —— 将来改 `py-2.5` 或行高会静默reintroduce 本任务要消除的跳动（plan-mandated：Step 3 要求回填实测像素），评审建议从设计 token 处加一行指针；④`:54-60` 的 7 行内联理由偏长（含会随 spec 重编号而腐烂的 §11 行数引用），属品味；⑤**`frontend/src/api/request.js:13` 用 `rounded-lg`/`shadow-lg`/`transition-all duration-300`** —— 三处全局约束违规（圆角、阴影、§11 表外动效；`text-sm` 是 14px 合规）躺在一个遗留 toast helper 里。见 Ruling 62。
- **Ruling 62（评审的 out-of-scope 发现，经我判断为真但不在本期范围）**：`api/request.js:13` 的样式违规**成立**，但该文件**不在 spec §14 的重构清单里**（新增 12 / 重写 5 / 改造 11 / 删除 3 都没有它），本期 7 个任务也无一触碰它 —— 它是 spec §3 审计时**漏掉的文件**（大概率是程序化创建 DOM 的 toast helper，不走 `.vue` 组件管线，故两次 grep 审计都没扫到）。裁定：本期**不修**，交最终整支评审 triage；并在**最终交付里向用户如实列出**（连同 spec §3 审计的这处遗漏一起说）。— 代价：交付后全站仍有一处不符合新设计语言的浮层（观感不一致，非功能缺陷）。
- **F4-T2 完成**：`Task 2: complete (commits 694bad2..c07345d, review clean)`。
- BASE for F4-T3: `c07345d`。
- **F4-T3 实现完成**（commit `271d1a4`，单文件 +38/−19）。报告 DONE_WITH_CONCERNS。自验：build 无警告、SFC 编译 exit 0；产物 CSS 规则级 diff 只 `+.p-8`（真实使用）、`−.mt-2`、`−.py-16` → **0 死规则**。**走查 10 项里 9 项实测**（隔离挂载真实 SFC + CDP，无需后端）：①843px 区域下内容中心 478.49 vs 区域中心 478.5 → **0.01px**；②结构（svg `aria-hidden`、14px 标题、13px/21.1px 行高的说明、`#0F766E` accent 按钮）全中；③真实 click → `["quick-scan"]`（`HomeView:67` 映射到与左栏本地分支同一个 `ws.doScan()`）；④数据到达 → 40 行、空态消失；⑤「清空」后空态回来仍 0.01px；⑥375×812 下 755px 区域 0.01px off **且**说明文字 `scrollWidth 311 == clientWidth 311`（0 溢出）；⑦按钮 `getClientRects().length===1`、`nowrap`、84×36；⑧`activeSource="openlist"` → 无按钮 + 左栏文案；⑨连接后仍无按钮。**仅第 10 项（读屏实际播报）deferred**。环境已清（:5199 / Chromium 9222 / worktree / harness 文件全删；:5173 pid 12475 未碰）。
- F4-T3 concern（均为观察，非缺陷，待评审独立判后再定）：①空态守卫是 `activeSource !== 'openlist'` 而 `@quick-scan` 硬连本地 `ws.doScan` —— 今天两源下正确，若加第三源则潜伏（spec 只定义两个源 → YAGNI）；②空态居中于**表头下方 57px 的表格区**而非整个面板（`deltaCentreMainVsContent = −28.49px` = 表头的一半）—— **与 spec §10 的「垂直居中于表格区」措辞一致**，且与 brief 的 markup（该块是表头**之后**的 `flex-1`）一致，故符合规格；③未跑真实端到端（同 T2 的隔离挂载路线）。
- **F4-T3 任务评审：Spec ✅ / Task quality: Approved，无 Critical 无 Important，4 条 Minor。** 评审做了几处有分量的独立验证：①**四个状态组合的穷尽性检查**（`scanning && !len` → 骨架；`scanning && len` → 数据行（T2 的「重扫不闪骨架」语义被保留）；`!scanning && len` → 数据行；`!scanning && !len` → 空态块、DOM 里无 `<table>`）—— **无缺口无重叠**；②`activeSource` 的域核实（唯一源列表在 `AppTopBar.vue:50-51`，默认 `'local'`，故 `!== 'openlist'` ⇔ `=== 'local'` 今日成立）；③flex 链核实（`FileTable` 根 `flex min-h-0 flex-1 flex-col overflow-hidden bg-surface`，工具条 `shrink-0`，新块的 `min-h-0 flex-1` → 有确定高度可居中）；④**指出这次移动修掉了一个真实缺陷**：空态原先在 `<td>` 里、受 `min-w-[560px]` 约束，375px 下会强制横向滚动，移到表外后不可能再发生（这是 brief 只暗示、未点明的一点）。类名增量与模板增量一致（只丢 `−.mt-2`/`−.py-16`、只加 `+.p-8` 且真实使用，0 死规则）。
- **评审核查项由我执行并消解**（评审的 ⚠️ 2）：它怀疑索引里混进了 dist。实测 —— `git diff --cached --name-only` **为空**；三个任务的提交区间（`21eb1b8..694bad2`、`694bad2..c07345d`、`c07345d..271d1a4`）对 `frontend/dist` 的 diff **全为空**；`git status --porcelain | cat -A` 显示所有 dist 条目前缀均为 ` D`（未暂存）。评审看到的「第一条无前导空格」是我早先 `git status --short` 的列对齐显示，非索引状态。**结论：三个提交与索引都不含 dist，符合裁定 4。**
- **Ruling 63（计划自相矛盾，已裁定）：空态在 `openlist` 源下**没有按钮**（以文案引导左栏），不采纳 brief Interfaces 行「换成『去连接 OpenList』」的说法。** 依据：①brief 的 Step 2 代码把 openlist 分支写成 `action: null`，Step 3 走查第 8 项（未连接）与第 9 项（已连接）**都**断言「空态没有按钮，文案指向左栏」—— 三处一致对一处；②要满足 Interfaces 行必须新增一个「连接」emit，而同一份 brief 的 Interfaces 又要求「**无新接口**」并把 emit 列表冻结在六项上 —— **那行读法自我矛盾且不可行**；③spec §10 的通用模式是「图标 + 标题 + 一句说明 + 主操作按钮」，而 openlist 源下"主操作"（去连接/选目录）发生在左栏，用一句文案指路是唯一不越界的表达。**记录**：这是本期第二处「brief 的 Interfaces 行与其 Steps 互相矛盾」（第一处是 T1 的 `aria-busy`），且都属「正文/约束为权威、陈旧表述为错」这一已立规律的复发。最终交付时向用户说明 spec §10 的通用模式在 openlist 源下以文案替代按钮。— 代价：若判错，openlist 空态少一个直达连接的按钮，用户需自己看左栏（而文案正指向左栏）。
- F4-T3 minor（deferred，记账）：①`title: '尚未加载文件'` 在两个分支里逐字相同，只有 `description`/`action` 变化 —— brief 逐字如此，但重复的字面量让 computed 读起来像"标题本该随源不同"；②`activeSource !== 'openlist'` 的**否定式**守卫 vs `@quick-scan` 硬连本地 `ws.doScan`，而工具条的扫描是按源分派的（`ws.activeSource === 'local' ? ws.doScan() : ws.doOpenListScan()`）—— 今日两源下完全一致，仅当将来加第三源才潜伏（brief 强制，见 Ruling 63 的关联）；③空态无 live region，故「清空」后它的出现不会被播报（超出 brief 的打磨）；④brief Interfaces 行与 Steps 的矛盾（见 Ruling 63）。⚠️ 遗留：走查第 10 项（真实读屏播报顺序）仍需人工。
- **F4-T3 完成**：`Task 3: complete (commits c07345d..271d1a4, review clean)`。
- BASE for F4-T4: `271d1a4`。
- **F4-T4 实现完成**（commit `87770a2`，单文件 +22）。报告 DONE。自验：build exit 0 无警告且**两次构建字节一致**；brief Step 2 的五条产物 grep 全非零（`prefers-reduced-motion` 1、`animate-spin` 3、`.modal-enter-active` 2、`.toast-enter-active` 1、`.overlay-enter-active` 1）；兜底块经机械确认**在层外且带 `!important`**。**9 项走查全部实测**（headless Chromium + Node WebSocket + 一次性 Vite 入口 :5199；:5173 未碰）—— 这是本期第一次「非 deferred 的全项走查」：`.modal-*`/`.overlay-*`/`.toast-*` 的 active 规则在 reduce 下 `transition-duration: 1e-05s`、no-preference 下恢复 `0.2s`；`animate-spin`（AppButton 与 overlay）`1e-05s` / `iteration-count: 1`、基线 `1s` / `infinite`；表格行 `transition-colors` `1e-05s` vs `0.15s`；**模态关闭后 `stillInDom: false`** → `.01ms` 而非 `0` 的那条守卫（`transitionend` 必须能触发）被实测确认；第 8 项 `:active` 的 `scale: 0.98` **在 reduce 下仍生效**（证明兜底没有过度覆盖）；第 7 项骨架 `animation-name: none` 两态一致，**正确记为「设计如此」而非归功于兜底**（裁定 50）；无页面 console 错误。
- F4-T4 concern（观察，非缺陷，待评审独立判后再定）：①感知流畅度（走查 1/3/5）与真实 GNOME 开关行为仍需人工（本轮用 CDP 的 `Emulation.setEmulatedMedia` 模拟）；②`scroll-behavior: auto !important` 当前**惰性**（全项目无 `smooth`）；③通配符会把将来任何**有意义的**无限动画一并压平 —— 按 spec §11（表外动画一律不加）是有意的，属设计后果而非缺陷。探针脚手架已删、进程已杀。
- **F4-T4 任务评审：Spec ✅ / Task quality: Approved，无 Critical 无 Important，3 条 Minor。** 评审做了本轮最重的独立核验：①**独立确认层外位置**（`style.css` 全文只有 `@layer base` 一处 at-rule 于 :116–124，故追加的 :158–178 在所有层之外；产物 `index-Dj5qjTSY.css` 里该块位于 byte offset 23126，open-`@layer` 栈 `[]`、brace depth 0）—— 它明说读 `style.css:108-178` 超出了 diff 上下文，理由是"追加块的层归属正是本任务的关键"；②兜底确实落在**始终加载的入口样式表**（`index-*.css` 有，懒加载的 `HomeView-*.css` 没有），规则能作用于所有路由的模态/Toast；③**三类载体逐处重新推导**（不从报告采信）：类型 1 的 `transition-colors duration-150` 12 处 + `transition-transform duration-150` 3 处 + **`transition-all duration-300` 在 `api/request.js:13`**（那个程序化创建 DOM 的 toast）——"第四处**站点**，通配符能抓到而枚举选择器方案会漏掉"，这条正好反证了兜底选型的正确；类型 2 的 AppModal/Toast/ExecutingOverlay 的 `<style>` 过渡；类型 3 的两处 `animate-spin`；④**无第四类载体可漏**（`@keyframes` 全项目零处，只有 Tailwind 内建 `spin`；两处 `requestAnimationFrame` 只是延迟类翻转、非动画；无 `delay-*`/`transition-delay`）；⑤**无竞争 `!important`**（`grep -rn '!important' src` 只命中本块四条）→ 层序与 scoped 特异性均无碍，与实测 `1e-05s` 吻合；⑥运行期声称由**保留的产物**佐证（`/tmp/cdp-out.txt` 存在且含逐项 computed 值，`frontend/probe.html`/`probe.js` 已从仓库移除）。它还独立指出兜底**按构造**不会过度覆盖：只设插值时长、迭代上限与 `scroll-behavior`，从不抑制状态变化或最终计算值 —— 故 `<Transition>` 收尾（`stillInDom: false`，且 `runtime-dom.cjs.js:314-318` 另有 `setTimeout` 兜底）与 `:active` 的即时 `scale`（`active:scale-[.98]` 编译为 `scale` longhand，而行上的 `transition-property` 本就不含 transform/scale）都存活。
- **Ruling 64（plan-mandated 的注释错误，我裁定：值不动、措辞需改，带进 T6 修）**：`style.css:166-167` 写的理由是「Vue 的 `<Transition>` 依赖 transitionend 事件来收尾，duration: 0 时事件可能不触发，元素会卡在离场状态」—— **该因果论断不成立**：评审用源码证明 Vue 3.5 在 computed transition timeout 为 0 时令 `type = null`（`runtime-dom.cjs.js:330-339`，`getTimeout` 在 `:354-360` 求 duration+delay 之和），`whenTransitionEnds` 于是提前 `resolve()`（`:299-302`）—— 即 **duration 0 会立即卸载而非卡住**；且 `grep -rn 'transitionend\|animationend' src` 除这条注释外无任何消费者。**但 `0.01ms` 仍是正确的值**（spec §11 的兜底代码块明文就是 `.01ms`，spec 是绑定权威；且它是 Bootstrap 惯例写法、能让 end 事件对任何消费者继续触发（含 `api/request.js:13` 那种程序化 DOM）、视觉上等同 0）。裁定：**值不动**；注释措辞改为陈述真实理由（零时长过渡不会启动、故任何监听者都收不到 end 事件），**修正动作带进 T6**（T6 本来就要改 `style.css`，边际成本近零；派发时说明这是 controller 要求的遗留项，以免评审误判为越界）；同时在最终 triage 清单里保留一条兜底记录。**这是本轮第 5 次「我的错误措辞被逐字抄进代码注释」**（前四次：裁定 11、24、43 与 Ruling 52），规律已足够稳固：**凡我在计划/派发里写「因为 X 所以 Y」式机制论断，若未经实测，写进代码注释就是给后人埋雷**。
- **Ruling 65（覆盖边界，作为保证限制记录，不修）**：通配符兜底只覆盖 **CSS 交付**的动效。将来若出现 ①`transition-delay`/`delay-*`（reduce 下延迟仍会保留）②Web Animations API（`element.animate()`）③rAF 驱动的动画，都会**逃逸**本块；此外 JS 发起的 `scrollTo({behavior:'smooth'})` 也不受 CSS 的 `scroll-behavior` 影响。今天这三者与 `scroll-behavior` 的 `smooth` 用法**全部为零**，故是前向性限制而非缺陷。记入最终交付的「保证边界」一节。— 代价：将来引入上述任一种动效时，reduced-motion 会静默失效，需同步更新兜底。
- F4-T4 minor（deferred，记账）：①`style.css:166-167` 的注释措辞（见 Ruling 64，已带进 T6）；②`scroll-behavior: auto !important` 当前**惰性**（全项目无 `smooth`，brief 强制，属前向性）；③覆盖边界（见 Ruling 65）。⚠️ 遗留：走查 1/3/5 的**感知**一半（"直接出现/消失"的手感）与真实 GNOME 全局开关仍需人工 —— 数值侧已由 CDP `Emulation.setEmulatedMedia` 全量实测（同一 media query，风险低）。
- **F4-T4 完成**：`Task 4: complete (commits 271d1a4..87770a2, review clean)`。
- BASE for F4-T5: `87770a2`。
- **F4-T5 实现完成**（commit `359b995`）。报告 DONE_WITH_CONCERNS。自验：Step 1 审计**无输出**（clean）、Step 2 全部 **12 个文件 ≥1**、build clean、`check-sfc-compile.mjs` exit 0。**走查 9/10 实测**（vite :5199 + headless Chromium/CDP、**真实键事件**、`/api/**` 已被 stub）+ 为新的 ArrowRight 下钻**新增 1 行**；仅第 10 项（读屏实际播报）deferred，但把它的 aria 管道测了（`aria-label="文件重命名预览"` 存在、容器 `aria-busy` 走 null→"true"→null）。两条裁定落实：`FileTable` 的 `<table>` 加了 `aria-label`、滚动容器加了 `:aria-busy` 且 **`v-else` 保留**（Ruling 55）；`BrowseDialog` 的 `KEYS` 加了 `ArrowRight` + 下钻分支，`Enter`/`Space` 保持选中语义（Ruling 56）。
- **Ruling 66（采纳实现者对 brief 字面机制的偏离）：焦点交接改用 `load(path, { focusFirst })`。** 我在派发里写的字面是「在分支里聚焦下一项**并** `navigate(...)`」。实现者**先测了机制再动手**：`navigate` 会**同步**置 `loading = true`，而 `v-if="loading"`/`v-else` 这一对会**卸载整个目录列表** → 下钻后 `document.activeElement` 落到 `BODY`、焦点离开对话框、下一次 Tab 被 AppModal 的焦点陷阱拉回对话框首控件 —— 所以「聚焦下一项再 navigate」**是惰性的**，照字面实现会交付一个「焦点消失的下钻」。它把 brief 的"the next item"读作"下一个该持有焦点的元素"，通过 `load(path, { focusFirst })` 把焦点交给新列表第一项；其他 `navigate` 调用者不传 opts，故行为**逐字节一致**（实测 ArrowDown/Enter/Esc/选中全部仍通过）。**裁定：采纳** —— 偏离保留了意图（键盘下钻可达）而我给的机制与真实代码相悖；这是本轮第三次「实现者用实测/源码推翻我的指令」（前两次：Ruling 52 的理由、T1 评审推翻我的"不可达"判断），纪律结论同前：**我给机制时要么先实测，要么标注为待验证的猜测**。
- **两个测量陷阱（记入 ledger，供 T7 复用）**：①CDP 的 `Fetch` 拦截模式 `*/api/*` 会**连带匹配 Vite 自己的 `/src/api/*.js` 模块**并把它当 JSON 返回 —— 必须锚定到 dev root 之后；②**非空 `box-shadow` 不是焦点环的证据** —— Tailwind 总会发一个透明的占位层，且 v4 把 `ring-accent/35` 编译成 `color-mix(...)`，**序列化为 `oklab(0.510931 -0.0854972 -0.00957102 / 0.35)`**；只认 `rgb`/`rgba` 的解析器会误报（曾得"17/47 站无环"），修正后**页内全部 47 个站点都有真实 accent（#0F766E）环**，唯一无环的是 `body`（浏览器自身 Tab 环绕边界）。**T7 Step 5 的对比度抽检必须按这个教训实现颜色解析。**
- F4-T5 concern 裁定（均记账、不修）：①焦点交接偏离 brief 字面 → **采纳**（Ruling 66）；②下钻到**空目录**后焦点留在 `body` 直到下一次 Tab —— 已缓解（实测模态陷阱会把它拉到对话框的关闭按钮），更完整的 fallback（如退到"上级"）是设计决定，不单方面做；③第 10 项读屏需人工；④Step 4 的盲区 grep **不是字面上空的** —— `AppModal` 的 `FOCUSABLE` 选择器字符串 `'textarea:not([disabled])'` 是一个假阳性，实际无真实 `<textarea>`、无 `<div @click>`，故什么都没转换（与 F2-T2「注释里的字面量让 grep 假失败」同类，属 brief 判据的假阳性）；⑤下钻是 `ArrowRight`，而 `ArrowLeft` 不上溯 —— 不对称是有意的（面包屑负责上溯，对话框内 `ArrowLeft` 无既有语义，无 spec 依据）。环境已清（:5199 / :9222 释放，自身 vite 与 Chromium 已杀；:5173 pid 12475 未碰、仍 200），驱动与输出留 `/tmp/er5/`。
- **F4-T5 任务评审：Spec ✅ / Task quality: Needs fixes。1 条 Important + 4 条 Minor。** 评审做了本轮最细的一次判断：**它先采纳了实现者的偏离** —— (a) 机制声称**从模板结构即可验证**（`load` 在首个 `await` 前同步置 `loading.value = true`（`BrowseDialog.vue:187`），列表在 `v-if="loading"` → `v-else-if="!dirs.length"` → `v-else ref="listRef"`（`:50-57`）链之后，故下一 flush 全部按钮卸载，"聚焦下一项再 navigate"确实惰性）；(b) 交付实现达成意图（`load` 在 `loading` 翻回后 `await nextTick()` 再聚焦新列表首项，`:204-211`，一次 `nextTick` 够用因为四个状态都在同一 job 内同步变异）；(c) 非下钻调用者**经枚举确认**全无影响；(d) 索引对齐**是真的**（容器唯一后代是那批按钮，嵌套元素是 `<svg>`/`<span>`、无嵌套 `<button>`）。它另确认新加的 `aria-busy` **落在真能触发的位置**（`scanning` 为真时 `v-else` 容器确实挂载、而骨架行 `aria-hidden="true"` 故 busy 区域不会把占位方块当内容播报），并**手工补了 Step 1 正则的结构性盲区**（`RouterLink` 没有字面 `<a `）—— 查到 `AppTopBar.vue:29-36` 带全套 focus ring，故盲区为空。
- **F4-T5 的 Important（本任务引入的回归，已派发修复）**：`BrowseDialog.vue:208-211` —— **下钻进一个没有子目录的目录时，焦点落到 `BODY`，而 `aria-modal="true"` 的对话框还开着**。`listRef` 在 `v-else-if="!dirs.length"` 那一支为 null，`focusFirst` 的 fallback 成了空操作。评审论证它不是罕见边缘（下钻到碰巧无子目录的目录是普通浏览失误）、且**本任务之前不可能发生**（键盘用户根本下不了钻）；判 Important 而非 Critical 是因恢复路径确实存在（它核实 AppModal 在 `document` 上以**捕获阶段**注册处理器，故 `Escape` 仍能从 body 关闭、下一次 Tab 也被 `!inside` 分支拉回）。它还明确否掉了一种读法：报告里的"documented limitation"是**理由**而非降级依据 —— 此处的最低要求（**焦点留在打开的对话框内**）并不是实现者所拒绝做的那个设计决定（那是"选哪个目标"）。修法（我采纳）：给「上级」`AppButton`（`:23-30`）加模板 ref 作 fallback，`;(listRef.value?.querySelector('button') || parentButtonRef.value)?.focus()`；语义上合适（走进死路后往上走是自然的下一步），**不新增控件**。
- **Ruling 67（破例把一条 Minor 并入本轮，理由同裁定 13）**：把 Minor「下钻**失败**时 `focusFirst` 仍用**旧列表**跑、焦点被挪到旧列表第一项（`:199-203`）」并入本次修复。理由：它与 Important 同处一段代码、修法相邻（都是 `focusFirst` 交接的守卫），分两轮改会在同一处产生冲突；且它不是措辞或观感问题而是**交接的正确性**。修法：把交接限定在成功路径（`loaded` 标志或把交接块移进 `try` 的成功分支）。— 代价：本任务多改几行。
- **Ruling 68（越界发现，归入最终 triage 清单）**：`AppTopBar.vue:21` 在控件上用 `rounded-[6px]`，而约束只允许 12px（容器）/ 8px（控件）/ 既有的 4px（小元素）。本 diff 未引入任何 radius，故不在本任务范围。裁定：**记入最终整支评审的 triage 清单**，与 Ruling 60 的 `rounded-[4px]` **合并成一条结论**：约束文本（「只允许 8/12」）与代码现实（实际存在 4px 与 6px 两档小元素圆角）不符，应在最终交付里向用户点出并建议修正约束措辞，而非逐个改 markup。
- F4-T5 minor（deferred，记账）：①`BrowseDialog.vue:164` 的 `event.preventDefault()` 在 `current < 0`/`!target` 守卫之前（不可达：容器无 `tabindex`，keydown 只能从子按钮冒泡上来）；②报告 §2「无 `<div @click>`」的验证用了单行正则，多行宽容搜索会找到 `AppModal.vue:4-8` 的遮罩背景 `@click="onOverlayClick"` —— **结论正确且该元素正确地不该转换**（它是点击外部面，键盘等价物是 `Escape`），仅验证方法覆盖不足；③`AppTopBar.vue:21` 的 `rounded-[6px]`（见 Ruling 68）。
- **F4-T5 修复轮 1 已派发**（resume 原实现者）：Important（「上级」按钮 ref 作 fallback）+ Ruling 67 并入的 Minor（成功路径守卫）。要求三条焦点断言实测：①下钻到**有**子目录 → 焦点落在新列表首项（复测防退化）；②下钻到**无**子目录 → 焦点**在对话框内**（修前 BODY、修后「上级」按钮）——核心断言；③下钻**失败** → 焦点不被移到旧列表首项。外加 `ArrowDown/Up/Home/End/Enter/Esc/选中` 全部仍通过。
- **F4-T5 修复轮 1 完成**（commit `377c080`，单文件 23+/8−；无 amend、无 push；`dist` 未暂存未还原）。**三条焦点断言全部 before/after 实测**（同一 driver/同一 stubs，真实应用 + CDP）：**A1** 下钻到**有**子目录 → 焦点 = 新列表首项（`Season 1`，框内）修前修后一致、无退化；**A2** 下钻到**无**子目录 → 焦点**在对话框内**，修前 `BODY`/`insideDialog: false` ❌、修后 **`上级`**/`true` ✅；**A3** 下钻**失败** → 焦点**不**被移到旧列表首项，修前 `Show`（idx 0，而用户原在 `Anime` idx 2）❌、修后 **`Anime`**（同一项）、框内 ✅。回归集修前修后完全一致（`ArrowDown→Anime`、`ArrowUp→Movies`、`End→Documentaries`、`Home→Show`、`Enter→aria-pressed="true"`、`Esc→关闭且焦点回「浏览」`）；Step 4 走查对修后源码重跑（48 站、同序、仅 `body` 环绕站无环、3–9 项与第 10 项的 aria 管道全过、无 console 错误）；build clean（2.56s，零 warn/error）、`check-sfc-compile.mjs` exit 0。修法：Finding 1 —— 在既有「上级」`AppButton` 上加 `parentButtonRef`，并抽出 `focusListIndex(idx)` helper，回退链 `items[idx] || items[0] || parentButtonRef.value?.$el`（**未新增控件**；`AppButton` 的根就是 `<button>`，故 `$el` 即原生控件）；Finding 2 —— `load(path, { focusFirst, restoreIndex })` + `loaded` 标志。
- **Ruling 69（采纳实现者对我修法的纠正 —— 本轮第 4 次）**：我在修复指令里写「把 `focusFirst` 的交接**限定在成功路径上**」，**字面执行是错的**：`loading` 已经在途中把列表卸载了，失败时若跳过交接，焦点会再次落到 `BODY`，**错误路径精确复现 Finding 1**。实现者指出这一点并改用 `restoreIndex`（失败时 `dirs` 未变，故该索引仍指向同一个目录）—— 于是**同时**满足两条要求：焦点留在对话框内（Finding 1）**且**用户不被挪动（Finding 2 陈述的伤害）。**裁定：采纳。** 它还把 A3 的被聚焦目录刻意放在 idx 2（不是首项），使"被移到首项"与"保持同一项"可区分 —— 这是修前 `Show` vs 修后 `Anime` 差异可证的原因，方法上比我的指令更严谨。**这是本轮第 4 次由实现者用机制推理推翻我的指令**（前三次：Ruling 52 的理由、T1 评审推翻"不可达"、Ruling 66 的惰性机制），规律依旧稳固。
- **F4-T5 scoped re-review 已派发**（reviewer, sonnet；fix diff `review-359b995..377c080.diff`；findings = Finding 1 + Ruling 67 并入的那条 Minor；并要求判断那处 `restoreIndex` 偏离是否正当）。
- **F4-T5 scoped re-review：All findings addressed, no new Critical/Important breakage。** 两条均判 **ADDRESSED**：重要那条落在 `BrowseDialog.vue:24`（`ref="parentButtonRef"` 绑在既有「上级」`AppButton` 上，**未新增控件**）与 `:226` 的回退链 `(items[idx] || items[0] || parentButtonRef.value?.$el)?.focus()` —— 空叶子分支下 `listRef` 为 null、`items` 为 `[]`，故确实走到 fallback；Minor 那条落在 `:215` 的 `focusListIndex(loaded ? 0 : restoreIndex)` + `:178` 的 `restoreIndex: current`（`dirs` 只在成功路径赋值（`:202`），故失败时索引仍指向用户按 ArrowRight 的那个目录）。**偏离判 LEGITIMATE**（它独立复述了机制：`loading.value = true` 在首个 `await` 之前（`:192`）、模板的 `v-if="loading"`（`:51`）卸载列表，故**任何**下钻都让 `activeElement` 途经 `body`；跳过失败时的交接会逐字复现重要那条）。**新破坏：无** —— 它遍历了回退链每个分支都能到原生可聚焦元素（列表在 → `items[idx]`/`items[0]`；空叶子 → `$el`；对话框已关闭 → `?.focus()` 是 no-op 而 `AppModal.onClose` 已恢复触发器），并**在安装的 Vue 3.5.43 里核实 `$el` 路由**（prod `runtime-core` 为 `$el: i => i.vnode.el`；dev 的 `getDevRootFragmentEl` → `resolveDevRootEl` 对非 fragment 根返回 `undefined` 后回退到 `i.vnode.el`，即那个 `<button>`；`$el` 在 `publicPropertiesMap` 内故无需 `defineExpose`；同一路由已在 `TemplateConfig.vue:98` 使用），还核实了 `nextTick` 顺序（`finally` 的 `loading.value = false` 先入队，故失败路径上 `items[restoreIndex]` 存在而非陈旧）与 `restoreIndex` 不可能为负/陈旧。
- F4-T5 out-of-scope（记账）：①回退目标有个理论漏洞 —— 若某次下钻得到 `currentPath === rootPath`，「上级」按钮的 `:disabled`（`:27`）会让 `.focus()` 静默 no-op、焦点又落 `body`；**经下钻路径不可达**（下钻总落在当前目录的子目录），仅在后端路径规范化不一致时可达；②`probe-fix.mjs` 的 A1/A2/A3 只以报告表格形式复现、无原始捕获（`/tmp/er5/walkthrough-out.txt` 是唯一保存产物，其下钻段佐证修后行为：空叶子后 `activeElement: BUTTON`、Esc 恢复焦点到「浏览」）；③报告 §3「47 站」与 §10「48 站」实际一致（48 含一个 `body` 环绕边界站）。
- **F4-T5 完成**：`Task 5: complete (commits 87770a2..377c080, review clean)` —— 一轮修复（1 条 Important 本任务引入的回归 + 1 条并入的 Minor；含我一次错误修法的更正）。
- BASE for F4-T6: `377c080`。
- **F4-T6 实现完成**（commit `e7c90ec`，单文件 +3/−36；仅 `style.css` 暂存，dist 未动）。报告 DONE。自验：**Step 1 正则审计 0 命中**（ugrep 与 GNU grep 双重确认）→ 无需改写任何类名；build clean（0 warn/err）、`check-sfc-compile` 22 OK/exit 0；**规则级产物 CSS diff：362↔362、0 removed / 0 added，且 CSS 字节完全一致**；**live computed 样式对比：2694 元素 × 14 属性 = 37716 个值、跨 14 个 UI 状态、0 处不一致**（含两个 bit-clean 确定性对照）；brief 的两条 grep 各有一个 `--color-border-control` **前缀假阳性**（非真实幸存者，严格模式为空）；控制器要求的注释修正已含（`0.01ms` 值不变、假的 Vue `<Transition>` 理由换成真实理由）。
- **Ruling 70（T6 的实测结论，改变了对本任务价值的判断）：删除别名层可证明是惰性的 —— 第一期那层别名早已被 Tailwind tree-shake 至 0 字节。** 证据：删前删后的产物 CSS **逐字节相同**、规则级计数 362↔362、0 removed/0 added。含义：①本期 Review Focus 第 2 条（「删别名层后某个类名静默失效」）的风险**在产物层面早已不可达** —— 真正的闸门是 Step 1 的源码级正则审计（已 clean）；②本任务的实际价值是**源码卫生**（`style.css` 只剩一套 token、去掉一份会误导后续读者的脚手架），而非任何可见变化。这一点应在最终交付里如实说明（避免把"零产出差异的删除"讲成"消除了一个风险"）。— 代价：若判错（别名其实还在发射），规则级与字节级两道闸门会立刻暴露。
- **重要发现（带进 T7）：Tailwind 把所有 `hover:` 规则关在 `@media (hover: hover)` 之后，而普通 headless Chromium 里该媒体特性为假，且 CDP 的 `setEmulatedMedia` 无法覆盖它。** 实现者是靠重启 Chromium 并加 `--blink-settings=availableHoverTypes=2,primaryHoverType=2,availablePointerTypes=4,primaryPointerType=4` 才让 hover 变得可测，并在**两侧都用该 flag 重新捕获**（保持了对比的对称）。**T7 必须继承这条**：其 §15.5 的对比度抽检表里有一行是「白 | `danger` | 「清空」按钮**悬停态**」，且 §15.6 的 reduced-motion 走查第 6 项是「悬停表格行」—— 少了这个 flag，那两项会取到未生效 hover 的颜色而假通过。另记：前几期若有 hover 相关的实测在无此 flag 的普通 headless 下"通过"，其结论应视为未真正覆盖（本轮 T5 的走查第 6 项「行 hover」即属此类，已记）。
- F4-T6 concern（记账）：①删除惰性（见 Ruling 70）；②hover 测法（见上）；③**未覆盖面**（报告 §8 有理由）：已连接 OpenList 时的左栏变体、以及 `request.js` 那个硬编码调色板的回退 toast（与 Ruling 62 记的 `api/request.js:13` 是同一处 —— 它不走新 token，故本任务的对比自然覆盖不到它，属预期）。环境已清（:5199/:9222 释放，:5173 未碰）。
- **F4-T6 任务评审：Spec ✅ / Task quality: Approved，无 Critical 无 Important，2 条 Minor。** 评审的验证强度本轮最高：①**自己复跑了 Step 1 的判据正则**（brief 原样、`/usr/bin/grep -rnP` 过 `frontend/src`）→ **0 命中（exit 1）** —— 这是本任务自己的验收判据，由评审独立确认；②**补了正则的已知盲区**（brief 自己承认正则漏前缀）：`(from|via|to|decoration|border-x|border-y|border-t|border-r|border-b|border-l|border-s|border-e|inset-ring|bg-linear|bg-radial|bg-conic)-<旧名>` 过 `src/ index.html scripts/` → 0 命中；严格型 `var(--color-<旧名>)(?![a-z0-9-])` → 0 命中；③**动态类名藏不住幸存者** —— 所有 `:class` 绑定都经静态映射（`AppButton.vue:7`、`AppBadge.vue:4`、`Toast.vue:8`、`AppInput.vue:20`），唯一组合式 className 是 `api/request.js:13`，其 `colors[type]` 是字面调色板名（`bg-red-500`/`bg-emerald-500`/`bg-amber-500`/`bg-slate-700`）与别名无重叠；④删除范围**精确**（34 行 = 注释头 11 + 「不加 static」注 2 + 17 个别名声明的 `@theme` 块 + 尾随空行；两侧上下文未动，`@theme static` 的闭合 `}` 与 `html, body, #app` 都在）；⑤`grep -rn '@theme' src/ index.html` 只返回 `style.css:16` → **只剩一套 token**；⑥**从产物独立证明惰性**（三处 sha256 一致 + `cmp` IDENTICAL；且**删前的产物里就完全没有**别名 token 与别名工具类 —— 其完整 `--color-*` 集合是 20 个新 token 加 `white`/`red-500`/`emerald-500`/`amber-500`/`slate-700`）；⑦**提交的文件就是被测量的文件**（`sha256(frontend/src/style.css)` 与 `/tmp/er6/style.css.after-edit` 相同）—— 堵掉了"测量的和提交的不是同一份"这个常见缺口。
- **Ruling 71（注释措辞过宽，记账不修）**：`style.css:133` 的括号 `（transitionend / transitioncancel / animationend）` **过宽** —— transition 半边成立（零时长过渡不启动故不发 `transitionend`/`transitioncancel`），但**零时长动画仍会触发 end 事件**：评审**在 Chromium 152 里直接验了**（`animation-duration: 0s` 的元素同时记录 `animationstart` 与 `animationend`）。故该句读起来像"0 会抑制 `animationend`"，是假的。无行为后果（声明两侧都是 `0.01ms`），且控制器原先点出的那条假论断（Vue `<Transition>` 依赖 `transitionend`、元素会卡在离场态）已**完全消失**。修法：从括号里去掉 `animationend`（或拆句）。裁定：**Minor、不改**，交最终整支评审 triage —— 它零行为影响，且最终 fix wave 会一次性处理所有注释类 minor。**另记（评审的诚实披露）**：它指出 transition 那半边在本环境**无法机器验证**（其 harness 在 100ms 阳性对照下也没产生任何 transition 事件），故依赖标准做法与 `0s` 元素的静默（`transitionrun`/`start`/`end` 全缺）。
- F4-T6 minor（deferred，记账）：①上述 `animationend` 过宽（见 Ruling 71）；②`/tmp/er6/last-compare.json` 只持久化了确定性对照，主对比（`BEFORE_HOVER vs AFTER_HOVER`）只存在于报告文本里 —— 建议持久化（进程 nit，非代码缺陷）；③评审的两个 ⚠️（主对比输出未持久化；`/tmp/er6/before` 构建的来源）均**不改变结论**（字节相同的 CSS 蕴含相同的 computed 样式，且惰性另由"删前产物本就无别名"独立佐证）。
- **F4-T6 完成**：`Task 6: complete (commits 377c080..e7c90ec, review clean)`。
- BASE for F4-T7: `e7c90ec`。

---

## 最终整支评审的输入清单（controller 汇总，供 triage）

**A. 代码注释 / 文档与事实不符（不修，交 triage 决定是否在 merge 前一次性清掉）**

| # | 位置 | 问题 | 出处 |
|---|---|---|---|
| A1 | `src/style.css` 的 `@theme static` 注释 | 字节数写 72，本仓库实测 +98 | F1-T2 minor ① |
| A2 | `src/components/ui/AppPanel.vue:13` | 注释里有 `shadow-sm` **字面量** → Tailwind 会采集并产出**无人使用的规则** | 裁定 43 |
| A3 | `src/components/Toast.vue` 的离场过渡 | 代码 `0.18s`，而计划与 spec §11 均为 200ms | 裁定 48 |
| A4 | `src/stores/workspace.js:226` | 守卫理由的措辞不成立（裁定 27 已改为"防御性守卫"，裁定 29 指出其论证仍不成立） | 裁定 29 |
| A5 | `frontend/scripts/check-sfc-compile.mjs` docstring | 过度声称能抓"未定义函数"（编译器抓不到） | 裁定 17 |
| A6 | `src/lib/settingsSchema.js` 注释 | 引用了**不存在**的后端符号与路径 | 裁定 38 |
| A7 | `src/style.css:133` | 括号里的 `animationend` 过宽（零时长动画**仍会**触发它，评审判 Chromium 152 实测） | Ruling 71 |
| A8 | `src/App.vue` 的注释 | 症状描述曾错（裁定 24）；裁定 27 的修复轮已改，**需在 triage 时复核是否已消解** | 裁定 24 / 27 |

**B. 代码级 Minor（不改，交 triage）**

| # | 位置 | 问题 |
|---|---|---|
| B1 | `src/lib/settingsSchema.js:52-54` | `defaultTemplateId: ''` 原样透传，与 `conflictStrategy` 白名单风格不一致 —— **已由 Ruling 58 裁定不修**（开放引用 vs 闭合集，分层正确） |
| B2 | `src/components/TemplateConfig.vue` | `VARIABLE_DEFS[].example` 是死数据（可删或并进 chip 的 `title`） |
| B3 | `src/components/ui/AppCheckbox.vue` | 调用方传 `id` 会落到 `<label>` 而非 `<input>`；图标型复选框靠 `ariaLabel` prop 兜（已规避，但 `id` 场景不成立） |
| B4 | `src/views/SettingsView.vue` | `save()` 的 loading 态不可观测（`async` 内无 `await`，同一同步任务内完成） |
| B5 | `src/lib/settingsSchema.js` | 小数被静默 `Math.trunc`（`padRangeError(2.5)` 返回空）—— 正是该错误提示特性要消除的那类意外 |
| B6 | `src/components/ui/AppInput.vue` | `v-model.number` 在本原语上是惰性的（编译器产出 `modelModifiers`，但赋的是 `$event` 原值） |
| B7 | `src/components/ui/AppButton.vue` 等 | `text-white` 非设计 token（全站约定；将来收窄调色板时需补 `--color-white`） |
| B8 | `src/components/ui/AppButton.vue` | variant/size 映射无兜底（生产构建剥离 validator）；`type` 缺 validator；两张映射表写在 `<script setup>` 内、每实例重建 |
| B9 | `src/components/ui/AppButton.vue` | `loading` 同时置 `disabled` → 加载中也是 45% 透明 + `not-allowed` 光标（两条要求叠加的必然结果，值得判断是否要区分「工作中」与「不可用」） |
| B10 | `src/api/request.js:13` | **三处全局约束违规**：`rounded-lg`、`shadow-lg`、`transition-all duration-300`（`text-sm`=14px 合规）。该文件**不在 spec §14 的重构清单里**，是 spec §3 审计**漏掉的文件**（程序化创建 DOM 的 toast，不走 `.vue` 管线）→ 见 Ruling 62 |
| B11 | `src/components/FileTable.vue` | `h-[59.4px]` 是与字体度量耦合的硬编码值且无守卫（改 `py-2.5` 或行高会静默复活"跳布局"） |
| B12 | `src/components/FileTable.vue` | 空态的 `title` 在 `emptyState` 两分支里逐字相同（只有 `description`/`action` 变），读起来像标题本该随源不同 |
| B13 | `src/components/FileTable.vue` | 空态无 live region → 「清空」后它的出现不被播报 |
| B14 | `src/components/BrowseDialog.vue` | 空叶子回退的理论漏洞：若下钻得到 `currentPath === rootPath`，「上级」的 `:disabled` 会让 `.focus()` 静默 no-op（**经下钻路径不可达**） |
| B15 | 约束文本 vs 代码现实 | **`rounded-[4px]`（`AppCheckbox`/`SettingsView`/`BrowseDialog`/骨架块）与 `rounded-[6px]`（`AppTopBar.vue:21`）** 都真实存在，而 Global Constraints 只写「只允许 8/12」 → **应在最终交付里向用户建议修正约束措辞，而非逐个改 markup**（Ruling 60 + Ruling 68） |

**C. deferred-to-human（需要人的眼睛 / 耳朵 / 真实系统开关）**

- **读屏**：T3 走查第 10 项、T5 走查第 10 项（aria 管道已实测，实际播报未验）。
- **观感**：T1 走查 1/2/4/5/6/7（遮罩只有一个旋转图标、无第二个圆环、Esc 不关、指针捕获）；T4 走查 1/3/5 的感知一半（"直接出现/消失"的手感）；T6 未覆盖面（已连接 OpenList 时的左栏变体）。
- **真实系统开关**：T4 的 reduced-motion 用 CDP `setEmulatedMedia` 模拟（同一 media query，风险低），未在真实 GNOME 全局开关下走一遍。
- **hover 相关的历史结论需重验**：Tailwind 把 `hover:` 关在 `@media (hover: hover)` 后，普通 headless 中该特性为假且 `setEmulatedMedia` 无法覆盖 → **T5 走查第 6 项（行 hover）等早期 hover 结论应视为未真正覆盖**；T6 已用 `--blink-settings=…` 重测，T7 会继承该 flag。
- **spec §17 的三条已知遗留**（T7 Step 10 会声明）：假设置（`openlist.concurrency`/`requestInterval`）、无暗色主题、无前端测试设施。

**D. 跨期裁定中"不修"的（无需 triage，仅备查）**：裁定 5（F3-T8 跳过）、裁定 39/58（`''` 哨兵）、Ruling 62（`request.js`）、Ruling 50/51/53/54/55/56/63/64/65/66/67/70/71。
- **F4-T7 实现完成**（commit `aba9d96`，6 张 PNG，无代码改动 —— 验证未发现缺陷）。报告 DONE。**7/9 PASS、1 NOT-APPLICABLE、1 PARTIAL**，6/6 截图按精确尺寸产出（尺寸从 PNG 头读回）。逐项：**PASS §15.1** 构建（exit 0、`✓ built in 2.16s`、warn/error grep 空；另跑 `check-sfc-compile` 22/22 与 `check-settings-schema` 30/30）；**PARTIAL §15.2** 路径 1/2/5 对真实后端 + 磁盘跑通（路径 2：5/5 重命名、磁盘名与对话框逐条一致；路径 5：`chmod 555` 目录 → 失败=3、红底红字徽章、磁盘未动、无崩溃），OpenList 路径 3/4 = 「环境不可用，未验证」（5244/3000/8080 均 `curl 000`、后端 `/api/openlist/test` 报 "All connection attempts failed"、`docker ps` 权限拒绝、无 podman、无 alist/openlist 二进制）；**N/A §15.3** 补零闭环 —— 但它**照样实测了**：设置存下 `episodePadDigits:3` 且提示「设置已保存」，而 `preview` 载荷的键是 `[file_ids, source, path, template, folder_template, create_season_folder]`、**无 `episode_pad_digits`**，输出恒为 `S01E01`、`S01E001` 从不出现，且无 `/api/settings` 路由 —— 这是裁定 5 的直接证据；**PASS §15.4** 键盘 10/10（52 站一圈、51/52 有真实 accent 环、仅 body 环绕站无环）；**PASS §15.5** 对比度 9/9 渲染值 + 两个易错点正确（表头 `ink-2` `#4B5257`、每个输入框边框 `border-control` `#8A9199`、零 `line-strong`）；**PASS §15.6** reduced-motion 9/9；**PASS §15.7** 响应式 4/4（左栏展开 405px = 900 的 45.0%）；**PASS §15.8** 长表格 —— **硬判据 `scrollHeight - innerHeight` = 0**（328 行）、sticky 表头 delta 0、底栏钉在最大滚动处、`window.scrollTo(0,5000)` 被拒；**PASS §15.9** 6/6（1280×900 / 1280×900 / 900×900 / 375×812 / 1240×906 / 523×545）。进程已清（8000/5199/9333 释放，:5173 未碰）。
- **Ruling 72（交付物缺口，交最终 fix wave）：T7 Step 10 的验证清单没有进版本历史。** plan 的 Step 10 明确要求「把 Step 1–9 的结果整理成一份清单，**附在提交信息或 PR 描述里**」，而实现者把它写进了 `task-7-report.md` —— 该文件位于 `.superpowers/sdd/` 下，被同目录的 `.gitignore` 排除。于是这次花了大力气做的验证（328 行的硬判据、9/9 对比度、10/10 键盘、4/4 响应式、OpenList 环境不可用的三重证据）**在 git 历史里只剩一条 "docs: 前端重构改后截图留档"**，将来无人能从中看到结论；且本项目在本地分支上、没有 PR 描述这个载体。裁定：**交最终 fix wave 处理** —— 把验证结论写成仓库内的一个文件（或至少扩写该提交的信息），使结论随代码落档。— 代价：仓库多一个文档文件（或一次信息扩写），换来验证结论可被后人检索。
- **Ruling 73（plan 收尾表与实测冲突，以实测为准，必须在最终交付里纠正）**：plan 的「全系列完成后的状态」表第 11 行把「补零位数是假设置」标为 **✅ 后端 + 前端端到端打通（第三期，依赖后端计划）**，而 T7 的实测**直接否证**了它：前端根本没发该字段、后端也没有可接收它的路由（`/api/settings` 不存在）。plan 自己的 §前置 已声明此情形（「Task 8 若因后端未合入而跳过，本期 Task 7 第 3 项需标注为「不适用」」），故**收尾表那行与前置声明自相矛盾**，实测站在前置声明这一边。裁定：**最终交付以实测为准**，明确告知用户第 11 条**未闭环**，且给出未闭环的精确形态（前端不发、后端无路由、改动静默无效）与后续路径（另起一轮执行 `2026-09-25-backend-pad-digits.md`）。— 代价：若判错，用户会以为有一项未完成而实际已完成 —— 但证据是实测的三重事实，不成立。
- **F4-T7 concern（记账，进最终交付）**：①§13 补零闭环未闭合（见 Ruling 73）；②OpenList 在本环境**不可测**（无容器运行时、无二进制、无可达服务），故任何 OpenList 端到端声称都缺直接证据；③**整个 §15 验证不可重跑**（driver 只在 `/tmp/er7/`）—— 这是「无前端测试设施」遗留的直接后果，无法在 CI 重复；④截图与验证结论的载体见 Ruling 72。
- **F4-T7 任务评审：Spec ✅ / Task quality: Approved，无 Critical 无 Important，6 条 Minor。** 评审的验证深度本轮最高 —— 它去读了**原始证据**而非采信叙事：①**PNG 头逐个核**（`od -An -tu1 -j16 -N8`）→ 六张尺寸与 brief 表 **6/6 精确匹配**（1280×900 / 1280×900 / 900×900 / 375×812 / 1240×906 / 523×545）；②提交范围（`git show --name-only`）→ 6 × `A screenshot/after-*.png`，**零个 `frontend/src` 文件**、dist 未暂存；③**旧图完好**（三张仍被跟踪、mtime 为 9月25日、未被本提交修改；两对叠图尺寸真的匹配）；④**§15.3 的结论在代码层正确**（`workspace.js:265-272` 的 preview 载荷恰为 `file_ids, source, path, template, folder_template, create_season_folder`；`backend/app/models/api.py:34-41` 无 pad 字段；`backend/app/main.py:27-31` 只有 scanner/parser/renamer/template/openlist、**无 settings router**；`episode_pad_digits` 仅存于 `config.py:29` 并由 `template.py:28` 从 config 读；前端只把它写进 localStorage（`settings.js:32-51`））—— **值无处可去，判定正确**；⑤**§15.8 的结构前提端到端在位**（`App.vue:18` → `HomeView.vue:12/15/54` → `FileTable.vue:2/51` 的 `min-h-0` 链逐环点名，无缺环）；⑥**原始产物仍在磁盘且佐证报告**（`/tmp/build.log` 零 warn/error 且 `✓ built in 2.16s`；`step8.mjs:60,73,88` 算的正是字面 `documentElement.scrollHeight - innerHeight`；`step8-out.json` 记 0/0/0、328 行、`scrollerScrollTop 0→2400→18400`、header/aside/main/footer 的 y 固定；`/tmp/er7-long` 真有 328 文件）；⑦**对比度算术独立重算三行**（`#4B5257`/`#FFF`=7.95 vs 报告 7.94；`#FFF`/`#0F766E`=5.48；`#8A9199`/`#FFF`=3.19），且 `step5.mjs` 实现的是教科书式 WCAG 相对亮度、`S5.A.colourSchemeFlag {"hover":true,"pointer":true}` **证明 hover flag 确实生效**；⑧磁盘状态佐证路径 1/2/5（`/tmp/er-test/Season 01/` 恰是对话框里那五个名字；`/tmp/er7-faildir` 为 `dr-xr-xr-x` 且 3 个文件未改名；失败路径徽章 `rgb(253,236,234)`/`rgb(179,38,30)`）；⑨两个 node 脚本它自己跑过（都过）；⑩**六张截图逐个看过**，内容与声称状态一致；⑪OpenList「环境不可用」独立确认（`ss -ltn` 只有 631/5173 与两个 DNS stub、无 openlist/alist、`docker ps` 权限拒绝且用户在 wheel 组而非 docker 组）—— **是正当判定而非搪塞**。结论：**未发现任何虚假或无支撑的声称**。
- **Ruling 74（brief 内部两处截图范围冲突，已裁定）：以 Step 9 的六张枚举为准，不补 1024×900 与 768×900 两张。** Step 7 的措辞是「逐档截图」，而 Step 9 给出的是**命名 + 尺寸的交付物清单**（且其中两张刻意与旧图同尺寸以便叠图对比）。交付物的定义权在 Step 9；Step 7 的"逐档截图"是检查点描述。评审另指出 `after-mobile.png` 是在 `scrollLeft=150` 捕获、故复选框与原文件名列部分被切 —— 但那正是 plan Step 9 对该图的定义（「表格横向滚动状态」），**符合要求**。— 代价：若判错，用户想要 1024/768 两档的留档图需自行补拍（测量数据 `step7-out.json` 已有）。
- F4-T7 minor（记账，进最终 triage）：①**Step 10 的九项清单没进提交信息**（`aba9d96` 正文只有一行；表格只在 gitignored 的报告里）= 与 **Ruling 72 同一条**，评审独立发现；②Step 7 vs Step 9 的截图范围（见 Ruling 74）；③**§15.5 第一行是用替代对评的** —— 没有 rail 元素渲染裸 `ink`（`text-ink` 只出现在 `SettingsView.vue:71,82-88`、`ExecutingOverlay.vue:17`、`AppButton.vue:19,21`），故该行报的是 `ink-2`/`canvas`=7.41 与声明的 `ink`/`canvas`=16.61，两者都通过且偏离已在正文披露，但"9/9 通过"把一行算作"在 brief 指定处所测"→ 建议在汇总表加脚注；④Step 6 第 3 项的 `.toast-enter-active` reduce 值**不是实时读数**（`step6-out.json` 里为 `[]`，过渡太短未捕获；`1e-05s` 来自确定性交叉核对），报告描述了两种方法、属呈现问题；⑤**阶段收尾的 dist 提交仍欠着**（HEAD 只含图片、dist 仍脏）—— 按计划「阶段收尾」一节，它必须在最终 fix wave **之后**单独做（fix wave 可能再动 src → dist 会再变）；⑥路径 2 的磁盘证据比报告暗示的薄一层（`step2.mjs:127` 只记录 `readdirSync('/tmp/er-test')`），评审从存活磁盘状态确认了声称是真的。
- **F4-T7 完成**：`Task 7: complete (commits e7c90ec..aba9d96, review clean)`。
- **第四期七个任务全部完成**（T1 一轮修复、T5 一轮修复，其余零修复）。进入最终整支评审。

---

## 最终整支评审（opus，`d72f4c98..aba9d96`，12,696 行包）

**结论：Ready to merge? **With fixes**。3 条 Important + 9 条 Minor + 3 条计划/文档缺陷。无 Critical。**

- **Important #1（本轮最重的发现，且它显现在本分支自己交付的截图里）：季/集 number 输入框截断数值 —— 相对 base 的回归。** `FileTable.vue:65-66`（th `w-[72px] md:w-[80px]`）与 `AppInput.vue:19`（未抑制 spinner）叠加：1280px 下集单元格 80−32=48px，Chromium 的 `::-webkit-inner-spin-button` 占 ~17px、`px-2.5` 占 20px，只剩 ~11px 文本区 → **每个两位数集数被截断**：`after-home.png` 第 6 行的 `10` 渲染成 `1(`，328 行的 `after-home-long-table.png` **每行**的 `24` 渲染成 `2(`，375px 下 `after-mobile.png` 的季/集**完全空白**。base 的 th 是 `w-[90px]`（~21px 文本区，够两位）→ **回归**。评审在真实 Chromium 里独立复现（48px 的 number input 同样截断、`appearance: textfield` 后完整渲染）。**它躲过了五次任务评审与 T7 的截图检查** —— 后者甚至声称"六张截图逐个看过、内容与声称状态一致"，这暴露了"逐张看截图"这一验证方式的真实局限。
- **Important #2**：`frontend/dist` 与源码不符（HEAD 的 dist 是第三期的，`git show HEAD:frontend/dist/assets/index-DmXb8oVX.css | grep -c prefers-reduced-motion` → **0**）；正确构建**未暂存**在工作区。计划 §阶段收尾（line 990）已说明 `backend/app/main.py:60-73` 在单端口模式 serve `frontend/dist`，故检出本分支会 serve 一个没有第四期的前端。
- **Important #3**：**OpenList 挂载点选择器是死控件** —— `SourceConfigPanel.vue:67-73` 发 `mount-change` 带挂载点，`HomeView.vue:35` 映射到 `ws.onMountChange`，而 `workspace.js:210-212` **不读参数**、只清 `path`。于是 `olStore.selectedMount` 从不被 UI 写入：DOM select 显示用户的选择而 store 保留旧值 → 浏览对话框根在**前一个**挂载点、扫描与 `browseConfirm` 拿 `path` 与**陈旧**挂载点比较、陈旧值被持久化。**既有**（base 同形），但第二期重写了两侧，而 OL 路径从未被走过。两行修法。
- **Important #4**：§15 验证结果没进版本历史（= Ruling 72，评审独立发现同一条）。
- **评审对我一条依据的否证（我接受）**：我在 Global Constraints 写的「**`.vue` 注释里的类名会生成真实规则** → 死 CSS」**在 HEAD 上不可复现**。它做了两个方向的测试：整棵树里"只存在于注释"的类名 token 恰好两个（`AppPanel.vue:13` 的 `shadow-sm`、`FileTable.vue:74` 的 `animate-pulse`），**两者都不发射规则**；而 `shadow-lg` 确实发射，但来自 `api/request.js:13` 的**真实字符串字面量**而非注释，证明生成器工作、负结果有意义。它推断原测量被混淆（当年引用的两个样本 `rounded-[12px]` 与 `grid-rows-[56px_1fr]` **都出现在真实标记里**，很可能把标记自己生成的规则归因给了注释）。**闸门理由仍成立**（grep 闸门会为只在注释里的名字开绿灯，且这项卫生实践已在多期防止了误判），但"死 CSS"不得再作为已测事实复述。
- **评审纠正的路径错误（会浪费 fix wave 的时间）**：我的清单 A6/B1/B5 写 `src/lib/settingsSchema.js`，实际文件在 **`frontend/src/stores/settingsSchema.js`**（不存在 `src/lib/`）。
- **Ruling 75（最终评审的 fix wave 范围，已派发）**：**进本轮修复的 8 项** —— ①Important 季/集输入框截断（spinner 抑制 + 列宽 `md:w-[96px]`，要求量测文本区够 3 位）；②Important `onMountChange(mount)` 两行修法；③Important §15 记录落档为**仓库内文件** `docs/superpowers/verification/2026-09-25-frontend-refactor-phase4.md`（比只扩写提交信息更可检索，且解决了 Ruling 72 的关切）；④`Toast.vue:53` 的 `0.18s` → `0.2s`（评审判 must fix）；⑤`settingsSchema.js` 注释改写（引用不存在的后端符号）；⑥**`executeDryRun` 在 `finally` 里重置** —— 评审把它列为 Minor，**我提升为修复项**：它使「先试运行再执行」跳过遮罩的 200ms **进场**过渡（Vue `<Transition>` 首次挂载默认不播 enter），这是 spec §11「模态进出 200ms」的真实违反；连带更新 T1 那两处现已过期的注释（`onMounted` 守卫保留为防御性，但可达路径变成 watch）；⑦注释清理（A1 删字节数、A7 删 `animationend`、A4 删假从句）；⑧重拍受截断影响的 `after-home*.png` 与 `after-mobile.png`。**明确排除（绑定）**：不删 `api/request.js`、不动 `AppPanel.vue:13`、不加 h1/底栏标签/`aria-selected` 重构/去重对话框标题、不改 `buildPreview` 吞错、不让干跑显示遮罩、**不动任何 `rounded-[4px]`/`rounded-[6px]` markup**、不 `git add dist`、不派子代理。
- **Ruling 76（我接受评审对一条依据的否证，并已改正计划文本）**：已把第四期 plan 的 4 处文本改正 —— ①Global Constraints 的注释类名条：删除"实测 `.vue` 注释生成真实规则 → 死 CSS"的声称，加一条带日期的修正说明（保留闸门理由为唯一依据）；②Task 2 的骨架代码块去掉 `animate-pulse`、正文改为「不加」并注明连带 Step 3 第 7 项不适用；③收尾表第 11 行由 ✅ 改为 ⚠️ 未闭环（附精确形态与后续路径）。**这些编辑尚未提交**（避免与 fix wave 的提交交错）—— 与阶段收尾的 dist 提交一并或紧随其后提交。
- **Ruling 77（不在本轮做的，及其去处）**：①`api/request.js` 的删除（0 importer、三处违规、唯一的绿色"成功"幸存者）—— **不删**，因为它在 spec §14 的文件清单之外、属未授权范围；**在最终交付里向用户明确提出**并附"可安全删除"的事实；②B15 的约束文本修正 —— **不改 spec**（用户文档），已在 plan 与最终交付里建议用户把 spec §5.3 改成「4/6/8/12」的故事；③Minor 2（干跑无进度反馈）、Minor 4（底栏丢失可见标签）、Minor 5（无 h1、20/24px 档未用）、Minor 6（对话框标题播报两次）、Minor 7（`aria-pressed` 误用）、Minor 8（`buildPreview` 吞错 + `previewAll` 无条件报成功）—— 均**不修**，逐条列入最终交付（其中 Minor 8 要**显著**列出：它是唯一能让静默失败先于破坏性批处理的地方）。
- **Ruling 78（"Declined to judge" 八条的裁定）**：评审列出的 8 条其余项全部**接受为已声明的遗留**，无需动作 —— ①§13 补零闭环（另起后端计划）；②`openlist.concurrency`/`requestInterval` 假设置（spec §17 #1，设置页已有 UI 说明）；③无暗色主题（§17 #2）；④无前端测试设施（§17 #3，两个 node 断言脚本是其经认可的替代）；⑤`AppBottomBar` 置于 `HomeView` 而非 App shell（第二期的布局选择，内容确是 workspace 作用域、`/settings` 不该有这些动作）；⑥丢掉置信度列改用中性徽章（spec §5.2 驱动）；⑦截图范围（Ruling 74 已裁定）；⑧`api/request.js` 的死 toast（见 Ruling 77①）。
- **最终评审的 fix wave 完成**（7 个提交，`aba9d96..95cdabf`）：`51bc3f9` 季/集数字框不再裁切、`8a8880f` Toast 离场对齐 200ms、`b0a8508` §15 九项结论落成被跟踪文档、`101e927` 重拍携带裁切缺陷的截图、`0c32ba1` OpenList 挂载点写回 store、`4d71da8` 复位 `executeDryRun`、`95cdabf` 注释去伪。8 项全部落地。自验：`rm -rf dist && npm run build` exit 0、0 warn/error；两个检查脚本 22/22 与 30/30。#1 给了**像素级实测**（修复后可用文字宽 42px vs 3 位数 23.13px，`24`/`999` 的墨迹 28/43 img px；修复前同元素把 `24` 截到 20 img px、375px 下内宽 **0**），并实测原生 spinner 预留 **15.5px**（此前只是估算）；#6 用 `document.getAnimations()` 实测（执行时遮罩上有 opacity/200ms 过渡 + `overlay-enter-*` 类；预置修复前状态的对照组两者皆无）—— 这是对"过渡真的恢复了"的直接证据，而非"元素存在"。
- **我的独立核实（controller 复跑）**：7 个提交**无一触碰 `frontend/dist`**；6 张截图尺寸重拍后仍精确（1280×900 / 1280×900 / 375×812 / 1240×906 / 523×545 / 900×900）；索引干净（`git diff --cached` 空）；产物 CSS 含 `webkit-inner-spin-button`×3、`appearance:textfield`×4、`min-w-[96px]`×1、`md:w-[96px]`×1；旧 `index-DmXb8oVX` 已归零；**新 dist 已含 `prefers-reduced-motion`**（T7 曾指旧 dist 为 0）→ **Important #2 的实质已解决**，只差把它提交（阶段收尾）。工作区仅剩我的 plan 文档改动 + dist。
- fix wave 的三个 concern：①**它主动申报了一处越界** —— 375px 那半光靠 spinner + `md:w-[96px]` 修不掉（超约束表里 `w-*` 被完全忽略：实测列宽 72→128 恒为 54），故补 `min-w-[96px]`；不补则重拍的 `after-mobile.png` 仍是空白字段。**代价需评估**：`min-w` 会抬高窄屏 min-content，而本分支有既往前测（窄屏表宽 ≈783.88px、横滑距离 409px，见 F2-T7 裁定 37）→ 已作为具名风险交给 re-review 判断窄屏仍可接受；②**我给的判据无效**：`scrollWidth <= clientWidth` 在本题前后都是 62（spinner 也计入 content-box），它换成"解析式 + 像素式"两条并说明 —— **本轮第 5 次我的判据/机制被纠正**（前四次：Ruling 52 的理由、T1 评审推翻"不可达"、Ruling 66 的惰性机制、Ruling 69 的成功路径守卫）；③`appearance:textfield` 使**全站**数字框失去原生上下箭头（含设置页）—— 这是最终评审所提修法的固有后果，需判断可接受性与是否有恰当披露。
- fix wave 明确的未覆盖面（记账）：OpenList 端到端仍不可验证（只有 store 级 + 静态链路证据）、补零闭环仍不通、本轮驱动同样是一次性的且不在仓库里。
- **fix wave 的 scoped re-review 已派发**（reviewer, sonnet；`review-aba9d96..95cdabf.diff`，7 提交 / 31,523 B；8 条 finding 逐条 verdict；两个具名风险 = `min-w-[96px]` 对窄屏 min-content 的影响、`appearance:textfield` 的全站副作用）。
- **fix wave 的 scoped re-review：All findings addressed, no new Critical/Important breakage。** 8 条全部 **ADDRESSED**，逐条给了 file:line 证据，且多数是**独立复跑**而非采信报告：①`AppInput.vue:68` 的 `NUMBER_CLASSES`（含 `[appearance:textfield]` 与两条 `::-webkit-*-spin-button:appearance-none`）经确认绑在**真实 `<input>`** 上（`:20` 的直接 `:class`，非 fallthrough —— 它专门核了 `AppInput.vue:4-6` 的 caveat 不适用），产物 CSS 里四条规则俱在；容量与控件一致（`:max="999"`），且**逐张看了重拍的图**（1280 下第 6 行的 `10` 完整、328 行表里 227/226/225 三位数完整、375 下原本空白的字段现在有 `1`/`8`/`10`/`9`）；②`onMountChange` 的写路径**静态端到端**核实（`SourceConfigPanel` 的 AppSelect → `$emit('mount-change', $event)` → `HomeView:35` → `workspace.js:214`），并确认持久化由 `openlist.js:44-57` 的 deep watcher 接续；③新文档 119 行，它核了引用（`config.py:29-30` 确无钳制、`utils.py:20-21` 是裸 `zfill`、`template.py:23,28` 消费 config）→ §2(a) 的"前端钳制是唯一防线"为真；④`Toast.vue:53` 已是 `0.2s`，产物里 `HomeView-*.css` 确认；⑤`grep -rn "PAD_MIN\|PAD_MAX" backend/` 无输出，新注释措辞准确；⑥`workspace.js:388` 的复位在 `finally` 里、**无竞态**（execute 时两个 ref 同 tick 同步写，复位后组件已挂载且 `modelValue=false`，下一次 execute 经 `flush:'post'` 的 watch 翻转，焦点与 200ms 进场都落在那里），且**守卫未被删**（`ExecutingOverlay.vue:64-66` 保留）；⑦三处注释俱改（`style.css:15` 无 `72 字节`、`:133` 只剩 `transitionend / transitioncancel`、`workspace.js:246` 去掉假从句）；⑧三张缺陷图重拍，另有 `after-home-rail-collapsed` 与 `after-settings` 也重拍（后者因设置页的数字框同样失去 spinner —— 它判断这与本次改动同源、刷新是**一致而非越界**），而 `after-browse.png` 字节相同、三张改造前的旧图未动。
- **re-review 对越界步骤的判定：legitimate completion, not overreach。** 它独立论证：375 下表格超约束（`min-w-[560px]` vs 375 视口，叠加 ~784px 的内容 min-width），故 `w-*` 只是被 auto 布局丢弃的"提示"，**只有最小值能钉住列**；成本它从**本分支自己记录的 min-content** 算出（`frontend-2-shell.md:1820` 的 783.88px = 48+351.83+54+54+80+192.03+~4）：两列 54→96 ⇒ 窄屏 min-content ≈868px、横滑距离 ~409→~493px（**+84px**）；并确认 §15.7 的每条不变量都不被违反（`bodyScrollW 375 === innerWidth`、底栏 `bottomGap 0` 且不换行、顶栏 h=56、scroll 0 时仍可见复选框与原文件名），页面本身仍不溢出。它另指出**存在更省的钉法**（季 `min-w-[72px]` / 集 `min-w-[80px]` → 只 +44px），选 96 是为跨断点无列跳变与余量，**属可辩护但多花约 40px**。
- **Ruling 79（residual 两条 Minor，均 park 并记入交付）**：①**全站 spinner 移除的披露缺口** —— `AppInput.vue:68` 对每个 `type="number"` 抑制 spinner，re-review 核实全站仅 6 个 number 输入点（`SettingsView.vue:14,23,51,58` 与 `FileTable.vue:152,163`）且全部走 `AppInput`、无绕过；可接受（设置页字段全宽、值仍可输入、`min`/`max`/`step` 未变、**键盘方向键步进仍工作**，只失去"点击步进"），但 `AppInput.vue:60-67` 只解释了窄表理由、未声明全站意图与其触及设置页。park（非阻塞、零行为影响）；**在最终交付里向用户明确列出**（一条注释即可闭合，但 controller 不改代码）。②**窄屏 min-content +~84px 的成本** —— 已量化于上，非缺陷；同样 park 并列交付。
- **re-review 的三条 out-of-scope（记录，不重开 finding 6）**：①**并发 dry-run → execute 仍可跳过进场过渡**（既有、本次未改）：dry run 期间 `executing` 为 false 且两个底栏按钮**不为它禁用**（`AppBottomBar.vue:25,30-31` 只看 `selectedCount`），故能在 dry run 在飞时启动 execute → 同 tick 写 `executing=true, executeDryRun=false` → 重挂载且 `modelValue` 已 true，走同一条 mount-while-open 路径；且 dry run 自己的 `finally` 会覆盖 `executing`、提前关闭遮罩。**修复前后行为相同**，规定的修法不覆盖它 —— 记入交付。②**375px 超约束表的根因未动**：`FileTable.vue:137-138` 给原文件名 ~351.83px min-content、`:180` ~192.03px，使 `min-w-[560px]` 惰性、迫使 `min-w-[96px]` 变通；`frontend-2-shell.md:1820` 把该 markup 工作记给了第三期，**显然没落地** —— 记入交付（变通本身正确）。③`ExecutingOverlay` 的 `dryRun` prop 是死的（既有、未动）。
- **阶段收尾**：①提交按最终评审实测更正的计划文本（4 处：Global Constraints 的注释类名条、Task 2 的骨架 `animate-pulse` 代码块与正文、收尾表第 11 行）；②`npm run build` 后提交 `frontend/dist`（计划「阶段收尾」一节；`backend/app/main.py:60-73` 在单端口模式 serve 它，故必须与源码一致 —— 这正是 Important #2 的实质）。§15 的结论已由 `docs/superpowers/verification/2026-09-25-frontend-refactor-phase4.md` 落档，故提交信息不必再承载它。
